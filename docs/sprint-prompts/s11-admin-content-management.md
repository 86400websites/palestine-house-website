# S11 — Admin content management

| | |
|---|---|
| **Status** | ⬜ Ready to run (not started) |
| **Branch** | `claude/sprint-s11-admin-content-management` (from latest `main`) |
| **Decision** | Scheduled by the **D-S10-b** Stage-3 replan (2026-06-26); the admin-management UI fold-in is authorized by **D-S10-a**. Record sprint-specific decisions **D-S11-a … D-S11-i** in `PROJECT-STATUS.md` §4 as each is scoped/resolved at the gate. |
| **Goal** | Give HQ a CRUD admin panel at `/admin/content` — behind the existing `is_admin()` gate — to add/update/delete the 30 element bodies, academy videos, and resources/templates, plus add/remove admins, through hardened RLS-safe write RPCs, so the client self-manages all private-page content without scripts. |

> This is the **ready-to-run brief** (not a post-merge record). Paste the prompt below into a fresh Claude Code session to execute S11 in owner-gated sub-steps. Save the completed record (Mode B, `/sprint-prompt save`) **after** the PR merges. This is a **risky DB sprint** (new hardened write RPCs over gated content + the admin table) → the independent Codex review under "## Independent review (required)" is mandatory before merge. Scope + exit gate: `ROADMAP.md` §B (S11); plan rationale + decisions: `PROJECT-STATUS.md` §1/§4. **Depends on S5 (content schema) and S10 — both must be merged before this runs.**

## Scope (12 gated sub-steps)

1. **(11-1)** Extend the admin nav — add a single **Content** item → `/admin/content` to the NAV array in `src/components/admin/admin-shell.tsx`; logo/brand/topbar/logout unchanged. `AdminShell` currently hardcodes `active="approvals"` (set in `src/app/admin/layout.tsx`); make the active nav key **route-aware** (or render the `/admin/content` subtree with `active="content"`) so the Content item highlights correctly **without** changing the Approvals highlight. Confirm anon → `/login`, authenticated non-admin → 404 `notFound()` on `/admin/content`. (Smallest gating change; unblocks the route.)
2. **(11-2)** Create the gated route shell — `src/app/admin/content/page.tsx` (+ a `layout.tsx` if the `/admin` layout's `is_admin()` gate doesn't already cover the subtree, or if the content subtree needs `active="content"`); copy the server-side `is_admin()` gate pattern from `src/app/admin/layout.tsx`. Use the **approved** heading **"Content admin."** (from `docs/page-copy/04-admin/admin-content.md` — never invent a heading) for a hub with sections: **Elements · Academy · Resources · Admins**. **Canon-vs-scope reconciliation:** the canon (`admin-content.md`) lists a **"Live Programming sessions"** section, but S11 scope **excludes programming** (already managed via the S9 `/programming` tool). CLAUDE.md forbids silently diverging where copy and architecture disagree — record this as a **D-S11** decision in `PROJECT-STATUS.md` §4 (and update `admin-content.md` if the owner approves dropping/relabelling that section). Reconcile the section labels the same way: the canon groups **Topics & Academy videos** / **Resources & templates**; this hub splits Elements / Academy / Resources — confirm the grouping with the owner before shipping, do not silently diverge. (Route gate + structure.)
3. **(11-3)** Migration **0023** — admin **content** read/write RPCs (`admin_list_elements` · `admin_get_element` · `admin_upsert_element` · `admin_delete_element` · `admin_list_academy_modules` · `admin_get_academy_module` · `admin_upsert_academy_module` · `admin_delete_academy_module` · `admin_list_resources` · `admin_get_resource` · `admin_upsert_resource` · `admin_delete_resource`). All `SECURITY DEFINER`, `search_path=''`, fully-qualified objects, in-function `is_admin()` guard, narrow returns, idempotent upsert on natural keys, EXECUTE revoked from public/anon + granted to authenticated. **Caution:** `admin_delete_element` cascades to the 1:1 `academy_modules` row (`0015` `ON DELETE CASCADE`) — confirm this is intended behaviour at the gate. Write the matching `0023…down.sql` (drop-if-exists, re-runnable) + `S11_verify_TEST_db_only.sql`. Apply to **TEST** via `supabase-test` MCP; role-verify. (Content CRUD RPCs.)
4. **(11-4)** Migration **0024** — admin **management** RPCs (`admin_list_admins` · `admin_add_admin(p_user_id)` · `admin_remove_admin(p_user_id)`); same hardening; `admin_remove_admin` **refuses to remove yourself or the last remaining admin** (clear neutral message). Write `0024…down.sql` + extend the TEST verify script. Apply to **TEST**; role-verify (admin can manage; non-admin denied; lockout guards fire). (Admin-management RPCs — old-S10 admin UI folds in here.)
5. **(11-5)** Server Actions — `src/lib/admin/content-actions.ts` (mirror `src/lib/admin/actions.ts`): one action per write (upsert/delete × elements/academy/resources, add/remove admin). Each: zod-validate input → re-check session + `is_admin()` server-side → call the matching `admin_*` RPC → `revalidatePath` → return `{ ok, message }`. Never called from a Client Component; neutral errors only. (Admin write actions, defence-in-depth.)
6. **(11-6)** Elements CRUD UI — `src/app/admin/content/elements/page.tsx` (reuse the `approvals-queue.tsx` table-list + detail-panel + form architecture). Load via `admin_list_elements()`; row → detail panel with the `*_md` textareas (overview / simple guide / watch-out-for) + metadata; Save/Delete call the Server Actions. Use the **approved action/confirmation labels** from `admin-content.md` verbatim (**Add · Edit · Save · Saved.** etc.); only **NEW** strings the canon does not cover (inline error/empty states) are drafted per brand-voice, owner-approved at the gate, then written back to the canon. (Elements CRUD.)
7. **(11-7)** Academy CRUD UI — `src/app/admin/content/academy/page.tsx`. Table of modules by element title; detail form with `youtube_url` + `body_md` (script) textareas + metadata. youtube_url + body_md only — **no** cover-image / file upload (D-S11-d). (Academy CRUD.)
8. **(11-8)** Resources CRUD UI — `src/app/admin/content/resources/page.tsx`. Table with title / type / focus area / element; detail form = **metadata only** (raw `storage_bucket` / `storage_path` never rendered to the browser). Per D-S11-c, file replacement stays out of the UI for MVP (re-ingest path) unless the owner approves a minimal "replace file" flow. (Resources CRUD.)
9. **(11-9)** Admin-management UI — `src/app/admin/content/admins/page.tsx`. List via `admin_list_admins()` (email + created_at); add-by-email form (looks up the auth user, handles "user not found" / "already admin"); remove button (guards self / last-admin with a clear message). (Admin add/remove/see — old-S10 folds in.)
10. **(11-10)** RPC hardening self-audit + local verification — re-read every `admin_*` RPC against the §15 invariants (SECURITY DEFINER · `search_path=''` · fully-qualified · in-function `is_admin()` · narrow returns · revoke→grant · idempotent upsert · down-SQL re-runnable). `pnpm run typecheck && pnpm run lint && pnpm run build`; spot-check `/admin/content/*` gating (anon → `/login`, non-admin → 404) and that each CRUD form submits + revalidates. (Hardening + local gate.)
11. **(11-11)** TEST verification + archive — apply 0023 + 0024 (both up) to TEST via `supabase-test` MCP; run `S11_verify_TEST_db_only.sql` (role matrix: anon/pending/approved denied; admin can list/read/write/delete; non-admin cannot add/remove admins; lockout guards fire; no data leakage incl. hidden storage paths + no PII beyond email). Save the final `0023.up/.down`, `0024.up/.down`, `S11_verify_TEST_db_only.sql`, `S11_verify_PROD_safe_readonly.sql` to `supabase/sql/migrations/` + `supabase/sql/verification/`. (TEST proof + migration archive — owner hand-applies to PROD per WORKFLOW §14.)
12. **(11-12)** Sprint exit gate — full-diff review of the whole sprint (content UI + admin UI + RPCs + migrations); confirm no new secrets, no RLS regression, `is_admin()` enforced on every admin request, no data leakage, approval gate (`is_approved()`) untouched, no public-page changes, no new rate-limit/Turnstile (deferred to S14). `git status` clean (no `.env.local`). typecheck/lint/build green. Update `docs/PROJECT-STATUS.md` (§1, §2, §4 decisions, change log) + tick S11 in `docs/ROADMAP.md`. (Security/correctness gate + docs.)

## Prompt used

```text
You are my senior engineer for the Palestine House website, working in Claude Code.

Session start: read docs/PROJECT-STATUS.md §1–§2, then the S11 sprint scope + exit gate in docs/ROADMAP.md §B. CLAUDE.md governs everything below. This is a DATABASE sprint — before any database step, also read docs/WORKFLOW.md §14 (database change protocol) and docs/SUPABASE-MCP-SAFETY.md (the MCP rulebook), and skim supabase/sql/README.md so the migration numbering + verify-script discipline is exact.

Sprint: S11 — Admin content management
Branch: claude/sprint-s11-admin-content-management (create from latest main)

Goal:
Give HQ a CRUD admin panel at /admin/content, behind the existing is_admin() gate, so the client self-manages all private-page content without scripts:
- Add / update / delete the 30 element topic bodies (overview_md, simple_guide_md, watch_out_for_md + metadata), the academy modules (youtube_url + body_md script), and the resources/templates metadata.
- Add / remove / see admins (the old-S10 admin-management UI folds in here).
All writes go through NEW hardened SECURITY DEFINER RPCs gated on is_admin(); the content tables stay RLS default-deny; the public/approved read projections (get_elements / get_element / get_academy_modules / get_resources) are untouched and must keep returning correct data. No schema changes to the auth/approval gates; no new env vars; no rate-limit/Turnstile (that is S14).

Execute in gated sub-steps (one owner gate after each):
1. (11-1) Add a single "Content" nav item → /admin/content to the NAV array in src/components/admin/admin-shell.tsx. Leave logo/brand/topbar/logout untouched. AdminShell currently hardcodes active="approvals" (passed from src/app/admin/layout.tsx) — make the active nav key route-aware (or render the /admin/content subtree with active="content") so the Content item highlights correctly WITHOUT changing the Approvals highlight. Confirm anon → /login and authenticated non-admin → 404 notFound() on the new route (file location is not access control).
2. (11-2) Create the gated route shell: src/app/admin/content/page.tsx (+ a layout.tsx only if /admin/layout.tsx's is_admin() gate does not already cover the subtree, OR if the content subtree needs active="content" — verify first). Copy the server-side is_admin() gate pattern from src/app/admin/layout.tsx. Render the hub using the APPROVED heading "Content admin." from docs/page-copy/04-admin/admin-content.md (never invent a heading), linking the sections: Elements · Academy · Resources · Admins. CANON-VS-SCOPE: admin-content.md lists a "Live Programming sessions" section, but S11 scope EXCLUDES programming (already managed via the S9 /programming tool). Do NOT silently diverge — CLAUDE.md requires recording a copy-vs-architecture conflict in PROJECT-STATUS.md §4 as a D-S11 decision (and update admin-content.md only if I approve dropping/relabelling that section). Same for the section grouping: the canon groups "Topics & Academy videos" and "Resources & templates"; this hub splits Elements / Academy / Resources — show me the grouping and WAIT for approval before shipping.
3. (11-3) Migration 0023_admin_content_rpcs: the admin content read/write RPCs — admin_list_elements(), admin_get_element(p_slug), admin_upsert_element(...), admin_delete_element(p_slug); admin_list_academy_modules(), admin_get_academy_module(p_element_id), admin_upsert_academy_module(...), admin_delete_academy_module(p_element_id); admin_list_resources(), admin_get_resource(p_id), admin_upsert_resource(...), admin_delete_resource(p_id). Inspect the live schema on supabase-test FIRST (elements / academy_modules / resources columns + natural keys) — never guess names. Every RPC: SECURITY DEFINER, search_path='', fully-qualified objects, is_admin() checked INSIDE the function (not from an argument), narrow returns, EXECUTE revoked from public/anon then granted to authenticated only; upserts idempotent on natural keys (elements.slug, academy_modules.element_id, resources(storage_bucket, storage_path)). The list/get RPCs that feed the table must NOT return raw storage paths (admin_get_resource may, for the server action only). CAUTION: admin_delete_element cascades to the 1:1 academy_modules row (0015 academy_modules.element_id is UNIQUE … ON DELETE CASCADE) — flag this delete-RPC behaviour at the gate and confirm it is intended. Write 0023_admin_content_rpcs.down.sql (drop function if exists … re-runnable) + supabase/sql/verification/S11_verify_TEST_db_only.sql (role matrix). Apply 0023 to TEST via supabase-test MCP, read back, iterate to zero errors.
4. (11-4) Migration 0024_admin_management_rpcs: admin_list_admins() (returns the auth user id + email + created_at — email only, no other PII; the admins table is user_id + created_at only, so JOIN auth.users for the email), admin_add_admin(p_user_id) (is_admin()-gated; validates the user exists in auth.users; refuses a duplicate), admin_remove_admin(p_user_id) (is_admin()-gated; REFUSES if removing yourself or if it would leave zero admins — raise a clear neutral message; check the count before delete). Same hardening pattern as 0009/0020. Write 0024…down.sql + extend S11_verify_TEST_db_only.sql with the management role matrix + the lockout guards. Apply 0024 to TEST; role-verify. Do NOT modify the is_admin() function or the admins table schema (0003).
5. (11-5) Create src/lib/admin/content-actions.ts (mirror src/lib/admin/actions.ts). One Server Action per write: upsert/delete for elements, academy, resources, plus add/remove admin. Each action: zod-validate the input → re-check the session + is_admin() server-side → call the matching admin_* RPC → revalidatePath the affected admin + gated pages → return { ok, message }. No action is callable from a Client Component; errors are neutral (never expose DB state or upstream bodies).
6. (11-6) Elements CRUD UI: src/app/admin/content/elements/page.tsx, reusing the src/components/admin/approvals-queue.tsx architecture (table list + detail sidebar + action forms via Server Actions + useActionState). Load via admin_list_elements(); a row opens a detail panel with the *_md textareas (overview / simple guide / watch-out-for) + metadata fields; Save + Delete call the Server Actions. USE the APPROVED action/confirmation labels from docs/page-copy/04-admin/admin-content.md VERBATIM (Add · Edit · Save · Saved., etc.). Only DRAFT a NEW string the canon does not cover (inline error/empty states) — per brand-voice, WAIT for my approval, then write it to docs/page-copy/04-admin/admin-content.md.
7. (11-7) Academy CRUD UI: src/app/admin/content/academy/page.tsx. Table of modules keyed by element title; detail form with youtube_url + body_md (script) textareas + metadata. youtube_url + body_md only — NO cover-image or file upload (D-S11-d). Validate the youtube_url the same way the publish tool does (reuse src/lib/live/youtube.ts if it fits) before save.
8. (11-8) Resources CRUD UI: src/app/admin/content/resources/page.tsx. Table with title / type / focus area / element; the detail form edits METADATA only — the raw storage_bucket / storage_path must never render in the browser. Template/booklet FILE replacement stays out of the UI for MVP (the existing scripts/ingest-content.ts re-ingest path); propose a minimal "replace file" flow only if I ask for it (D-S11-c).
9. (11-9) Admin-management UI: src/app/admin/content/admins/page.tsx. List via admin_list_admins() (email + created_at); an add-by-email form that looks up the auth user then calls admin_add_admin, handling "user not found" + "already an admin" gracefully; a remove control per row that surfaces the self / last-admin guard message. This is the old-S10 admin-management UI folding in.
10. (11-10) Hardening self-audit + local verification: re-read every admin_* RPC and confirm each is SECURITY DEFINER, search_path='', fully-qualified, in-function is_admin() (auth.uid() where the actor matters), narrow returns, revoke→grant, idempotent upsert, with a re-runnable .down.sql. Run pnpm run typecheck && pnpm run lint && pnpm run build; spot-check /admin/content/* gating (anon → /login, non-admin → 404) and that every CRUD form submits + revalidates.
11. (11-11) TEST verification + archive: with 0023 + 0024 applied to TEST, run S11_verify_TEST_db_only.sql and confirm the full role matrix — anon/pending/approved are denied every admin_* RPC; an admin can list/read/upsert/delete elements, academy, resources; a non-admin cannot add or remove admins; the self / last-admin guards fire; no data leakage (no storage paths in list returns, no PII beyond email). Save the final 0023.up/.down, 0024.up/.down, S11_verify_TEST_db_only.sql, and S11_verify_PROD_safe_readonly.sql into supabase/sql/migrations/ + supabase/sql/verification/. I (the owner) hand-apply 0023 then 0024 to PRODUCTION via the Supabase SQL Editor (WORKFLOW §14) — you NEVER write production; after I apply, you verify PROD read-only via supabase-prod-readonly.
12. (11-12) Sprint exit gate — full-diff review of the whole sprint (content UI + admin UI + RPCs + migrations). Confirm: no new secrets and .env.local untracked; no RLS regression (content tables + admins stay default-deny, no client policies added); is_admin() enforced server-side on every admin request; no data leakage; the approval gate is_approved() and the public/approved read RPCs are untouched and still correct; no public-page changes; no new rate-limit/Turnstile. Then update docs/PROJECT-STATUS.md (§1, §2, §4 decisions D-S11-a…i, change log) and tick S11 in docs/ROADMAP.md §B.

Per-step protocol (every sub-step, no exceptions):
1. Read the exact input(s) for this sub-step BEFORE coding — for a DB step, inspect the live schema on supabase-test first (never guess column/RPC names).
2. Build it: smallest safe change, one focused concern.
3. Verify: pnpm run typecheck && pnpm run lint && pnpm run build; spot-check the affected admin routes and confirm nothing else broke. For a DB step, also apply to TEST via the supabase-test MCP and read back to zero errors.
4. Self-review the diff for bugs and gating holes, and fix them before committing (full review at the exit gate).
5. Commit AND push to the task branch — every sub-step, so I can review live in the open PR.
6. Report in ≤6 lines: what shipped, checks run, anything flagged — then STOP and WAIT for "proceed". Never start the next sub-step without it. For ANY new user-facing string AND ANY schema/scope decision (D-S11-a…i — e.g. how *_md is edited, markdown UX, resource file upload, resource versioning, content grouping, the admin-list read, the canon-vs-scope Live-Programming reconciliation), show me the draft / the choice and WAIT for my approval before shipping.

Owner remote commands: "proceed" = next step · "pause" = hold · "status" = where are we ·
"fix <thing>" = fix before continuing · "skip to <n>" = jump (record the skip in PROJECT-STATUS.md).

Locked inputs (read before coding; never invent copy or design values):
- The admin copy canon for this screen IS docs/page-copy/04-admin/admin-content.md — use its approved strings VERBATIM (heading "Content admin.", the section labels "Topics & Academy videos" / "Resources & templates" / "Live Programming sessions", the action labels Add / Edit / Publish / Unpublish / Save, and the confirmations Saved. / Published. / Unpublished.). Only draft NEW strings it does not cover (e.g. inline error/empty states) per brand-voice (docs/page-copy/00-global/brand-voice.md — warm, short, concrete; never charity tone, hype, slogans, or filler; no exclamation marks) with my approval, then add them to that canon. CANON-VS-SCOPE: the canon lists a "Live Programming sessions" section but S11 excludes programming — record the reconciliation as a D-S11 decision in PROJECT-STATUS.md §4; do not silently drop or keep it.
- The content being managed is real, locked content — the 10 focus areas / 30 topics / academy modules / 267 templates + 2 booklets: docs/page-copy/06-elements/_index.md and the source under docs/source-assets/. NEVER invent, rename, or drop a topic/resource — admin CRUD edits existing rows; new rows use the same shape.
- DB pattern to copy VERBATIM (structure, not content): supabase/sql/migrations/0009_admin_approval_rpcs.up.sql (admin_set_application_status — in-function is_admin() gate, revoke→grant) and 0020_programming_publish.up.sql (write RPC hardening: search_path='', auth checks, left()-capping, atomicity). The gate itself (0003 is_admin(); admins table = user_id + created_at only) is used, never modified — JOIN auth.users for an admin's email.
- UI pattern to reuse: src/app/admin/layout.tsx (server is_admin() gate), src/components/admin/admin-shell.tsx (nav), src/components/admin/approvals-queue.tsx (table+detail+forms), src/lib/admin/actions.ts (Server Action shape). Design: reuse the existing admin chrome + design-system tokens (DESIGN §3) + S8 premium kit (DESIGN §14). No new design language, no new mockups, no new dependencies.
- Proof numbers are fixed: 10 · 30 · 200+ · 267 · 120-day launch. Header/footer chrome is locked.
- Migration numbering: latest applied is 0022 (verify with supabase/sql/README.md + the migrations folder before naming files) — this sprint adds 0023 then 0024. Never reuse or renumber; an applied migration is immutable.

Before editing:
1. Inspect the repo (src/app/admin/*, src/components/admin/*, src/lib/admin/*, supabase/sql/migrations/, scripts/ingest-content.ts) and the live TEST schema (elements / academy_modules / resources / admins) via the supabase-test MCP, and read every input above.
2. Propose a short plan and confirm scope before changing files — especially the /admin/content hub shape + the canon-vs-scope Live-Programming reconciliation (11-2), the 0023/0024 RPC signatures + natural keys (11-3/11-4), and any of the D-S11 decisions that affect the build.

While editing (sprint-specific safety rules — non-negotiable):
- Every /admin route stays behind the server-side is_admin() gate: anon → /login, authenticated non-admin → 404 notFound(). File location is not access control — the gate is re-checked on every admin request and again inside every Server Action (defence in depth).
- ALL content writes go through the new hardened SECURITY DEFINER RPCs: search_path='', fully-qualified objects, is_admin()/auth.uid() authorization decided INSIDE the function (never from arguments), narrow returns, EXECUTE revoked from public/anon then granted to authenticated — mirroring admin_set_application_status (0009) and set_my_account (0018) / publish_programming_session (0020). The content tables (elements / academy_modules / resources) and admins stay RLS default-deny with NO client policy. If any new table is added (none expected), it ships RLS default-deny in the same file.
- Ship versioned up-SQL + a matching .down.sql + RLS in the PR. Apply to the TEST project FIRST via supabase-test (read+write) and role-verify; then I apply to PRODUCTION by hand. supabase-prod-readonly is READ-ONLY — NEVER write production through any channel. Follow docs/SUPABASE-MCP-SAFETY.md and WORKFLOW §14 exactly.
- Content edits must NOT corrupt the anon/approved read projections — get_elements / get_element / get_academy_modules / get_resources / public_programming_sessions stay untouched and keep returning correct rows after an upsert. Upserts are idempotent on natural keys so a re-run never changes ids or orphans rows. Mind the cascade: academy_modules.element_id is a 1:1 UNIQUE FK ON DELETE CASCADE (0015), so admin_delete_element also removes that topic's academy module — confirm intended at the gate.
- Any Storage admin write (templates = private bucket, booklets = public) stays server-side; the secret storage path NEVER reaches the client (the admin list shows metadata only). No new Storage bucket policy is needed if a file replace uses the sanctioned admin ingest path.
- admin_remove_admin must refuse removing yourself or the last admin (clear neutral message) — prevent lockout.
- No new secrets / env vars; no rate-limit / Turnstile / CAPTCHA (deferred to S14); no Edge Functions; no new auth flow or session change; no public-facing endpoint or page change.
- New admin UI strings follow the admin copy canon (admin-content.md) / brand-voice and are owner-approved at the gate (the S10 copy pattern). Decisions D-S11-a…i are owner-approved before the affected code ships.

Verification (must pass before reporting done):
- pnpm run typecheck && pnpm run lint && pnpm run build
- git status — .env.local untracked, no secrets staged, no service_role / sb_secret_* / DB password anywhere in the diff.
- DB role matrix on TEST (supabase-test MCP, via S11_verify_TEST_db_only.sql): anon + pending + approved are denied every admin_* RPC; an admin can list/read/upsert/delete elements + academy + resources; a non-admin cannot add or remove admins; the self / last-admin guards fire; list returns leak no storage path and no PII beyond email.
- Manual, at 320px AND desktop: /admin/content + each section gated (anon → /login, non-admin → 404, Content nav item visible only to an admin, highlighting the Content item without breaking the Approvals highlight); add/edit/delete an element, an academy module, a resource, and an admin each persist and revalidate; an element edit reflects on the gated /elements/[slug] page; the admin tables are readable and don't overflow on a phone.

When the sprint is complete, in the same branch: update docs/PROJECT-STATUS.md (§1, §2, §4 decisions, change log) and tick S11 in docs/ROADMAP.md.

Report at the end: summary · files changed · commands + results (incl. the TEST role-matrix result) · risks/follow-ups · suggested commit message · sprint status. Push policy: commit + push after every gated sub-step (standing authorization, 2026-06-12) so I review in the open PR; never merge; never push beyond the task branch; never write production.
```

## Independent review (required)

This sprint adds **new hardened write RPCs over gated content and the admins table** — exactly the risk class (auth / approval gate / RLS / SECURITY DEFINER) that AGENTS.md makes a blocking review. Run the Codex independent review on the branch diff after the exit gate (11-12) and before merge; treat any "Palestine House gating checks" failure as blocking.

```text
You are my independent code reviewer for the Palestine House website.
Read AGENTS.md in the repo root — it defines your rules, priorities, and the
blocking gating checks. Review the branch DIFF only (vs main), not the whole repo.

This sprint (S11 — Admin content management) adds an /admin/content CRUD panel and
NEW hardened write RPCs (migrations 0023 admin_*_element/_academy_module/_resource,
0024 admin_list_admins / admin_add_admin / admin_remove_admin). Focus your review on:

- Admin gating: every /admin route + every admin Server Action re-checks is_admin()
  server-side (file location is not access control); anon → /login, non-admin → 404.
- RPC hardening: each admin_* RPC is SECURITY DEFINER, search_path='', fully-qualified
  objects, authorization decided INSIDE the function from is_admin()/auth.uid() (never
  from arguments), narrow returns, EXECUTE revoked from public/anon then granted to
  authenticated only; upserts idempotent on natural keys; .down.sql drop-if-exists +
  re-runnable. Could an approved-but-non-admin user call any admin_* RPC directly via
  PostgREST and write or read content / the admin list?
- RLS: content tables (elements/academy_modules/resources) + admins stay default-deny
  with NO client policy; no policy was loosened; the approval gate is_approved() and the
  public/approved read RPCs (get_elements/get_element/get_academy_modules/get_resources/
  public_programming_sessions) are untouched and still correct.
- Data safety: no raw Storage path or PII beyond email leaks to the browser; admin_remove_admin
  cannot remove yourself or the last admin (no lockout); no content loss / id churn on upsert;
  admin_delete_element's cascade to the 1:1 academy_modules row (0015) is intended, not accidental.
- Secrets / boundary / build: no secret committed or behind NEXT_PUBLIC_*; no service_role/
  sb_secret_* in app code; no secret into a Client Component; Server Components default;
  typecheck/lint/build pass.

Report serious issues only: correctness, security/data safety, secret leaks, broken
approval/admin gating, App Router boundary mistakes, Supabase/RLS risks, build breakage.
No style nits; do not critique approved copy or the locked design. Any failure of the
AGENTS.md "Palestine House gating checks" is blocking.

Return: Blocking issues · Non-blocking issues · Missing checks · exact file:line
locations · suggested fix for each · merge recommendation (approve / request changes /
blocking). Do not make changes, push, or merge.
```

## Re-validate before running

- **Dependencies:** S5 (content schema — `elements` / `academy_modules` / `resources` / `admins` + the gated read RPCs) **and** S10 (private-pages clarity) must both be **merged to `main`** before this sprint runs. Confirm both are merged; branch from the latest `main`.
- **Latest migration number:** at the time of writing the latest applied pair is **0022** (programming URL guard), so S11 adds **0023** then **0024**. The codebase will have changed — re-confirm the highest number in `supabase/sql/migrations/` and `supabase/sql/README.md` §6 before naming the new files; never reuse or renumber.
- **Approved admin copy:** the copy canon for this screen is **`docs/page-copy/04-admin/admin-content.md`** (heading "Content admin."; section labels; action labels Add/Edit/Publish/Unpublish/Save; confirmations Saved./Published./Unpublished.) — use it verbatim, never invent. Reconcile its "Live Programming sessions" section and Topics+Academy / Resources+Templates grouping against the S11 scope (programming excluded) by recording a **D-S11** decision in `PROJECT-STATUS.md` §4 before shipping the hub — do not silently diverge.
- **Live file paths:** confirm these still exist and match before coding — `src/app/admin/layout.tsx` (hardcodes `active="approvals"` — make route-aware), `src/app/admin/page.tsx`, `src/components/admin/admin-shell.tsx` (the NAV array + `active` prop), `src/components/admin/approvals-queue.tsx`, `src/lib/admin/actions.ts`, and the gated read RPCs in `supabase/sql/migrations/0011_elements`, `0014_resources`, `0015_academy_modules` (note: `academy_modules.element_id` is a UNIQUE 1:1 FK `ON DELETE CASCADE`), plus the hardened-RPC templates `0009_admin_approval_rpcs` / `0020_programming_publish`. Inspect the **live TEST schema** (column names + natural keys for `elements` / `academy_modules` / `resources` / `admins`) via the `supabase-test` MCP before writing 0023/0024 — never guess names.
- **Decisions to resolve at the gate (record in `PROJECT-STATUS.md` §4):** D-S11-a how `*_md` is edited (plain textarea vs re-ingest) · D-S11-b markdown editing UX · D-S11-c template/video file upload (UI vs re-ingest) · D-S11-d academy scope (youtube_url + body_md only; no cover image) · D-S11-e admin self/last-admin removal guard · D-S11-f content grouping shown in the admin list (incl. the canon's Topics+Academy / Resources+Templates grouping vs the hub split, and the Live-Programming section reconciliation) · D-S11-g resource versioning (overwrite vs new row) · D-S11-h admin-list read RPC exposing emails · D-S11-i stale/missing-content no-op handling. Recommended defaults are in the research; confirm with the owner before the affected sub-step ships.
- **MCP safety:** `supabase-test` is read/write (draft + prove + role-verify here); `supabase-prod-readonly` is **read-only** — the owner hand-applies 0023 then 0024 to production, then Claude verifies PROD read-only. Re-read `docs/SUPABASE-MCP-SAFETY.md` §1/§4/§6 and `docs/WORKFLOW.md` §14 before any DB step.