# dependencies
node_modules/
.pnpm-store/

# next.js
.next/
out/

# production (root-only — must not match the src/app/(workspace)/build route or src/lib/build)
/build/

# vercel
.vercel/

# logs
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# local env files
.env
.env*.local

# typescript
*.tsbuildinfo

# testing / coverage
/coverage

# SYS2: Playwright launch-gate evidence + stored robot sessions — local only,
# never committed (activate-testing: "Never commit test artifacts").
/test-results/
/playwright-report/
/playwright/.auth/

# build tooling
.turbo/

# system files
.DS_Store
Thumbs.db

# local AI reference folders
# These are intentionally kept out of GitHub and pasted locally when needed.
docs/page-copy/
docs/page-designs/
docs/source-assets/

# The owner's mockup copy at docs/ root — kept on disk until the PP series closes (PP7); never commit it
docs/PH - Palestine House Final.html

# PP8: the Playwright MCP writes snapshots, console logs and screenshots into
# the working directory. They are verification scratch, not source — and they
# arrive untracked and un-ignored, so without this line a browser run silently
# offers itself up for commit.
.playwright-mcp/
*-1440.png
*-768.png
*-320.png

# SYS1 (D-SYS-5): the Website-Development-System SOP pack was a reusable master
# copied in for reference, never part of this repo's doc set. Its whole
# development track is now installed and PH-filled at docs/*.md,
# docs/templates/, docs/testing-setup/, docs/error-tracking/ and the five
# skills — so the copy was DELETED from the repo on 2026-08-22 (owner
# confirmed he holds the master elsewhere; it was never tracked, so nothing
# left git history). The rule stays as a guard: if the pack is ever dropped in
# here again it must not be committed, because 57 duplicate governing docs
# would immediately start drifting against the real ones.
docs/Website-Development-System/

# PP6c: 0030's down-migration carries 1.6 MB of guide prose as literal inserts
# (D-PP-k requires the bodies to be restorable). It was withheld from git while
# that prose was treated as approval-gated; the owner DECLASSIFIED it on
# 2026-08-16, so it is now committed — which is also what makes the rollback
# operationally recoverable rather than a mutable local file.
