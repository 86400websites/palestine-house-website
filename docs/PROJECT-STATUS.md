# Palestine House — Project Status

> **The living tracker and source of operation.** Any fresh session — Claude Code, Codex, claude.ai, or a human — reads this file first to know exactly where the build stands and what to do next. It is updated **in the same PR** that completes (or meaningfully advances) a sprint or phase. Plan and exit gates: [`ROADMAP.md`](./ROADMAP.md). Process: [`WORKFLOW.md`](./WORKFLOW.md).

---

## 1. Right now

| | |
|---|---|
| **Current stage** | **🚀 LAUNCHED — Stage 2 MVP complete through S7.** S1–**S7 all merged on `main`** (S7 Final review & launch = **PR #29, 2026-06-19**; S6 = PR #25). Production is **LIVE with S7** on https://palestine-house-website.vercel.app — deploy verified at the public layer (café §4 copy, `og:locale en_US`, favicon/manifest/icon routes, `canonical`=Vercel domain, security headers). Migrations `0001`–`0019` applied + verified on **both test and prod**. The full two-shells-behind-one-gate site is shipped: 12 public · 3 auth · 11 workspace · `/elements/[slug]` ×30 · `/admin/approvals`. **Stage 3 (post-launch): S8 Workspace Visual Polish ✅ (PR #32) · S9 Live Programming ✅ (PR #34) · S10 Private-pages clarity & polish ✅ (PR #37) · S11 Admin content management ✅ (PR #39) · **S12 Email-automation placeholders + settings ✅ (PR #41)** · S13 Full testing + fix-all → 0 bugs · S14 Final hardening + relaunch. (Stage 3 replanned 2026-06-26 per owner direction — D-S10-b.)** |
| **Active sprint** | **S12 — Email-automation placeholders + settings — ✅ MERGED** (PR #41, 2026-06-27, merge `405643b`; owner-gated 12-0…12-10, push-per-step; post-merge `/close` = GO). Mailchimp + Resend wired as honest **no-op placeholders** so the owner goes live by adding keys + verifying the Resend sending domain alone — no real-delivery test this sprint. **Two server-only helpers** (`src/lib/mailchimp/client.ts` `upsertContact` + `src/lib/resend/client.ts` `sendEmail`) instantiate their SDK only when all env vars are present, return `{ configured:false }` and no-op when absent — **never throw, never fake a send**. **Six touchpoints:** booklet capture (`POST /api/mailchimp/booklet-capture`, tags `lead-booklet-a/b`; both `LeadForm` + `FooterLeadForm` wired) · apply tag `applicant` (graceful-degrade) · contact (`POST /api/resend/contact`, **email-only, no table/migration**, reply-to = submitter) · support owner-email (reply-to = auth `user.email`; graceful-degrade) · approve/decline applicant email (via `admin_list_applications()` by id; graceful-degrade; declined → public `/contact`) · declined→contact dashboard path (owner-scoped status read). Public writes are zod-validated and **fail closed in real production** — keyed on `VERCEL_ENV` (`src/lib/env.ts`) so Preview/local honestly no-op; gated writes degrade gracefully (data stored/flipped first). All new copy owner-approved; existing approved copy reused verbatim. Owner scope decisions (all **zero-migration**): apply-tag on success · no newsletter opt-in (locked form, tag both booklets) · contact email-only · approval-email via the existing admin RPC. **No DB migration; `is_approved` + `is_admin()` gates + CSP (`connect-src 'self'`) untouched.** Adversarial **6-lens exit-gate review = PASS** (1 non-blocking Preview-env finding fixed). typecheck/lint/build green (46 routes). **Previous: S11 ✅ (PR #39).** Record: `docs/sprint-prompts/s12-email-automation-placeholders.md`. |
| **Next action** | **S12 ✅ MERGED — PR #41, 2026-06-27** (merge `405643b`); post-merge `/close` = **GO** (merged-`main` build green 46 routes, tree clean, secret-scan clean, no migration; Codex = approve). **Next = S13 (Full testing + fix-all → 0 bugs)** — scope with `/sprint-prompt S13`. **Switch-on the S12 emails when ready (env-vars-only):** add the six `MAILCHIMP_*` / `RESEND_*` server-only vars in Vercel + verify the Resend SPF/DKIM/DMARC domain — step-by-step in `docs/EMAIL-SETUP-CHECKLIST.md` (note: booklet *delivery* also needs a Mailchimp automation keyed on the `lead-booklet-*` tags). **Carryovers (non-blocking):** owner sign-off on the new S12 email + decline copy; admin-panel microcopy (S11); S10 prod smoke (`/program` double-chrome + Sample videos); production **auth click-through** (S7 steps 3–7); confirm temp `SUPABASE_INGEST_*` removed from `.env.local`. |
| **Production URL** | **https://palestine-house-website.vercel.app** — **LIVE with S7 (2026-06-19, PR #29)**; deploy verified at the public layer (café §4 copy, `og:locale en_US`, manifest/icon/apple-icon/sitemap/robots, `canonical`=Vercel domain, CSP/HSTS/X-Frame headers). |
| **Last updated** | 2026-06-30 — **UI-polish sprint (pre-S13) BUILD COMPLETE** on `qa/image-placement-polish` (owner-gated 0a–0e, push-per-step): home-hero art capped to the landing viewport (no bottom crop) + copy nudged right (0a) · booklet lead magnet collapsed to a single **home-hero modal** — footer + the 4 page-bottom forms removed, `FooterLeadForm` deleted (0b/0c) · nav mega-menu **thumbnails removed** + the two link columns centered (0d) · `/apply` given the standard **art-hero**, art moved up, "Who brings what" text left + form right (0e). UI/copy/layout only — **no DB/auth/approval-gate/RLS/CSP/env change**; typecheck/lint/build green; overrides recorded in §4. Pending owner Preview + merge; **then S13**. — *Prior:* 2026-06-27 — **S12 ✅ MERGED — PR #41** (merge `405643b`); post-merge `/close` = **GO**: merged-`main` typecheck/lint/build green (46 routes), working tree clean + up to date with `origin/main`, secret-scan of the S12 code range clean (no provider keys, no `NEXT_PUBLIC_`, `.env.local` untracked), **no `supabase/sql` changes (no migration)**; independent **Codex review = Approve, zero blocking**; the `is_approved` + `is_admin()` gates + CSP (`connect-src 'self'`) untouched; sprint record saved; trackers flipped to merged on `docs/s12-close`. — *Prior:* 2026-06-27 — **S12 BUILD COMPLETE (pre-merge)** — Mailchimp + Resend wired as honest no-op placeholders across six touchpoints (booklet capture, apply tag, contact, support, approve/decline email, declined→contact); public writes zod + fail-closed in real production (keyed on `VERCEL_ENV`); gated writes graceful-degrade; 6-lens exit-gate review = PASS (1 non-blocking Preview-env fix). — *Prior:* 2026-06-27 — **S11 ✅ MERGED — PR #39** (merge `040ad6b`); post-merge `/close` = **GO**: merged-`main` typecheck/lint/build green (44 routes), working tree clean + up to date with `origin/main`, secret-scan of the S11 range clean (the only `service_role`/`sb_secret_*` hits are doc text in the sprint record + Codex prompt, not real secrets); migrations **0023 + 0024 applied + verified on TEST and PRODUCTION** (engine PROD read-only re-verify: 15 RPCs secdef+pinned, anon/public EXECUTE revoked, no storage/secret leak, `admin_remove_admin` lockout-race lock present, content tables + `admins` RLS-on with 0 client policies, public projections untouched, content 30/30/269); the **approval gate (`is_approved`) + the public read projections are untouched**; sprint record saved; trackers flipped to merged on `docs/s11-close`. — *Prior:* 2026-06-26 — **S11 BUILD COMPLETE** (pre-merge) — gated `/admin/content` CRUD + hardened RPCs 0023/0024 (TEST-verified), 4-lens exit-gate review PASS, independent Codex review caught + fixed one blocking `admin_remove_admin` lockout race (SHARE ROW EXCLUSIVE lock). — *Prior:* 2026-06-26 — **S10 ✅ MERGED — PR #37** (merge `922a979`); post-merge `/close` = **GO**: merged-`main` typecheck/lint/build green (39 routes), working tree clean, secret-scan of the S10 range clean, 16 files changed — **none under `supabase/` / `next.config.ts` / `.env` / `middleware`** (UI/copy/curation only); the **approval gate + the no-public-Apply-CTA-in-the-gated-area invariant hold** (the `GATED_PREFIXES` fix for `/program` + `/programming` reinforces the latter); sprint record saved. — *Prior:* **S10 BUILD COMPLETE** on `claude/sprint-s10-private-pages-clarity` (pre-merge). Simplified flat sidebar + a new `/program` page split out of Operate (Programming & Aswātna + Membership & Service moved; all 30 topics still appear once) + the Welcome page rebuilt as four premium nav cards (Plan/Build/Operate/Program + live Build %) + **in-site video players** (youtube-nocookie embed, no CSP change) with clearly-marked Sample placeholders + premium white cards + crystal-clear beginner intro copy on every gated page + reordered Build CTAs (+ Watch-video) + element-page mobile tab-strip fix. Three owner mid-sprint changes folded in (videos play in-site, the `/program` page, the 4-card Welcome). Adversarial 4-lens exit-gate review = **PASS**; low/nit fixes applied (malformed-URL→sample fallback, reduced-motion scroll guard, dead-CSS removal, equal-height cards, mobile focus-ring, AA meta contrast, canon sync); typecheck/lint/build green (39 routes); **UI/copy/curation only — no DB/auth/RLS/approval-gate/CSP/public-page change.** Record to save on merge: `docs/sprint-prompts/s10-private-pages-clarity.md`. — *Prior (2026-06-26):* **Post-launch roadmap replanned to S10–S14** (owner direction, D-S10-b; planning/docs only — no code, no DB, gate untouched): old pending S10 Email / S11 QA / S12 Hardening → **S10 Private-pages clarity & polish · S11 Admin content management · S12 Email-automation placeholders + settings · S13 Full testing + fix-all · S14 Final hardening + relaunch**; old-S10 work remapped (email/approval-email + declined→contact → S12, clearer pending copy → S10, admin-management UI → S11); ROADMAP §A/§B/§C + this tracker §1–§4/§6–§8 updated in lockstep; S10 brief saved at `docs/sprint-prompts/s10-private-pages-clarity.md`. — *Prior (2026-06-25):* **S9 (Live Programming) ✅ MERGED — PR #34** (merge `2ebce03`). **Read path (9a–9d):** `src/lib/live/*` typed data layer + one shared `SessionCard`/`StatusBadge` (DESIGN §6); `/live` on real data with interactive `?mode=` chips; `/live/[id]` youtube-nocookie watch view + CSP `frame-src` (D1, header verified via curl); Experience strip on the same feed. **Write path (9e–9f):** migrations `0020`/`0021`/`0022` (hardened publish RPC + `mode` CHECK + approval-symmetric delete + YouTube URL cap/host guard) **applied + verified test+prod** via MCP; gated `/programming` publish/edit/remove tool (approval-gated, owner-scoped). Every sub-step ran an adversarial multi-agent review (a **blocking** `Intl.DateTimeFormat` watch-view crash caught pre-merge; all write-path gates proven: anon/pending refused, cross-owner blocked, mode CHECK, no `created_by` leak). Holistic 4-lens exit-gate review + independent **Codex review** (no blocking; 2 non-blocking mediums fixed — YouTube-only `frame-src`, RPC URL guard) → fixed the mobile publish-form 2-col collapse, the Watch/Details card affordance, a filter-aware `/live` empty line, card title/summary line-clamp, `?mode=` encode, a stale comment. **D2 → RESOLVED (listing-only, no RSVP).** New UI strings owner-approved. Build green (37 routes; `/live`, `/live/[id]`, `/experience`, `/programming` now `ƒ`). Record: `docs/sprint-prompts/s9-live-programming.md`. |

### How to resume in a fresh AI session
1. Read this file, then the active sprint's scope + exit gate in `ROADMAP.md`.
2. Read `CLAUDE.md` (or `AGENTS.md` if you're a review agent) and the docs it points to for the task at hand.
3. Verify the repo state yourself (`package.json`, `src/app/`, `git log`) — trust the code over this file if they disagree, then fix this file.
4. Work only inside the active sprint. Anything else → propose it under §5 or the backlog.
5. Before ending: update §1–§3 here, tick `ROADMAP.md` checkboxes, and include this file in the PR.

## 2. Sprint board

Status legend: ⬜ not started · 🔵 in progress · ✅ done · ⏸ blocked (say why in Notes)

| Stage / Sprint | Status | Merged PR(s) | Date | Notes |
|---|---|---|---|---|
| 0.0 Setup pre-sprints (0a–0d: repo, scaffold, CI, Vercel preview) | ✅ | #1, #2 | 2026-06 | Scaffold (`80c7fcc`), CI workflow, Vercel Preview verified — pre-completes parts of 1.1/1.2 |
| 0.1 Foundation (tokens, fonts, chrome, headers, 404) | ✅ | #4 | 2026-06-12 | Real tokens, Spectral+Inter, motion primitives, security headers + CSP, locked chrome (mega-menus per mockup), 404/error |
| 0.2 Home + The Model | ✅ | #4 | 2026-06-12 | Built on the Stage 0 branch per the gated sub-step protocol (`docs/sprint-prompts/stage-0-master-prompt.md`) |
| 0.3 Experience · Bring a House · Our Support | ✅ | #4 | 2026-06-12 | All three pages built; Experience live strip in designed empty state until S9 |
| 0.4 Live + Focus Areas + About + Contact | ✅ | #4 | 2026-06-12 | `/live` ships the approved empty states (watch view + filters activate with real data, S9); contact form no-ops honestly until Resend (S10) |
| 0.5 Apply UI + auth UI + legal + SEO | ✅ | #4 | 2026-06-12 | Apply + auth forms no-op honestly until S3; legal pages carry the approved counsel-review note; sitemap/robots/OG/JSON-LD live |
| **Stage 0 exit gate** | ✅ | #4 | 2026-06-12 | Build-side gate + owner Preview sign-off passed; merged to `main` |
| 1.1 GitHub (protection + CI) | ✅ | #5 | 2026-06-12 | gitleaks (+ `pull-requests: read` fix) + Dependabot merged; branch protection rule + secret scanning set by owner; **repo stays public** (owner decision, §4 — gated content leaves the repo by S5) |
| 1.2 Vercel (envs + production deploy) | ✅ | | 2026-06-12 | `NEXT_PUBLIC_SITE_URL` + Supabase vars set (Production only) and redeployed; live site verified |
| 1.3 Supabase projects (prod + non-prod, auth URLs) | ✅ | | 2026-06-12 | Production project `jwogtqizqujwhbvpoziu` configured (Site URL + 3 redirects); **non-prod project at S2 start in a free org** (owner decision, §4) |
| **Stage 1 exit gate — barebones site LIVE** | ✅ | | 2026-06-12 | https://palestine-house-website.vercel.app verified (headers/canonical/sitemap/robots/OG/404/copy); CI + protection live; Preview pipeline working |
| S1 Design consistency pass (1a–1d) | ✅ | #15, #16 | 2026-06-12 | + owner design refresh: white-bg art, dark sections, premium cards, hero balance, nav art swap, 2 copy changes (§4); audits 1a–1d passed |
| S2 DB phase 1 — identity & approval (2a–2b) | ✅ | #17 | 2026-06-16 | 7 migration pairs in `supabase/sql/` (profiles/applications/admins + RLS default-deny + hardened `is_admin`/`is_approved`/`get_my_profile` + `handle_new_user` trigger); consolidated `bundles/S2_apply_all.sql`; applied to non-prod + prod, role-verified (anon locked out, owner-scoped, no self-escalation); fixes 0005/0006 from verification + 0007 (status-forge) from independent review, **all applied + re-verified on both DBs**; SQL organised into `migrations/` · `bundles/` · `verification/` |
| S3 Auth complete (3a–3c, Apply live) | ✅ | #19 | 2026-06-16 | `@supabase/ssr` browser+server clients + `src/middleware.ts` session refresh + allow-list `safe-redirect`; live login/logout (Server Actions) + session-aware header via same-origin `/api/auth/session` (public pages stay static, CSP unchanged); forgot/update password + `/auth/confirm` code→session; Apply live = sign-up (instant session) + owner-scoped `pending` `applications` insert + approved confirmation; **+ owner-approved Password field** on Apply (§4). DB migration **`0008`** maps `full_name` from signup metadata → applied + verified test + prod. Built in owner-gated sub-steps, pushed per step |
| S4 Approval gate + /admin/approvals (4a–4c) | ✅ | #21 | 2026-06-17 | Server-side session gate on the `(workspace)` group + pending/newly-approved `/dashboard`; `/admin/approvals` (`is_admin()` → 404 for non-admins) + Approve/Decline flip via `0009`/`0010` RPCs (no secret-key path); post-login → `/dashboard`; public pages stayed `○ Static`. Owner Preview test ✅ + Codex review (no blocking; 2 non-blocking SQL fixes); migrations `0009`/`0010` applied + verified test + prod. §15 satisfied. Deferred: Resend approval email → S10 (post-launch), notes → backlog |
| S5 DB phase 2 — content schema (5a–5e) | ✅ | #23 | 2026-06-18 | Migrations `0011`–`0015`: `elements` · `checklist_items`/`checklist_progress` · `programming_sessions` (anon-safe `public_programming_sessions()`) · `resources` + private `resources`/public `booklets` Storage buckets · `academy_modules`. RLS default-deny + 7 hardened RPCs (anon-exec **only** the public projection). Applied + role-verified (anon/pending/approved) test→prod; full content ingested into both DBs + Storage via the idempotent `scripts/ingest-content.ts` (30 · 728 · 30 · 267+2 · 269 objects). Codex review → 2 blocking gate gaps fixed by `0016` (checklist_progress read + programming_sessions delete now `is_approved()`-gated; applied test, **prod pending**). No UI (pages = S6). **Merged** (PR #23, 2026-06-18); `0016` still pending prod apply (carryover owner action). |
| S6 Private platform pages (6a–6g) | ✅ | #25 | 2026-06-19 | **Merged.** The full approval-gated workspace on the S5 schema: `/elements/[slug]` ×30 · `/build` · `/dashboard` · `/plan` · `/operate` · `/resources`+`/resources/[category]`+signed-URL downloads · `/academy` · `/tools` · `/account` · `/support`. Migrations `0016`–`0019` applied + verified on **prod** (re-verified S7 Step 1 via read-only MCP). Gates suppressed (D-S6-b). Codex brief: `docs/code-reviews/s6-private-platform-pages.md`. |
| S7 Final review & launch (7a–7d) 🚀 | ✅ 🚀 | #29 | 2026-06-19 | **MERGED (PR #29) + LAUNCHED.** Production live with S7; deploy verified at the public layer (copy/§4, `og:locale`, metadata routes, canonical=Vercel domain, headers); independent **Codex = approve, zero blocking**. 8 owner-gated sub-steps: prereq verify · exhaustive QA (10 defects fixed + adversarially verified) · content verify (copy verbatim, proof numbers + booklets clean) · SEO/perf (OG locale, AVIF) · launch polish (favicon/icons, manifest, Home metadata, loading.tsx) · repo-exposure verified clean · exit-gate full-diff review (security/gate clean; 1 low `/apply` fix). Build green (37 routes). Findings + production smoke-test: `docs/code-reviews/s7-qa-findings.md`. Ships without rate-limit/Turnstile (post-launch S14 hardening, D-S6-a). |
| **S8 Workspace Visual Polish (8a–8k)** *(post-launch)* | ✅ | #32 | 2026-06-24 | Lifted all ~12 gated pages to the public-shell premium bar; grew on owner feedback to add the topic-page redesign + inline template downloads (8i), the workspace logo → public home (8j; a public header was trialled then removed per owner preference), and milestone-gate retirement site-wide (8k — D-S8-c, proof → "120-day launch"). Behaviour untouched; approval gate intact. Decisions D-S8-a/b/c. Reviews: adversarial workflow + independent **Codex = approve** (one finding fixed, `195e4a3`). **Merged (PR #32)**; owner Vercel-Preview eyeball of the gated workspace still pending (non-blocking). Record: `docs/sprint-prompts/s8-workspace-visual-polish.md` |
| S9 Live Programming (9a–9g) *(post-launch)* | ✅ | #34 | 2026-06-25 | **MERGED.** `/live` + Experience on real data via one shared `SessionCard`; `/live/[id]` youtube-nocookie watch view + YouTube-only CSP (D1); gated `/programming` publish/edit/remove (approval-gated, owner-scoped). Migrations `0020`/`0021`/`0022` (publish RPC + `mode` CHECK · approval-symmetric delete · URL cap/host guard) **applied + verified test+prod**. Per-step + holistic 4-lens + independent **Codex** reviews = no blocking; D2 (RSVP→listing-only) **RESOLVED**. Record: `docs/sprint-prompts/s9-live-programming.md` |
| **S10 Private-pages clarity & polish** *(post-launch)* | ✅ | #37 | 2026-06-26 | **MERGED** (merge `922a979`; post-merge `/close` = GO). Simplified flat sidebar · beginner intro copy on every gated page · **4-card Welcome dashboard** (Plan/Build/Operate/Program + live Build %) · Build CTAs reordered + Watch-video · **videos play in-site** (youtube-nocookie, no CSP change) + Sample placeholders + premium white cards · element-page mobile tab-strip fix · **new `/program` page** split from Operate (all 30 topics intact). UI/copy/curation only. 4-lens exit-gate review = PASS; build green (39 routes). Record: `docs/sprint-prompts/s10-private-pages-clarity.md` |
| S11 Admin content management (11-1…11-12) *(post-launch)* | ✅ | #39 | 2026-06-27 | **MERGED** (merge `040ad6b`; post-merge `/close` = GO). Gated `/admin/content` CRUD: hub + Elements/Academy/Resources **edit-only** + Admins add/remove, behind `is_admin()`. Hardened RPCs **0023** (12 content) + **0024** (3 management) **applied + verified on TEST and PROD**; Server Actions re-check `is_admin()`; adversarial 4-lens exit-gate review = **PASS (0 findings)** + independent **Codex review** (1 blocking `admin_remove_admin` lockout race fixed — SHARE ROW EXCLUSIVE lock); content tables + `admins` stay RLS default-deny. The old-S10 admin-management UI folds in here. Record: `docs/sprint-prompts/s11-admin-content-management.md` |
| S12 Email-automation placeholders + settings (12-0…12-10) *(post-launch)* | ✅ | #41 | 2026-06-27 | **MERGED** (merge `405643b`; post-merge `/close` = GO; **Codex review = approve**). Mailchimp + Resend wired as honest **no-op placeholders**: booklet capture (`lead-booklet-a/b`) + apply tag (`applicant`) + contact + support + approval/decline email + declined→contact. Two server-only helpers; both public-write routes zod + fail-closed in **real** production (keyed on `VERCEL_ENV`), honest no-op on Preview/local; gates untouched; **no migration**. Adversarial 6-lens exit-gate review = PASS (1 non-blocking Preview-env fix applied). Owner adds env vars + verifies the Resend domain (§6) to switch on — see `docs/EMAIL-SETUP-CHECKLIST.md`. Record: `docs/sprint-prompts/s12-email-automation-placeholders.md` |
| UI polish — hero · lead-magnet · nav · apply (0a–0f) *(pre-S13)* | 🔵 | | 2026-06-30 | **BUILD COMPLETE (pre-merge)** on `qa/image-placement-polish` (owner-gated 0a–0e, push-per-step). Home-hero art capped to the landing viewport (no bottom crop) + copy nudged right · booklet lead magnet collapsed to a **single home-hero modal** (footer + 4 page-bottom forms removed; `FooterLeadForm` deleted) · nav mega-menu **thumbnails removed** + the two link columns centered · `/apply` given the standard **art-hero**. UI/copy/layout only — **no DB/auth/approval-gate/RLS/CSP/env change**; typecheck/lint/build green. Overrides recorded in §4. Pending owner Preview + merge, then S13. |
| S13 Full testing + fix-all → 0 bugs (13a–13c) *(post-launch)* | ⬜ | | | New (absorbs old S11 QA + S12 fix-all). Multi-hat E2E test (copywriting → UX → feature) covering everything, then **fix everything** → final, 0 errors / 0 bugs |
| S14 Final hardening + relaunch (14a–14d) *(post-launch)* | ⬜ | | | New (absorbs old S12 hardening). Upstash rate-limit + Turnstile + fail-closed verification + **custom domain + SEO** → final relaunch |

## 3. What exists / what's pending

**Done so far**
- Locked sitemap & architecture: `/docs/page-designs/content/PH_Sitemap_Architecture_TECH.txt` (summarized in `TECH-ARCHITECTURE.md` §0).
- Complete development-ready copy set (`/docs/page-copy/` — `00-global`, 12 public pages, 3 auth pages, 12 workspace pages, 3 admin pages, 30 element pages in `06-elements/`). All review flags resolved; the 30-topic reconciliation record is `/docs/page-copy/05-review/structure-reconciliation.md`. (The flat copy under `/docs/page-designs/content/` is the snapshot the mockups read — `/docs/page-copy/` is canonical.)
- **Approved high-fidelity mockups for all 30 pages** (`/docs/page-designs/`): `public/` (12) · `auth/` (3) · `member-workspace/` (12, incl. Search) · `admin/` (3), plus the locked shared chrome (`shared/site-chrome.*`, `workspace-chrome.*`, `admin-chrome.*`, `pages.css`) and browsable hubs (`index.html`, `WorkspaceIndex.html`). See `/docs/page-designs/README.md` for the route map and design rules.
- **Bound design system v2 in-repo**: `/docs/page-designs/design-system/` — `tokens/` (colors, fonts, typography, spacing), `base.css`, `styles.css`, self-hosted Spectral + Inter, component bundle.
- **Final artwork in-repo**: `/docs/page-designs/assets/art/` (mockup-referenced `PH-*.png`) + masters incl. textures and empty-state marks in `/docs/source-assets/images/`.
- Content package (`/docs/source-assets/resources/`): booklets, 10 focus areas, 30 topics, 267 templates, videos (uploaded to Supabase Storage in Sprint 5d/6e).
- Repo scaffolded on the locked stack (Next.js 15, TS strict, pnpm, Tailwind v4, shadcn/ui, Framer Motion) with CI and a verified Vercel Preview pipeline (PRs #1–#2).
- This docs/ops package (10 core MD files), aligned with the repo on 2026-06-11.

**Known design gaps (flagged in `/docs/page-designs/README.md` — never invent these)**
- ~~`/live` **partner publishing tools** UI is not designed~~ — **RESOLVED (S9 9f, 2026-06-25):** built as the gated **`/programming`** tool (publish/edit/remove a YouTube link + metadata; approval-gated, owner-scoped via `publish_programming_session` / `0020`). New UI strings authored to brand voice + **owner-approved**. Deliberately small per D-S9-a (no streaming, no video/cover upload — cover-image upload is a recorded follow-up).
- `/academy/[slug]` dedicated module view not designed — each topic's Video tab covers MVP behavior; design only if embeds need their own page.
- ~~4 topics missing the "Watch Out For" file (H2, H3, I3, J2)~~ — **resolved (S5, 2026-06-18):** all 30 topics actually have a Watch-Out-For source `.docx` on disk (several under inconsistent names — `…_WTWOF.docx`, `…_For_fixed.docx`); the page-copy `⚠️ placeholders` were *unedited copy*, not missing source. The S5 ingestion populated `watch_out_for_md` for **all 30** elements. The approved empty-state line stays as a safety fallback but isn't triggered by the current content.
- ~~Gate 2's label ("Build-out") is unapproved~~ — **moot: the milestone "gates" concept is RETIRED site-wide (D-S8-c, 2026-06-24, superseding the public half of D-S6-b).** The `/build` tracker uses the 10 focus-area grouping; the public pages drop the "gate"/"go-no-go"/"checkpoint" framing (the Day 30/60/108 milestone timeline on `/bring-ph` stays, without gate language); the proof strip's 5th stat is now "120-day launch".

**Pending inputs (owner)**
- Mailchimp audience + Resend sending domain + contact/legal email — owner adds as env vars before **S12** (the email-automation placeholders sprint; see §4 D4). **S10 (active) needs no owner inputs — it's UI/copy/curation only.**
- Decision D2 (in-person RSVP) — **RESOLVED 2026-06-25 (S9, listing-only)**; no open owner inputs remain.

## 4. Locked decisions (do not reopen)

- **UI-polish overrides (owner decision, 2026-06-30 — pre-S13 sprint on `qa/image-placement-polish`):** three deliberate chrome/layout changes the owner explicitly authorized; UI/copy/layout only (no DB/auth/approval-gate/RLS/CSP/env change). (1) **The booklet lead magnet now lives ONLY in the home-hero modal** — the second hero CTA ("Download The House Promise (free)") opens a dialog with the email form. This **supersedes the S1 "every page ends on the booklet block" decision** and removes the lead-magnet block from the footer chrome and from the bottom of `/`, `/bring-ph`, `/experience`, `/our-support`; `FooterLeadForm` is retired. Capture still POSTs to `/api/mailchimp/booklet-capture` with the same `lead-booklet-a` / `lead-booklet-b` tags — **no API/email change** (the home line "Start with a free read **below**" lost the now-stale "below"). (2) **The nav mega-menu artwork thumbnails are removed** from "The Model" + "Experience" and the two link columns are centered — a sanctioned deviation from the locked header-chrome mockup; the **mega-menu link strings are unchanged**. (3) The `/apply` top adopts the standard left-aligned art-hero (copy left, PH-APPLY art right) for consistency with the other public pages; the "Who brings what" context stays left and the form stays right. The home-hero art is capped to the landing viewport (no bottom crop) and the hero copy nudged slightly right. *(Supersedes the relevant clauses of the S1 footer/lead-magnet decisions below and the "three artwork thumbnails" detail in the Public-nav decision.)*
- Public nav: logo → Home + four labels (The Model · Experience · Bring a House · Our Support) + **Sign in** + green **Apply** button; hover one-liners via the Tooltip component. **The Model and Experience additionally open mega-menu panels** (two link columns + three artwork thumbnails) — built per the approved mockup chrome; the mega-menu link strings (Who's who / How it works / The rooms / More to find columns) are approved-via-mockup (owner decision, 2026-06-12). Live Programming is not a top-nav label — it surfaces via the Experience mega-menu, Home, the footer "Explore" column, and the Experience live strip.
- Footer (locked, identical everywhere): Explore (The Model · Experience · Live Programming) · Bring a House (Why bring one · Our support) · Account (Sign in) · Legal (Privacy · Terms · Contact) + Apply block + booklet lead-magnet block + tagline. `/focus-areas` and `/about` are secondary routes linked from page bodies, not chrome.
- Single CTA: "Apply to bring a House" · *Every application is reviewed by HQ.* Questions → Contact. Never "create a free account."
- **Apply = sign-up**, approval-gated: one form → pending account + application → HQ approval (`is_approved`) → unlock. No separate signup. Apply form uses a **single "Your name" field** (per mockup; resolves copy flag 5d — derive first name server-side if needed).
- **Apply Password field (owner decision, 2026-06-16, S3 3c):** the Apply form gained a **required Password field** (min 8 chars) when Apply went live. The locked mockup/copy had no password, but `/login` is password-based — letting the applicant choose their password at apply time is the reliable, standard flow (no dependence on an email link to ever sign back in). Mechanism: `signUp` (email confirmation OFF → instant session) creates the pending account, then the just-authenticated user inserts its own `pending` `applications` row under S2's owner-scoped RLS; `is_approved` stays false. `full_name` rides in signup metadata and the `0008` trigger copies it onto `profiles`. `docs/page-copy/01-public-pages/apply-partner.md` updated to match (page-copy is gitignored — OneDrive is canon).
- **Reference, not a course:** no quizzes, **no certificate**; Academy = optional gated video library; only per-user state = `/build` checklist progress.
- Stages: Plan & Prepare · Design & Build · Operate & Program. "Managing & Operating" (sidebar) and "Operate & Program" (stage) both resolve to `/operate` — intentional, not a duplicate. **Resources sits in the workspace sidebar as a primary destination** (accepted deviation, flagged in the design handoff). **(Sidebar simplified by S10 — D-S10-b, 2026-06-26: the workspace sidebar drops its group headers and flattens to Plan · Build · Operate · Live Programming · Resources › Videos / Tools & Templates, removing the duplicate "Managing & Operating" entry and the coming-soon "House Applications". The stage *content names* are unchanged; only the sidebar labels + structure simplify.)**
- Proof numbers: **10 focus areas · 30 topics · 200+ checklist items · 267 templates · 120-day launch** (267 is the true count; the milestone **3 gates** stat was retired site-wide per D-S8-c and replaced by "120-day launch"; "543"/"250+" retired).
- Live Programming: public listing + partner livestream tools, one `programming_sessions` feed; highlights strip on Experience reuses the same component. **Video host = YouTube** (resolved 2026-06-11, `notes/decisions.md`); CSP extended for the YouTube embed origin only (S9, post-launch). Partner publishing behind approval, as specified in the architecture doc.
- Launch on the **Vercel domain**; custom domain connected later (resolved 2026-06-11). Contact/legal email: none yet — owner adds later as env vars before S12/legal go-live (resolved 2026-06-11).
- `/tools` ships as a coming-soon placeholder. No donations/payments/store, no political/advocacy features, no hype mechanics. English first.
- Booklet lead magnets: *The House Promise* (Booklet A) + *Operating Model & Governance* (Booklet B), public, tags `lead-booklet-a` / `lead-booklet-b`.
- **Supabase environments (owner decision, 2026-06-12):** one project for now = **production** (`jwogtqizqujwhbvpoziu`, project `palestine-house-website`). The separate **non-production** project is created at the start of S2 — in a separate free Supabase org so it costs nothing — before any schema work; Production/Preview/Development must never share one database once schema or users exist (WORKFLOW §14). Until then, Supabase env vars are set in Vercel **Production only**; Preview/Development get the non-prod project's values at S2.
- **Repo visibility (owner decision, 2026-06-12): the GitHub repo stays public** — owner accepts the trade-off knowingly. Consequence: the gated source content currently in the repo (`docs/page-copy/06-elements/`, `docs/source-assets/resources/`) is world-readable until removed. Mitigation: that content moves out of the repo (or the repo flips private) **before launch (the new-S7 pre-launch cleanup)** — tracked in `notes/cleanup-before-launch.md`. Upside: Free-plan branch protection stays enforceable. **(Amended 2026-06-18, decision D-S5-b: deferred from the original "no later than S5" to pre-launch — the owner keeps the source in-repo for reconciliation during the content build; the public-repo exposure stays open and tracked until then.)** **(RESOLVED — S7 Step 7, 2026-06-19: verified there is NO exposure — the gated source trees (`docs/page-copy/`, `docs/page-designs/`, `docs/source-assets/`) are gitignored **and were never committed on any branch** (0 history adds), migrations are schema-only, the ingest script reads only the gitignored sources, and `.env.local` is untracked. No removal needed; the repo is safe to stay public. See `docs/notes/cleanup-before-launch.md`.)**
- **S5 full content ingestion (owner decision D-S5-a, 2026-06-18):** the complete existing content is ingested into the database + Storage **now, in full**, by a one-time idempotent script (`scripts/ingest-content.ts`), so the owner never hand-uploads. Content stays editable later, but the content-editing UI (`/admin/content`) is **V1 / post-MVP** — at MVP, content is re-ingested or edited via the Supabase dashboard. The script is a sanctioned **admin op** (reads the Supabase secret key from `.env.local`, never app code); it defaults to TEST and refuses any other project without an explicit `--i-understand-not-test` override.
- **S5 repo gated-content removal deferred (owner decision D-S5-b, 2026-06-18):** see the amended repo-visibility decision above — removal moves from "no later than S5" to **pre-launch (the new S7)**; the exposure stays tracked in `notes/cleanup-before-launch.md`.
- **S1 design refresh (owner decision, 2026-06-12):** artwork finals have white backgrounds; every section containing artwork renders on **white** so the art dissolves in (incl. `art-hero`, was the paper wash). Non-art paper-washed sections flip to the **dark ink surface** (`--ink-900`, the footer's surface) with white text via `.ph-section-dark`. Closing CTA / lead-magnet sections keep the light wash; conversion surfaces (apply/contact heroes, auth pages) stay light. Cards unify on the premium white `.ph-card` recipe (white, hairline border, soft shadow; `--lift` hover variant). This amends the mockups' section washes — do not "fix" pages back to the mockup colors. Also: `/our-support` closing CTA and booklet lead-magnet sections swapped so every page ends on the booklet block (consistent with Experience/Bring a House).
- **S1 copy adjustments (owner decision, 2026-06-12):** (1) Mobile nav one-liners shortened to: How it works. · Step inside a House. · What it takes. · Behind every House. (desktop hover one-liners unchanged); (2) Home café card line is now "A café where the recipes are here to stay." (was the grandmother's-recipe line). Both propagated to `docs/page-copy/` (navigation-copy.md, home.md) — the copy files remain canon.
- Stack: locked per `TECH-ARCHITECTURE.md`; build process per `WORKFLOW.md`; one sprint at a time.
- Dashboard snapshot leads with "Current stage"; Design & Build % hidden until Build starts. Soft opening Day 108 within the 120-day window (deliberate buffer).
- **D-S8-a — Workspace premium bar (owner decision, 2026-06-23):** the post-launch S8 lifts all gated/workspace pages to the **public-shell premium bar** by applying the site's *already-approved* patterns (scroll reveals via `Reveal/FadeIn/Stagger`, tatreez dividers, warm paper washes, ≤2px card-lift, richer empty/loading states, a stronger dashboard hero) within existing design-system tokens. **No new design language, no new mockups**: the workspace mockups stay the *structural* reference; the public shell (`/live`, `src/components/shared/`, public `globals.css` classes) is the *premium* reference. Behavior untouched. Driven by the first post-launch feedback (workspace functionally perfect but visually plain; a senior-design review found it a notch below the live public pages). Locked design input: `DESIGN.md` (Workspace premium layer); ready-to-run prompt: `docs/sprint-prompts/s8-workspace-visual-polish.md`.
- **D-S9-a — Live = YouTube-link publishing (owner decision, 2026-06-23):** S9 partner publishing is the **simplest powerful** path — partners publish a **YouTube link + event metadata** from the dashboard (owner-scoped writes under RLS) → public `/live` + the Experience strip. **No streaming infrastructure** (no RTMP/transcoding). True "live" later = a YouTube **Live** link (still just an embed). Reuses the planned YouTube-only CSP.
- **D-S10-a — Approval operations simplified (owner decision, 2026-06-23):** alongside the email integrations, S10 makes HQ approvals dead-simple — an **approval/decline email** (Resend; partners are told — deferred from S4), an **admin-management UI** (add/remove/see admins so the owner never hand-edits the `admins` table again), **clearer "under review" copy** (wait expectation + "we'll email you"), and a **declined→contact path**. The approval *gate itself* is unchanged and already simple (apply → HQ clicks Approve in `/admin/approvals` → unlocks live, no re-login). **(Remapped 2026-06-26 per D-S10-b: the email + approval/decline email + declined→contact path → S12; the clearer "under review" copy → S10; the admin-management UI (add/remove admins) → S11. The simplification of HQ approvals stands; only the sprints these pieces ship in changed.)**
- **D-S8-b — Topic/workspace CTA clarity relabels (owner decision, 2026-06-24):** during S8, owner-approved label changes in the gated workspace for clarity (idiot-proof): **"Open this topic" → "Read the full guide"** (`/build`, `/plan`), **"Download templates" → "View templates"** (element action bar), **"Blocked?" → "Add notes"** (build tracker note toggle; the form hint "Blocked? Note why and move on…" and the **Blocked** status label are unchanged), and the element Templates intro reworded to "…download any you need." (templates now download **inline** on the element page via the existing approval-gated signed-URL path). Meaning preserved, behaviour unchanged. Canonical CTA copy synced in lockstep (`00-global/shared-ctas.md`, `03-member-workspace/build.md` + `plan.md` + `elements-canonical-page.md`); the 30 `06-elements/*.md` template-intro lines are reconciled in the S8 documentation pass (the gates-removal sub-step). Supersedes the verbatim wording for these specific UI strings only — do **not** "revert" the UI to the old labels.
- **D-S8-c — Retire the milestone "gates" concept site-wide (owner decision, 2026-06-24):** the "3 gates" proof number and the Day 30/60/108 "go/no-go gates / three checkpoints" journey framing are removed from the public site + docs, to keep it simple. The public proof strip's 5th stat is now **"120-day launch"** (Home + `/focus-areas`). `/bring-ph` keeps the **Day 30/60/108 milestone timeline** (Foundation / Engine working / Ready to host) but drops the "gate"/"checkpoint"/"go-no-go" language (section reframed "The 120-day launch"); the header mega-menu sub-label, Home Design & Build line, and `/our-support` link are updated to match. The proof-number invariant becomes **10 · 30 · 200+ · 267 · 120-day launch** across the governance docs (CLAUDE/AGENTS/README/ROADMAP/WORKFLOW/DESIGN + skills) and canonical page-copy, in lockstep. **Supersedes the public half of D-S6-b** (the gated workspace already never rendered gates). ⚠️ MILESTONE gate ONLY — the **approval/access gate** (`is_approved`, gated routes) and the **sprint "exit gate"** (CI checklist) are unrelated and untouched. The `checklist_items.gate` DB column is left as harmless dead schema (optional later migration); a few internal code comments still reference the old gates (cosmetic, non-user-facing).
- **D-S10-b — Post-launch Stage-3 replan + S10 scope (owner direction, 2026-06-26):** the pending plan is replanned to **S10 Private-pages clarity & polish → S11 Admin content management → S12 Email-automation placeholders + settings → S13 Full testing + fix-all (0 bugs) → S14 Final hardening + relaunch (domain + SEO)**, superseding the old pending S10 (Email & approval ops) / S11 (Journey QA) / S12 (Fix-all + Hardening). **Old→new remap:** the email integrations + the **approval/decline email** + the **declined→contact** path → **S12**; the **clearer "under review" copy** → **S10** (its private-page copy rewrite); the **admin-management UI** (add/remove admins) → **S11** (same admin panel); the journey QA + fix-all → **S13**; the hardening + final launch → **S14**. **S10 is UI / copy / curation only** and is **authorized to revise previously-locked gated-workspace decisions:** the **sidebar menu** (simplified — see the Stages decision in §4), the **stage content-curation** (a careful operate-vs-program re-grouping, **proposal-first**, all 30 topics intact), and the **per-page intro copy** (rewritten clearer per brand-voice, owner-approved each, synced to the `03-member-workspace/*` canon). **No DB / auth / RLS / approval-gate change; no public-page change.** The **approval/access gate** and the **sprint "exit gate"** are untouched. Brief: `docs/sprint-prompts/s10-private-pages-clarity.md`.

- **D-S11-a…i — Admin content management (S11) scope decisions (owner-approved at the planning gate, 2026-06-26):** the `/admin/content` CRUD panel is built behind the existing `is_admin()` gate, with these resolved decisions — **(a)** the topic/academy markdown bodies (`overview_md` / `simple_guide_md` / `watch_out_for_md` / academy `body_md`) are edited as **plain raw-markdown textareas** (text edits need no re-ingest); **(b)** markdown UX = plain textareas, **no live preview** for MVP; **(c)** **no file/video upload in the UI** — template/booklet files change via the existing `scripts/ingest-content.ts` re-ingest path; **(d)** academy scope = `youtube_url` + `body_md` only (no cover image / file upload), the URL validated with the existing `parseYouTubeId` (`src/lib/live/youtube.ts`); **(e)** `admin_remove_admin` **refuses removing yourself or the last admin** (no lockout); **(f)** the hub uses a **four-way split — Elements · Academy · Resources · Admins** (clearer for CRUD than the canon's bundled "Topics & Academy videos" / "Resources & templates"), and the canon's **"Live Programming sessions" section is dropped from `/admin/content`** because programming is already self-managed in the gated `/programming` tool (S9) — `docs/page-copy/04-admin/admin-content.md` updated to record this; **(g)** resource editing **overwrites the row's metadata in place** (no new row, no id churn); **(h)** `admin_list_admins()` exposes **email only** (joined from `auth.users`), no other PII; **(i)** upserts are **idempotent on natural keys** (`elements.slug`, `academy_modules.element_id`, `resources` by id) and a delete of a missing row is a **neutral no-op**. **UI CRUD depth (owner-approved, Q1/Q2 delegated "safest"):** Elements / Academy / Resources are **edit-only in the UI** (no Add/Delete buttons — the 30 topics + 269 files are locked content; an element delete would FK-cascade away its 1:1 academy module); **Admins** gets **Add + Remove**. The full-CRUD write RPCs (0023/0024) still ship at the DB layer for completeness + re-ingest parity. **No change to the auth/approval gates; no new env vars; no rate-limit/Turnstile (S14); the content tables + `admins` stay RLS default-deny with no client policy.**

## 5. Open decisions (resolve here, then propagate)

| # | Decision | Needed by | Options / lean | Status |
|---|---|---|---|---|
| D2 | In-person events: RSVP at MVP? | S9 | listing-only at MVP (no RSVP/ticketing) | **RESOLVED 2026-06-25 (S9)** — listing-only; no RSVP/registration anywhere in the new code |
| D-S6-a | Launch (new S7) **before** Hardening — public writes (Apply, contact, `/support`) go live without Upstash rate-limit/Turnstile | new S7 (launch) | Mitigated by the HQ approval gate (no gated content reachable) + zod + email-uniqueness + the `/support` write being `is_approved()`-gated; full fix in post-launch S14 hardening | **ACCEPTED 2026-06-18** (owner) — see §7 |
| D-S6-b | `/build` gate→item mapping + **Gate 2 label** — which checklist items belong to Gate 1/2/3 | — | The 3 gates **never gated access** (the workspace is open-access by design); an automatic version only re-states the focus-area progress, and a sign-off version adds recurring HQ work to cross a milestone that unlocks nothing. So gates stay **off by design**: `checklist_items.gate` stays null, the `/build` tracker uses the 10 focus-area grouping + saved progress; the milestone gates concept was later **retired site-wide** (D-S8-c, 2026-06-24, superseding this public framing). No mapping or Gate 2 label needed. | **RESOLVED 2026-06-23** (owner) |
| D-S6-c | `/account` **Delete account** section hidden at launch (deviation from the locked Account mockup) | new S7 (launch) → post-launch | Real self-service deletion is a privileged, irreversible auth-user op — safer post-launch; meanwhile deletion requests route through `/support` | **ACCEPTED 2026-06-18** (owner) |

> D1 (video host → YouTube), D3 (domain → Vercel for launch), D4 (contact/legal email → added later as env vars), D5 (Apply name field → single field per mockup), D6 (partner publishing → behind approval as specified) were resolved on 2026-06-11 in `notes/decisions.md` and moved to §4.

## 6. Environment & infrastructure record (names/refs only — never keys)

| Item | Value |
|---|---|
| GitHub repo | `86400websites/palestine-house-website` |
| Vercel project / team slug | `palestine-house-website` / `86400-s-projects` — production: https://palestine-house-website.vercel.app |
| Supabase production ref | `jwogtqizqujwhbvpoziu` |
| Supabase non-production ref | `sdszcralogcrujtyghig` (`palestine-house-test-database`, separate free org; Preview + Development env vars point here) |
| Mailchimp audience | Wired as no-op placeholder (S12, built 2026-06-27) — switch on by adding `MAILCHIMP_API_KEY` + `MAILCHIMP_SERVER_PREFIX` + `MAILCHIMP_AUDIENCE_ID` in Vercel (server-only) |
| Resend sending domain | Wired as no-op placeholder (S12, built 2026-06-27) — switch on by adding `RESEND_API_KEY` + `RESEND_FROM_EMAIL` + `RESEND_TO_EMAIL` and verifying SPF/DKIM/DMARC (steps in `SUPABASE-VERCEL-SETUP.md`) |
| Upstash / Turnstile | TBD (S14, post-launch hardening) |

## 7. Known issues / bugs

_Log every known bug here with severity; the launch sprint (new S7) cannot pass while this section has unresolved bugs — except deferrals the owner has explicitly accepted as post-launch (e.g. #1 → S14 hardening, per §5 D-S6-a)._

| # | Severity | Where | Issue | Status |
|---|---|---|---|---|
| 1 | Medium | Public writes (`/apply`, contact, `/support`) | Apply went live in S3c **without rate-limiting (Upstash) or Turnstile**; S6 adds the `/support` write under the same posture — all sequenced to the post-launch **S14 hardening** per `ROADMAP.md`. **The roadmap launched MVP-first (S7), so the site launched with these unthrottled** (bot-signup / spam-application / spam-support abuse vector). They still zod-validate and fail closed; the **HQ approval gate (`is_approved=false`) blocks any abuse from reaching gated content**, Supabase enforces email uniqueness, and the `/support` write is itself `is_approved()`-gated (approved users only). **Resolved by S14** (Upstash + Turnstile + fail-closed verification); SECURITY-CHECKLIST §15 public-writes invariant is fully satisfied only then. Owner accepted this launch window (§5 D-S6-a). | Open — fix in S14 (post-launch hardening) |

## 8. Change log

| Date | Entry |
|---|---|
| 2026-06-27 | **S12 ✅ MERGED — PR #41** (merge `405643b`) + post-merge `/close` = **GO**. Independent **Codex review = Approve, zero blocking** (confirmed `.env.local` not in diff, provider vars server-only with no `NEXT_PUBLIC_`, SDK imports confined to server-only modules, public routes zod + fail-closed via `VERCEL_ENV`, admin/support gates intact, CSP `connect-src 'self'` unchanged). `/close` sweep on merged `main`: typecheck/lint/build green (46 routes), working tree clean + up to date with `origin/main`, secret-scan of the S12 code range clean, **no `supabase/sql` changes (no migration)**, the `is_approved` + `is_admin()` gates untouched. Added `docs/EMAIL-SETUP-CHECKLIST.md` (owner switch-on guide). Trackers flipped to merged on `docs/s12-close`. **Next: S13 — Full testing + fix-all → 0 bugs.** |
| 2026-06-27 | **S12 (Email-automation placeholders + settings) BUILD COMPLETE** on `claude/sprint-s12-email-automation-placeholders` (owner-gated 12-0…12-10, push-per-step). Mailchimp + Resend wired as honest **no-op placeholders** so the owner goes live by adding keys + verifying the Resend domain alone. **12-0:** deps `resend` + `@mailchimp/mailchimp_marketing`, plus the six server-only env NAMES completed in `.env.example` (no `NEXT_PUBLIC_`). **Helpers:** `src/lib/mailchimp/client.ts` (`upsertContact`, server-only, idempotent member+tags, minimal local `.d.ts` shim instead of a third package) + `src/lib/resend/client.ts` (`sendEmail`, server-only) — both return `{ configured:false }` and no-op when env absent, **never throw, never fake a send**. **Six touchpoints:** booklet capture `POST /api/mailchimp/booklet-capture` (tags `lead-booklet-a/b`; both `LeadForm` + `FooterLeadForm` wired) · apply tag `applicant` (graceful-degrade, before redirect) · contact `POST /api/resend/contact` (email-only, **no table/migration**, reply-to = submitter) · support owner-email (reply-to = auth `user.email`; `profiles` has no email; graceful-degrade) · approve/decline applicant email (fetched via `admin_list_applications()` by id; graceful-degrade; declined → public `/contact`) · declined→contact dashboard path (owner-scoped `applications.status` read). Public writes are zod-validated and **fail closed in real production** — keyed on `VERCEL_ENV` (`src/lib/env.ts`), so Preview/local honestly no-op rather than 503 (exit-gate review fix). Gated writes degrade gracefully (data stored/flipped first, email/tag failure logged + ignored). All new user-facing copy owner-approved; existing approved copy reused verbatim (contact, support). Scope decisions (owner, AskUserQuestion): apply-tag on success · no newsletter opt-in (locked form, tag both booklets) · contact email-only · approval-email via `admin_list_applications()` — all **zero-migration**. **No DB migration; the `is_approved` + `is_admin()` gates + CSP (`connect-src 'self'`) are untouched.** Docs: `SUPABASE-VERCEL-SETUP.md` gained an Email-integrations section (env format hints + Resend SPF/DKIM/DMARC steps); S14 rate-limit/Turnstile deferral commented at each public write. Adversarial **6-lens exit-gate review (workflow) = PASS** (1 non-blocking Preview-env finding fixed). typecheck/lint/build green (46 routes; new routes `ƒ`). No secrets in the diff; `.env.local` untracked. **Pending owner independent Codex review + merge.** Record: `docs/sprint-prompts/s12-email-automation-placeholders.md`. |
| 2026-06-11 | Docs/ops package created: 8 core docs customized to Palestine House + `ROADMAP.md` + this tracker. Stage 0 ready to start. |
| 2026-06 | Pre-sprints 0a–0d: repo created (`86400websites/palestine-house-website`), Next.js 15 scaffold merged, CI workflow added (PR #1), Vercel Preview verified (PR #2). |
| 2026-06-11 | Full docs↔repo alignment pass: corrected all content-layer paths (`page-copy/`, `page-designs/`, `design-system/`, `source-assets/`), recorded the complete mockup + design-system + artwork inventory, propagated resolved decisions D1/D3/D4/D5/D6 from `notes/decisions.md`, recorded real token values in `DESIGN.md`, fixed nav/footer descriptions to match the locked chrome, and updated the sprint board with the true code state. Sprint structure unchanged. |
| 2026-06-12 | Sprint 0.1 built on `claude/sprint-0-1-foundation`: real tokens → `globals.css`, Spectral + Inter via `next/font`, motion primitives (LazyMotion + Reveal/FadeIn/Stagger), security headers + CSP in `next.config.ts`, locked chrome (header with tooltip nav + mega-menus + mobile Sheet, footer with no-op lead form), branded 404/error pages, 6 mega-menu artworks in `/public/assets/art/`. Mega-menu header locked per mockup (owner decision). Self-review (7-angle /code-review) found and fixed: circular Tailwind font vars, Stagger single-child crash, skip-link focus, a11y describedby/aria-hidden, copy-preserving footer status line. typecheck/lint/build green; headers + chrome verified on the production build. |
| 2026-06-12 | **S1 complete** on `claude/sprint-s1-design-consistency` (14 owner-gated sub-steps, push-per-step into the open draft PR): owner's 24 white-background artwork finals compressed in place (54.8 MB → 22.4 MB, visual parity verified); owner design refresh applied site-wide — art sections on white, non-art paper sections to the dark ink surface (`.ph-section-dark`), closing CTA/lead sections kept light, all cards on the premium white `.ph-card` recipe; Home hero balanced into the landing viewport; mega-menu thumbs swapped (Model → HIW-01/02/03, Experience → APPLY-01/LIVE-02/EXP-01); two owner-approved copy changes (short mobile nav one-liners; Home café line → "here to stay") propagated to `docs/page-copy/`; /our-support ends on the booklet block; audits: 1a tokens (no drift), 1b motion/reduced-motion (pass; dormant cards synced), 1c responsive (all grids collapse), 1d AA (contrast computed 4.83–17.4, focus ring on dark fixed to green-100); closing Apply CTA extracted to one shared component (6 call-sites). typecheck/lint/build green throughout. |
| 2026-06-12 | **Stage 0 complete** (sprints 0.2–0.5, 13 gated sub-steps on the same branch, owner-gated per `docs/sprint-prompts/stage-0-master-prompt.md`): all 12 public pages + 3 auth shells built from the approved mockups with copy verified verbatim (200+ strings grep-audited against `page-copy/`); 18 final artworks shipped; honest no-op handling on every form until its real sprint (Apply→S3c, contact→S8b, booklets→S8a, auth→S3); `/live` ships the approved empty states (feed + watch view in S7); SEO layer (per-route canonicals, sitemap, robots, OG image, JSON-LD, auth noindexed). Exit-gate review (second multi-angle pass) fixed: sitemap lastModified churn, error-red styling on no-op statuses, contact live-region initial announcement, Apply confirmation focus drop, framer-motion features now lazy-loaded, shared PageDivider. Owner mobile change: Home hero art stacks first. Owner Preview sign-off + merge pending. |
| 2026-06-15 | **S2 complete** on `claude/sprint-s2-db-identity-approval` (owner-gated sub-steps, push-per-step into the open PR): first DB sprint. `supabase/sql/` runbook + 6 migration pairs — `0001` profiles (`is_approved`, RLS own-row read, `handle_new_user` trigger) · `0002` applications (mirrors the Apply form, owner-scoped insert/read-own) · `0003` admins (RLS default-deny, no client policy) + hardened `is_admin()`/`is_approved()` · `0004` `get_my_profile()` caller-only RPC · `0005` revoke EXECUTE from anon (Supabase default-priv gotcha caught when anon could run `is_admin()`) · `0006` revoke EXECUTE from authenticated on the trigger fn (caught by the prod smoke check). Consolidated `bundles/S2_apply_all.sql` for a clean prod apply + read-only `verification/S2_verify_PROD_safe_readonly.sql`. SQL organised into `migrations/` · `bundles/` · `verification/` with a plain-English README (`supabase/sql/README.md`). Applied to non-prod (`sdszcralogcrujtyghig`) then prod (`jwogtqizqujwhbvpoziu`); role-simulated verification (anon→0/denied, owner-scoped reads, insert-own works + cross-user blocked, helpers correct, self-approve/self-admin blocked, approval flips). typecheck/lint/build green throughout. No app code (Supabase clients/middleware/login = S3; Apply write path = S3c; approval-flip + `/admin/approvals` = S4). |
| 2026-06-15 | **S2 independent code review (Codex) + fixes.** One blocking finding: an applicant could insert their own `applications` row pre-set to `status='approved'` — does not breach `profiles.is_approved` (still locked) but corrupts the pending→admin workflow. Fixed by migration **`0007_applications_insert_pending_only`** (insert policy `with check … and status = 'pending'`); bundle + verification updated. Non-blocking hardening also applied: the test verification was rewritten so every section runs in `begin…rollback` (no persistent writes, safe even if mis-run); the prod smoke switched to `has_function_privilege()` (catches a PUBLIC/anon EXECUTE grant that a `pg_roles` join silently dropped) and now prints full policy definitions; added checks for anon-execute on all three RPCs, authenticated-execute on the trigger fn, and the status-forge rejection. `0007` applied to test + prod and re-verified (prod policy shows the `status = 'pending'` guard in `with_check`; test confirms a forged `status='approved'` insert is rejected). No secrets in the diff; RLS shape confirmed sound by the reviewer. Branch is review-clean, ready to merge. |
| 2026-06-16 | **S3 independent code review (Codex) + fixes** (review brief: `docs/code-reviews/s3-auth-complete.md`). One blocking + two non-blocking findings, all fixed on the branch. **Blocking (High, `safe-redirect.ts`):** `resolveSafeOrigin` trusted the raw `x-forwarded-proto`, so a forged `x-forwarded-proto: https://attacker.example/%2f` + an allow-listed `x-forwarded-host` produced an attacker-origin redirect string; `safeNextPath` also didn't reject backslashes. **Fix:** scheme is now derived from the host type (localhost→http, allow-listed remote→https, `x-forwarded-proto` ignored), origins re-parsed via `URL` + re-validated, all fallbacks validated identically, `safeNextPath` rejects `\`. Verified by forged-header smoke: proto/host spoofs resolve only to allow-listed PH origins (never `attacker.*`). **Non-blocking (Medium): (1)** `/update-password` accepted any session — added a short-lived httpOnly `ph-recovery` marker set by `/auth/confirm` (restricted to the `recovery` OTP type) and required+cleared by `updatePasswordAction`, so a normal session can't reset the password. **(2)** Apply could strand an applicant if the `applications` insert failed after signup — `applyAction` is now idempotent (reuse session / sign-in fallback / one-application check). typecheck/lint/build green; CSP unchanged; no secrets in the diff. |
| 2026-06-17 | **S4 (Approval gate + `/admin/approvals`) BUILT** on `claude/sprint-s4-approval-gate` (owner-gated sub-steps, push-per-step into the open PR). **Sub-step 1 (4a DB):** migration **`0009`** adds two hardened, `is_admin()`-gated `SECURITY DEFINER` RPCs — `admin_list_applications()` (the whole queue, pending-first; zero rows for non-admins) and `admin_set_application_status(id, status)` (sets `applications.status` **and** mirrors `profiles.is_approved`: approved→true, declined→false, atomically). It also renames the decline state `rejected`→**`declined`** to match the approvals copy/design (no row used `rejected`). Applied + verified on **test + prod** (role matrix: admin sees all + flips; non-admin denied; anon can't execute). **Sub-step 2 (4a):** gated `(workspace)` route group with a **server-side session-gate layout** (anon → `/login`; approval via cached `get_my_profile()`); `WorkspaceShell` ported from the locked chrome (pre-approval = locked sidebar; S6 destinations inert, no dead links; search omitted until `/search`); `/dashboard` renders the **pending** "under review" state and the **newly-approved** empty state (verbatim copy). Root chrome now routes via a small `SiteChrome` client component so gated routes get their own shell — **all public pages stayed `○ Static`, CSP untouched**. **Sub-step 3 (4b):** `admin` route group with an **`is_admin()` gate** (anon → `/login`; authenticated non-admin → **404** `notFound()`, never leaks the route); `/admin/approvals` reads `admin_list_applications()` and renders the queue + detail per the mockup (copy verbatim, status = colour+icon+label); **Approve/Decline** via a Server Action → the `0009` flip RPC (re-checks session + `is_admin`, then `revalidatePath`), verbatim toast confirmations. **Sub-step 4 (4c):** post-login default repointed `/`→**`/dashboard`** (safe `?next=` still honored); confirmed the gate reads approval **live** each request (no cached claim) so approval unlocks on the next navigation **without re-login**. **Deferred (owner, 2026-06-17): approval-notification email → S8** (Resend domain unverified until then); **"Add note" → backlog.** typecheck/lint/build green after every sub-step (26/26 routes; `/dashboard` + `/admin*` dynamic, public pages static); §15 self-review clean (server-side gates, admin-checked every request, no secret/`service_role` path, redirects validated, no new public writes); no secrets in the diff. Owner Preview test + merge pending. |
| 2026-06-16 | **S3 complete (Auth complete)** on `claude/sprint-s3-auth-complete` (owner-gated sub-steps, push-per-step into the open PR). **3a-i:** `@supabase/ssr` browser + server clients (`NEXT_PUBLIC_*` only, no secret key), `src/middleware.ts` cookie-backed session **refresh** (no route protection — that's S4), `safe-redirect.ts` (request-origin derivation + allow-list, blocks host-header poisoning + open redirects). **3a-ii:** live login/logout via Server Actions (verbatim non-revealing error copy); the locked header swaps **Sign in ↔ Sign out** via a same-origin `/api/auth/session` probe so **every public page stays statically rendered** and **CSP `connect-src` stays `'self'`** (auth never calls supabase.co from the browser). **3b:** forgot/update password live; `/auth/confirm` exchanges the email code/token for a recovery session then forwards to an allow-listed `next`; reset `redirectTo` is request-origin-derived (Preview emails → Preview); update signs out after so "you can sign in now" is literal. **3c:** Apply live = sign-up — `applyAction` zod-validates, `signUp` (instant session, `full_name` in metadata), then the just-authenticated user inserts its own `pending` `applications` row under S2's owner-scoped RLS; approved confirmation verbatim; fails closed with brand-voice errors. **DB:** migration **`0008`** maps `full_name` from signup metadata onto `profiles` — applied + verified on test + prod (the SQL-Editor ignores hand-written `begin…rollback`, so the test verifier was rewritten to self-clean with explicit deletes). **Decision:** owner-approved **Password field** added to Apply (§4). Auth pages aligned to `noindex,nofollow` (§6). typecheck/lint/build green throughout; CSP untouched; no secrets in the diff. Owner Preview auth testing + merge pending. |
| 2026-06-17 | **S4 Preview fixes + independent code review (Codex).** Owner Preview surfaced two interaction bugs, both fixed: (1) the workspace account menu closed itself on the same click that submitted **Sign out**, unmounting the form before it fired — clicks inside the menu now `stopPropagation`; (2) **Approve/Decline** carried the decision on the submit button's `name`/`value`, which `useActionState` didn't capture as the submitter, so the action hit its validation guard — each decision is now its own form with the decision in a **hidden input**. Also dropped the `(is_approved = true)` column ref from the approved status text. **Codex review** (branch diff vs `main`): **no blocking issues** — `/admin/*` re-checks session + `is_admin()` server-side, non-admins `notFound()`, no service-role key, public routes static / gated dynamic. Two non-blocking (both SQL): **(High)** the `rejected`→`declined` constraint swap (and its rollback) weren't safe if a stray row used the other value → `0009` up/down gained no-op `rejected`↔`declined` guards (no re-apply of `0009` needed); **(Medium)** `admin_set_application_status` didn't verify a `profiles` row was updated → new migration **`0010`** makes it refuse (and roll back) when the applicant has no profile (a state the `0001` trigger already prevents). `0010` to be applied test→prod + read-only verify. typecheck/lint/build green; no secrets in the diff. |
| 2026-06-18 | **S5 (Database phase 2 — content schema) DB + ingestion COMPLETE** on `claude/sprint-s5-content-schema` (owner-gated sub-steps; PR open). Migrations **`0011`–`0015`**: `elements` (5a) · `checklist_items` + owner-scoped `checklist_progress` (5b) · `programming_sessions` + the anon-safe `public_programming_sessions()` projection (5c) · `resources` metadata + the private `resources` / public `booklets` Storage buckets (5d) · `academy_modules` (5e). Every table RLS default-deny, read only via `is_approved()`-gated `SECURITY DEFINER` RPCs — the **only** anon-callable function is the public projection; `checklist_progress` is the sole per-user write (owner-scoped, status-locked). Applied to **test** (via Supabase MCP `execute_sql`) then **prod** (owner, SQL Editor) and verified read-only on both: 6 tables RLS-on, 7 hardened RPCs (`search_path=""`), anon-exec only the public projection, buckets private/public, `storage.objects` default-deny. **Role matrix** (anon / pending / approved, role-simulated, seeded then self-cleaned) passed on test — 30/30 checks. Full content **ingested into both DBs + Storage** by the one-time idempotent `scripts/ingest-content.ts` (parses the `.docx` source + `docs/page-copy/06-elements/_index.md` map; HTML-table checklist parser; upserts on content-stable natural keys; uploads via the Supabase **secret** key — a sanctioned admin op, never app code; defaults to TEST and refuses other projects without `--i-understand-not-test`): **30 elements · 728 checklist_items · 30 academy_modules · 267 templates + 2 booklets · 267+2 storage objects** on each. Decisions **D-S5-a** (full ingestion now; `/admin/content` = V1) and **D-S5-b** (repo gated-content removal deferred to pre-launch) recorded in §4. Finding: all 30 topics have a Watch-Out-For source (the "4 gaps" were page-copy placeholders) — §3 updated. Added script-only devDeps `mammoth` + `tsx`; saved `verification/S5_verify_TEST_db_only.sql` (role matrix) + `verification/S5_verify_PROD_safe_readonly.sql`. **Near-miss caught:** the script's TEST-default guard refused a run when `.env.local` was pointed at prod. No UI (pages = S6). typecheck/lint/build green; `.env.local` gitignored, no secrets in the diff. |
| 2026-06-18 | **S5 independent code review (Codex) + fixes.** Two **blocking** findings — both real gate gaps — fixed by a new migration **`0016_s5_review_hardening`** (`0011`–`0015` are immutable once applied to prod, so a new pair): **(1)** `checklist_progress`'s owner SELECT policy checked only `user_id = auth.uid()`, so a user whose approval was later revoked could still read its saved progress directly via the client — now also requires `public.is_approved()` (the gate must hold on every gated **read**, not just writes). **(2)** `programming_sessions` DELETE was ownership-only while insert/update required approval — a non-approved owner could delete sessions; DELETE now requires `is_approved()` too. Two **non-blocking** hardenings: the ingestion script's TEST guard switched from `host.includes()` to an **exact hostname + https** match (a look-alike host can't pass and receive the secret/writes), and `0016` defensively re-asserts the Storage bucket visibility flags (0014's `on conflict do nothing` wouldn't fix a pre-existing mis-flagged bucket). `0016` applied + verified on **test** (policy quals now include `is_approved()`; role-simulated: a pending user reads **0** progress rows and deletes **0** of its own sessions) — **owner to apply `0016` to prod** then re-run `S5_verify_PROD_safe_readonly.sql`. The `S5_verify_*` scripts gained coverage for both gates. **Acknowledged (not changed):** `0014_resources.down.sql` clears `storage.objects` for the two buckets — inherent to dropping a bucket (FK) and already documented; 0014 is immutable. typecheck/lint/build green; no secrets in the diff. |
| 2026-06-18 | **S6 (Private platform pages) STARTED + roadmap reshuffled (launch-first).** Branch `claude/sprint-s6-private-platform-pages` from `main` (S5 merged, PR #23). **Step 0 (docs only):** renumbered Stage 2 so **launch is the new S7** (was S10) and Live Programming → **S8**, Email automations → **S9**, Hardening (rate-limit/Turnstile) → **S10**, all **post-launch** — the owner launches immediately after S6. Updated `ROADMAP.md` (§A feature refs, §B sprint table reordered + depends-on, §C ordering rationale) and this tracker (§1, §2 board, §3/§4 sprint refs, §5, §7). Recorded decisions **D-S6-a** (launch before hardening — public writes ship without rate-limit/Turnstile, accepted), **D-S6-b** (`/build` gate→item mapping + Gate 2 label = open content needed before launch), **D-S6-c** (`/account` Delete section hidden at launch). S6 then builds one gated sub-step per page on the S5 schema, adding migrations `0017`–`0019` (TEST-first). |
| 2026-06-19 | **S6 (Private platform pages) BUILD COMPLETE** on `claude/sprint-s6-private-platform-pages` (owner-gated Steps 0–3.5, then Steps 4–11 in one owner-authorised autonomous session). **Routes:** `/plan` + `/operate` (read-only foundation/operating views over the 30 elements, grouped per the locked mockups; `/operate` carries per-group routine cadences) · `/resources` hub (featured booklets + focus-area **cards** → `/resources/[category]` slices + client type filter) with **server-issued signed-URL downloads** (templates via a 60s signed URL minted with the user's OWN authenticated client; booklets via public URL; the raw storage path never reaches the client — `get_resources()` omits it, `get_resource_download` resolves it server-side) · `/academy` (video library, one card per topic; all `youtube_url` null today → graceful "video coming" + link to the guide) · `/tools` (dignified coming-soon placeholder) · `/account` (display-name + email opt-in via `set_my_account`; **session-gated**, reachable pre-approval; Delete section **hidden**, D-S6-c) · `/support` (zod form → `submit_support_request`, **approval-gated**, **email deferred** — requests stored not sent; confirmation state). Every sidebar destination now resolves; the element breadcrumb + `/operate` dual sidebar entries (`alias` flag) wired. **DB:** migrations **`0017`** (storage SELECT policy on the private `resources` bucket gated on `is_approved()` + `get_resource_download` path RPC) · **`0018`** (`profiles.marketing_opt_in` + owner-scoped `set_my_account`, cannot set `is_approved`) · **`0019`** (`support_requests` RLS default-deny + approved-only `submit_support_request` + `is_admin()`-gated `admin_list_support_requests`) — all hardened (`SECURITY DEFINER`, `search_path=''`, revoke public/anon, grant authenticated), **applied + role-matrix verified on TEST** (anon/pending read/write nothing; approved scoped to own data; admin-read gated). PROD-safe verification scripts saved for each. **Quality:** every sub-step ran a multi-lens adversarial review workflow (copy · security/RLS · correctness · design), plus a cross-cutting exit-gate review — only minor lows surfaced and were fixed (a duplicate `.topic-code` rule; two README markdown glitches). typecheck/lint/build green throughout (34 routes; gated `ƒ`, public pages stayed `○`, CSP untouched). No secrets in the diff; `.env.local` untracked. Independent **Codex review brief** saved at `docs/code-reviews/s6-private-platform-pages.md`. Safety tag **`s6-step6-safe`** marks the through-Step-6 restore point. **Remaining owner actions before the new-S7 launch:** apply `0016`–`0019` to production + run each read-only verify (engine re-verifies via the prod read-only MCP) · Preview test · run Codex · delete the temp `SUPABASE_INGEST_*` prod lines from `.env.local` · merge. **D-S6-b (gate→item mapping + Gate 2 label) remains open content.** |
| 2026-06-19 | **S7 (Final review & launch) BUILD COMPLETE** on `claude/sprint-s7-final-review-launch` (8 owner-gated sub-steps, push-per-step into the open PR; awaiting merge + prod deploy). Final pre-launch QA + verification — no feature work. **Step 1:** re-verified prod migrations `0016`–`0019` via the read-only MCP (gate policies, hardened RPCs, buckets); `.env.local` gitignored + untracked. **Step 2:** multi-agent exhaustive QA (route × auth-state) → 16 candidates adversarially verified to **11 real defects**. **Step 3:** fixed 10 (the 11th kept as locked copy): double-chrome on every gated route except `/dashboard` (`GATED_PREFIXES` now covers the whole `(workspace)` group + `/admin`) · `/account` "Change password" → working `/forgot-password` flow · `/contact` fallback for pending Support · `/update-password` cookie-gated to stop a silent post-submit bounce · truthful workspace status badge (was a hardcoded stage) · `/apply` duplicate-email **login/enumeration oracle removed** · DB `youtube_url` http(s)-validated before any `<a href>` (latent stored-XSS) · authed users redirected off `/login`+`/apply` · legal `[contact email]` token dropped · build "Blocked?" stale-toggle reset. An 11-agent adversarial pass confirmed all fixes resolved, gate invariants hold, **zero regressions**. **Step 4:** content verified vs locked copy — proof numbers (10·30·200+·267·3) + booklet mapping clean; `/elements` help line bolded per template; the exit gate **caught + reverted a Step-4 café-card misfix that contradicted locked §4** (restored "A café where the recipes are here to stay."; on-disk `home.md` is stale vs §4 — owner to reconcile). **Step 5:** SEO/perf — `openGraph.locale`→`en_US`, enabled AVIF/WebP; sitemap/robots/JSON-LD/OG verified. **Step 6:** launch polish — favicon (`icon.svg`) + apple-icon + web manifest (theme `#1A6B4A`) + `viewport.themeColor` + Home `metadata` + root `loading.tsx` + Organization `logo` JSON-LD, all from the brand arch mark. **Step 7:** verified **no gated-content exposure** (gitignored + never committed; `cleanup-before-launch.md` rewritten) — no removals. **Step 8 exit gate:** full-diff adversarial review (security/gate · correctness · copy · build) — **security/gate/build clean**, 1 low `/apply` stranded-recovery item fixed (redirect now conditional on having an application row). Build green throughout (37 routes); no secrets in the diff. **Remaining owner actions:** merge → prod deploy → run the production smoke-test (`docs/code-reviews/s7-qa-findings.md`) → mark **Launched**. Rate-limit/Turnstile stay post-launch (S10, D-S6-a). |
| 2026-06-19 | **S7 MERGED (PR #29) + site 🚀 LAUNCHED — Stage 2 MVP complete.** S7 merged to `main` (PR #29, merge `6b56d2d`). **Production deploy verified LIVE** at the public layer on https://palestine-house-website.vercel.app: café §4 copy ("…recipes are here to stay"), `og:locale en_US`, favicon/manifest/icon/apple-icon + sitemap/robots routes, `canonical`=the Vercel domain (confirms `NEXT_PUBLIC_SITE_URL`), CSP/HSTS/X-Frame headers. Independent **Codex review = approve, zero blocking** (its one low build-tracker UI-state finding fixed by resetting the "Blocked?" form on any status change); exit-gate adversarial review (security/gate/build clean) + local production smoke-test all green — the smoke-test caught + fixed a Home `og:locale`/`og:type`/`og:site_name` regression (Step-6 metadata override) and an accidental `.pnpm-store` commit. `/close` verdict = GO. **Recommended post-launch follow-ups (non-blocking):** owner auth click-through on prod (apply→pending→approve→approved content + signed-URL download · admin gate); reconcile the gitignored `home.md` café line to §4; confirm temp `SUPABASE_INGEST_*` removed from `.env.local`. **Next: post-launch S8 (Workspace Visual Polish).** Records: `docs/sprint-prompts/s7-final-review-launch.md` + `docs/code-reviews/s7-qa-findings.md` + `s7-final-review-launch.md` (Codex brief). |
| 2026-06-24 | **S8 (Workspace Visual Polish) in progress** on `claude/sprint-s8-workspace-visual-polish` (owner-gated sub-steps 8a–8i, push-per-step). Presentation-only lift of the gated workspace to the public-shell premium bar — shared premium kit (`.ws-pagehead`/`.ws-lift`/warm empty slots, `LazyMotion` confirmed already wrapping) · dashboard hero (stat icons + Design&Build progress bar, gate stat still suppressed) · plan/operate (staggered topic rows + card-lift + aligned routine meta) · build tracker (eased meter, accordion reveal, completed affordance, **item layout fixed** — status pill inline (mobile blank-gutter gone), real action buttons) · **element/topic page redesign** (dark-green hero band + white image slot, tab icons, per-section banners + reading cards, **square-checkbox** checklist, **inline template downloads** via the existing approval-gated path, prominent dark-green Next-topic card) · resources/academy (staggered grids, card-lift, warm no-match) · account/support/tools (card-wrapped support form + confirmation, /tools reveal). All behaviour byte-identical. **Copy relabels ratified — D-S8-b** (canonical CTA copy synced; 30 element-intro lines pending the docs pass). Independent 3-lens adversarial review (behaviour · CSS/a11y · scope/security): behaviour + security **clean**; 2 low CSS items fixed (inert/disabled button contrast → `--stone-500`; `.ws-section-banner-text` min-width). typecheck/lint/build green throughout (37 routes). **Remaining S8:** 8j public nav inside the workspace · 8k remove the milestone "gates" concept site-wide (public copy + proof number + docs; the access/approval gate stays) · exit gate + full journey review. |
| 2026-06-24 | **S8 8j + 8k shipped.** 8j: the locked public **site header now renders above the workspace** (`(workspace)/layout.tsx`) so partners reach the marketing pages from the dashboard — desktop sticks below it via `--ph-header-h`, mobile header scrolls away; no public footer in the gated area; exactly one header (root SiteChrome still returns bare children for gated routes). 8k: the milestone **"gates" concept retired site-wide (D-S8-c)** — public proof stat → "120-day launch" (Home + `/focus-areas`); `/bring-ph` reframed "The 120-day launch" (keeps the Day 30/60/108 milestone timeline, drops gate/checkpoint/go-no-go); header sub-label + Home Design&Build line + `/our-support` updated; proof invariant → **10 · 30 · 200+ · 267 · 120-day launch** across CLAUDE/AGENTS/README/ROADMAP/WORKFLOW/DESIGN + the sprint-prompt/close skills + canonical page-copy. The **approval/access gate + sprint exit gate are untouched**; `checklist_items.gate` left as dead schema. typecheck/lint/build green (37 routes). |
| 2026-06-24 | **S8 review close + 8j refinement (Codex = approve).** Independent **Codex** re-review of the branch = **approve** — no blocking gating/security/App-Router/download/build issues; the earlier workspace-header Apply-CTA-leak fixed in `195e4a3` (`initialAuthed`). Then, **per owner preference, the public header was removed from the workspace** (the 8j trial stacked two mobile menus and felt cluttered): the original workspace chrome is restored (`SiteHeader` reverted to its pre-8j public-only form; the `.ws-frame` wrapper + `--ph-header-h` sticky offsets removed) and instead the workspace sidebar **"Palestine House" logo now links to the public home `/`** as the bridge back to the marketing site. Sprint record re-saved (`/sprint-prompt save`). typecheck/lint/build green (37 routes); `ws-frame`/`initialAuthed` confirmed fully removed. Pending owner Preview + merge. |
| 2026-06-24 | **S8 ✅ MERGED — PR #32** (merge `50f3557`). Workspace Visual Polish (8a–8k + the owner-driven topic-page redesign, the workspace logo→home bridge, and the milestone-gate retirement) merged to `main`. `/close` post-merge sweep = **GO**: merged-`main` typecheck/lint/build green (37 routes), working tree clean, secret-scan of the S8 range clean, no `supabase/sql` changes (no migration), **approval/access gate untouched**, sprint record saved, trackers reconciled. Independent **Codex = approve**. Open (non-blocking): owner Vercel-Preview eyeball of the gated workspace. **Next: S9 — Live Programming.** |
| 2026-06-23 | **Post-launch roadmap (Stage 3) planned + docs finalized (planning session, no code).** Acting on the first post-launch feedback (gated workspace pages functionally perfect but visually plain), defined **Stage 3: S8 Workspace Visual Polish → S9 Live Programming → S10 Email & approval operations → S11 Full journey simulation & QA → S12 Fix-all + final hardening + final launch.** Reshuffled the old post-launch order (Live/Email/Hardening as S8–S10): Live→S9, Email→S10 (now also carries approval-ops), Hardening folds into **S12**; added S8 (polish) and S11 (QA). Three decisions recorded in §4: **D-S8-a** (S8 lifts the workspace to the **public-shell premium bar** using already-approved patterns + tokens — no new design language/mockups, behavior untouched; a senior-design review found the workspace a notch below the live public pages), **D-S9-a** (Live = YouTube-link publishing, no streaming infra), **D-S10-a** (approval-ops: approval/decline email + admin-management UI + clearer pending copy + declined→contact). **Resolved D-S6-b** (the 3 `/build` gates stay **off by design** — they never gated access; a sign-off version adds recurring HQ work for no benefit; the focus-area grouping is the canonical view, the 3-gate journey lives in the public narrative). D2 (RSVP) reassigned to S9. Swept all old-S8/S9/S10 inline tags across `ROADMAP.md` + this tracker to the new numbering; added `DESIGN.md` "Workspace premium layer" and the ready-to-run `docs/sprint-prompts/s8-workspace-visual-polish.md`. Owner runs S8 next session per his gated "proceed"-per-step workflow. |
| 2026-06-25 | **S9 (Live Programming) BUILD COMPLETE** on `claude/sprint-s9-live-programming` (owner-gated 9a–9g, push-per-step). **Read path (9a–9d):** `src/lib/live/*` typed data layer (`getLiveSessions` over the anon `public_programming_sessions()` RPC, fail-closed; pure group/filter/YouTube-embed/UTC-format helpers) + one shared `SessionCard`/`StatusBadge` (DESIGN §6) → `/live` on real data with interactive `?mode=` chips (9b); `/live/[id]` watch view embedding the video via **youtube-nocookie** rebuilt from a validated 11-char id (9c; CSP gained `frame-src 'self' https://www.youtube-nocookie.com` — D1; `X-Frame-Options: DENY` / `frame-ancestors 'none'` intact, header verified via curl); Experience strip on the same feed/component (9d). Approved empty states byte-identical; a zero-row feed renders exactly the Stage-0 page; `/live`+`/experience` now dynamic. **Write path:** migration **`0020`** (hardened `publish_programming_session` RPC — SECURITY DEFINER, `search_path=''`, `is_approved()`+`auth.uid()` gate, `created_by` forced, `left()`-capped, revoke public/anon→grant authenticated; `mode` CHECK = the 4 threads, drop-guarded; in-function `invalid mode` raise) + **`0021`** (delete-verb RLS now requires `is_approved()` — write-verb symmetry) — **applied + role-verified on TEST then PROD** via MCP (anon revoked; no-JWT + pending refused; approved publishes; cross-owner edit/delete blocked; bad mode rejected; anon projection leaks no `created_by`). Gated **`/programming`** tool (9f): approval-gated publish/edit/remove of a YouTube link + metadata; reads own rows under `select_own` RLS; server actions zod+YouTube-validate, neutral errors, `revalidatePath`; sidebar "Live Programming" repointed from public `/live` → `/programming` (Locked for pending). **Quality:** every sub-step ran an adversarial multi-agent review (caught a **blocking** `Intl.DateTimeFormat` that would have crashed the watch view; hardened RPC re-runnability + mode error; a11y labels on row actions). Holistic 4-lens exit-gate review = clean (no blocking/high) → fixed the mobile publish-form 2-col collapse, the Watch/Details card affordance, a filter-aware `/live` empty line, card title/summary line-clamp, `?mode=` encode, a stale comment. **D2 → RESOLVED (listing-only, no RSVP).** New UI strings (watch/404/publish form) **owner-approved**. Vercel Preview uses the **test** DB, seeded with 3 demo sessions (Rickroll swapped for a neutral clip). Build green throughout (37 routes; `/live`, `/live/[id]`, `/experience`, `/programming` now `ƒ`). **Follow-ups (non-blocking):** auto-flip stale `live`→`past`; per-event local timezone; partner cover-image upload. Record: `docs/sprint-prompts/s9-live-programming.md`. **Pending: owner Preview click-through + merge; next = S10.** |
| 2026-06-25 | **S9 independent code review (Codex) + fixes.** **No blocking issues.** Two non-blocking mediums, both closed at the authoritative boundary: **(1)** CSP `frame-src` dropped `'self'` → **youtube-nocookie only** (the watch embed is the app's only iframe; `frame-ancestors 'none'`/`X-Frame-Options: DENY` intact, re-verified via curl). **(2)** migration **`0022`** re-creates `publish_programming_session` to cap the YouTube URL at 500 chars and **reject a non-YouTube host in-function** — so an approved partner calling the RPC directly via PostgREST can't bypass the Server Action's link validation and store junk in `stream_url`/`recording_url` (the render path was already safe — non-YouTube links degrade gracefully). Verified on test (vimeo → `invalid video link`; youtube link publishes; EXECUTE anon=false/authenticated=true). Build green (37 routes). **`0022` pending prod apply** (owner). |
| 2026-06-25 | **S9 ✅ MERGED — PR #34** (merge `2ebce03`). The full Live Programming feature is on `main`: `/live` + the Experience strip on real `programming_sessions` data via one shared `SessionCard`, the `/live/[id]` youtube-nocookie watch view + YouTube-only CSP `frame-src` (D1), and the gated `/programming` publish/edit/remove tool (approval-gated, owner-scoped). Migrations `0020`/`0021`/`0022` **all applied + verified on test AND prod** (owner ran prod by hand; engine verified read-only — publish RPC SECURITY DEFINER + `search_path=''` + `is_approved()`/`auth.uid()` gate + URL cap/host guard; `mode` CHECK; delete-verb `is_approved()` symmetry; anon EXECUTE revoked). Per-step adversarial reviews + holistic 4-lens exit-gate review + independent **Codex review** (no blocking; 2 non-blocking mediums fixed) all clear. D2 RESOLVED (listing-only). Build green (37 routes). Saved via `/sprint-prompt save` (docs branch `docs/s9-close`). **Next: S10 — Email & approval operations.** |
| 2026-06-26 | **Post-launch roadmap replanned to Stage-3 v3 — owner direction (planning/docs only; no code, no DB, gate untouched).** Replaced the pending **S10 Email & approval ops / S11 Journey QA / S12 Fix-all + Hardening** with **S10 Private-pages clarity & polish · S11 Admin content management · S12 Email-automation placeholders + settings · S13 Full testing + fix-all (0 bugs) · S14 Final hardening + relaunch (domain + SEO)**. **S10** (active) = simplify the workspace menu (flatten the group headers; drop the duplicate Program + coming-soon House Applications; Library→Resources › Videos / Tools & Templates) · crystal-clear beginner intro copy on every gated page · Build-CTA reorder (Read the full guide · Watch video · Mark in progress · Add notes · Mark Complete) + Watch-video action · code-level sample videos + premium white cards · mobile fixes (esp. the element/topic page) · proposal-first operate-vs-program content re-grouping (all 30 topics intact) — **UI / copy / curation only**. **Old→new remap:** email + approval/decline email + declined→contact → **S12**; clearer pending copy → **S10**; admin-management UI (add/remove admins) → **S11**; journey QA + fix-all → **S13**; hardening + final launch → **S14**. Recorded **D-S10-b** (the replan + S10 scope + the authorization to revise the previously-locked workspace menu / stage-curation / intro-copy); annotated **D-S10-a** (remapped) and the **Stages** decision (sidebar simplified). Updated `ROADMAP.md` §A/§B/§C + this tracker §1/§2/§3/§4/§6/§7 in lockstep; saved the ready-to-run S10 brief at `docs/sprint-prompts/s10-private-pages-clarity.md`. The **approval/access gate + sprint exit gate are untouched.** Branch `docs/roadmap-s10-s14-replan`. **Next: run S10.** |
| 2026-06-26 | **S10 (Private-pages clarity & polish) BUILD COMPLETE** on `claude/sprint-s10-private-pages-clarity` (owner-gated sub-steps, push-per-step; pending owner Vercel-Preview + merge). **Original scope (10-1…10-6):** simplified the gated sidebar to a flat menu (Welcome · Plan · Build · Operate · Program · Live Programming · Resources › Videos / Tools & Templates; dropped the duplicate Operate alias + the coming-soon House Applications); crystal-clear beginner intro copy on every gated page (calmer Welcome, Operate/Videos/Tools & Templates relabels); Build-item CTAs reordered to Read the full guide · Watch video · Mark in progress · Add notes · Mark Complete (+ a `#video` deep link); code-level **Sample** video placeholders + premium **white** video cards; element-page mobile tab strip → horizontal-scroll. **Three owner mid-sprint changes folded in:** (1) **videos play in-site** — the element Video tab embeds youtube-nocookie and the Videos cards swap the thumbnail for an autoplay embed on click (reuses the existing CSP frame-src — no CSP change; the outbound "Watch" button removed); (2) Welcome **rebuilt as four premium nav cards** (Plan/Build/Operate/Program, icon + one-liner + link, with the live Design & Build % on the Build card; pending users keep the calm "under review" state); (3) **new `/program` page** added after Operate — Programming & Aswātna (D1/D2/D3) + Membership & Service (A1/A2/E1/E2/E3) moved out of Operate (Operate keeps the five operations groups; all 30 topics still appear once across /program + /operate; supersedes the 10-2 keep-all-in-Operate decision). **Adversarial 4-lens exit-gate review (workflow) = PASS, no blocking/high/medium-correctness:** applied the fixes — malformed real `youtube_url` now falls back to the Sample (was a latent dead card), `prefers-reduced-motion` guard on the tab scroll-into-view, removed the dead snapshot CSS (`.dash-grid/.dash-stats/.dash-next/.ws-stat/.dash-progress`), `.dash-cards` added to the S8 equal-height rule, mobile tab focus-ring → inset outline (un-clipped), `.dash-card-meta` → `--muted-foreground` (AA), and synced the page-copy canon (dashboard/operate/academy/elements-canonical + new program.md). **UI / copy / curation only — no DB, migration, RLS, auth, approval-gate, CSP, or public-page change; the approval gate + locked-for-pending behaviour are unchanged.** typecheck/lint/build green (39 routes; `/program` added). `/tools` route kept as an unlinked coming-soon placeholder (owner dropped it from the nav). Record: `docs/sprint-prompts/s10-private-pages-clarity.md`. |
| 2026-06-26 | **S10 ✅ MERGED — PR #37** (merge `922a979`) + post-merge `/close` = **GO**. **Post-Preview fix before merge:** owner Vercel-Preview caught the public header rendering over the workspace shell on `/program` + `/programming` (double chrome) — `GATED_PREFIXES` (`src/components/layout/site-chrome.tsx`) was missing both (`/program` new in S10; `/programming` a latent gap since S9). Fixed by adding both explicitly (the exact-or-`prefix/` match means `/program` doesn't cover `/programming`); build green. **`/close` sweep on merged `main`:** typecheck/lint/build green (39 routes); working tree clean + pushed; secret-scan of the S10 range clean; 16 files changed, **none under `supabase/` / `next.config.ts` / `.env` / `middleware`** (UI/copy/curation only — no DB/RLS/auth/CSP/public-page change); the **approval gate + the no-public-Apply-CTA-in-the-gated-area invariant hold** (the chrome fix reinforces the latter); sprint record saved (`/sprint-prompt save`). Trackers flipped to merged on docs branch `docs/s10-close`. **Owner prod smoke-check (non-blocking):** confirm the chrome fix + the Sample videos on the live deploy; verify/swap the placeholder Sample YouTube IDs. **Next: S11 — Admin content management.** |
| 2026-06-26 | **S11 (Admin content management) BUILD COMPLETE** on `claude/sprint-s11-admin-content-management` (owner-gated sub-steps 11-1…11-12, push-per-step; pending owner PROD apply + merge). A gated **/admin/content** CRUD panel behind the existing `is_admin()` gate: **11-1** route-aware admin nav (`AdminNav` via `usePathname`) + Content item; **11-2** hub ("Content admin." + four sections; Live-Programming dropped per **D-S11-f**, recorded in the `admin-content.md` canon); **11-3** migration **0023** = 12 content RPCs (elements/academy/resources list·get·upsert·delete — `SECURITY DEFINER`, `search_path=''`, in-function `is_admin()`, narrow returns with no storage paths, idempotent upserts on natural keys, YouTube host-guard); **11-4** migration **0024** = 3 management RPCs (`admin_list_admins` email-only · `admin_add_admin_by_email` · `admin_remove_admin` with self/last-admin lockout guards); **11-5** Server Actions (`src/lib/admin/content-actions.ts` — re-check `is_admin()` server-side, neutral errors, `parseYouTubeId`); **11-6/7/8/9** Elements/Academy/Resources **edit-only** UIs (server-driven `?slug`/`?element`/`?id` selection, lossless upsert via round-tripped hidden fields, storage path never in the browser) + Admins add/remove UI (old-S10 admin-management folds in); **11-10** §15 self-audit (all 15 RPCs secdef + pinned + anon-revoked); **11-11** TEST role-matrix via MCP (anon revoked / authenticated granted; admin 30/30/269/1, non-admin 0; non-admin writes 42501; idempotent; lockout guards; no storage-path/PII leak) + `S11_verify_TEST/PROD` scripts archived; **11-12** adversarial **4-lens exit-gate review (workflow) = PASS, zero findings** (gating · RPC/RLS/data-safety · correctness · scope/secrets). Decisions **D-S11-a…i** recorded (§4). **No auth/approval-gate/RLS change** (content tables + `admins` stay default-deny, no client policy); **no public-page / CSP / env / rate-limit change.** typecheck/lint/build green (44 routes; all `/admin/content/*` gated `ƒ`). Migrations 0023→0024 **applied + role-verified on TEST**; **owner hand-applies to PROD** then engine re-verifies read-only. Record to save on merge: `docs/sprint-prompts/s11-admin-content-management.md`. |
| 2026-06-27 | **S11 independent (Codex) review + fix.** One **blocking** finding: `admin_remove_admin` checked `count(*) <= 1` without locking `public.admins`, so two admins removing **each other** concurrently could both pass the last-admin guard and both delete → an empty admins table (lockout, a TOCTOU race — my serial-only reasoning had missed the concurrent case). **Fixed** by serializing removals: `lock table public.admins in share row exclusive mode` before the count read (the lock conflicts with itself, so a second concurrent removal waits then re-reads an accurate count; plain `is_admin`/`is_approved` reads take ACCESS SHARE and are not blocked). 0024 was **TEST-only** (not yet on PROD), so the fix is a `create or replace` in the same `0024_admin_management_rpcs.up.sql` — **re-applied + re-verified on TEST** (lock present; self-removal still blocked; positive add+remove still works). Verify scripts gained a structural lock assertion (TEST §12b + PROD-safe §7). Reviewer otherwise: admin/RPC gating clean, no secret/RLS/boundary issues; typecheck/lint/build pass. ⚠️ **If 0024 was already applied to PROD, this needs a new 0025 instead — confirm before PROD apply.** |
| 2026-06-27 | **S11 ✅ MERGED — PR #39** (merge `040ad6b`) + post-merge `/close` = **GO**. Owner hand-applied **0023 then 0024 (the fixed version) to PRODUCTION**; engine re-verified PROD read-only via `supabase-prod-readonly`: 15 admin RPCs present, all SECURITY DEFINER + pinned `search_path`, anon/public EXECUTE revoked + authenticated granted, **no storage/secret leak**, `admin_remove_admin` carries the lockout-race `SHARE ROW EXCLUSIVE` lock, `elements`/`academy_modules`/`resources`/`admins` RLS-on with 0 client policies, the six public/approved projections untouched, content intact (30 elements · 30 academy · 269 resources [2 public] · 1 admin) — so **migrations 0023+0024 are now applied + verified on TEST and PROD** (no 0025 needed). **`/close` sweep on merged `main`:** typecheck/lint/build green (44 routes), working tree clean + pushed, secret-scan of the S11 range clean (only doc-text matches), the **approval gate + public projections untouched**, sprint record saved. Trackers flipped to merged on `docs/s11-close`. **Owner smoke-check (non-blocking):** sign in as an admin on production and confirm `/admin/content` edits persist; sign off on the new admin microcopy. **Next: S12 — Email-automation placeholders + settings.** |
