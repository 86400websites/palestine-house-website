/* Interim home — replaced with the full approved Home page in Sprint 0.2.
   Exists so the locked chrome has a page to wrap during Sprint 0.1. */
export default function HomePage() {
  return (
    <section className="ph-section-lg">
      <div className="ph-container ph-center-stack">
        <p className="ph-eyebrow">Palestine House</p>
        <h1>A fixed address for Palestinian culture, in every city.</h1>
        <p className="ph-lead">
          The full Home page arrives in Sprint 0.2 — this screen verifies the
          foundation: tokens, type, and the locked chrome above and below.
        </p>
      </div>
    </section>
  );
}
