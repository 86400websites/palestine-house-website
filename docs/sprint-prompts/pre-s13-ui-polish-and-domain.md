# Pre-S13 — UI polish + custom-domain cutover

| | |
|---|---|
| **Date** | 2026-06-30 — **BUILD COMPLETE, Codex = APPROVE (zero blocking)**; pending owner Preview + merge + domain switch-on |
| **Branch / PR** | `qa/image-placement-polish` / PR pending (8 commits ahead of `main`; mixed authorship — 3 prior artwork commits + 0a–0g) |
| **Decision** | Owner-directed interstitial polish sprint, run **before S13**; pulls the custom-domain cutover forward from S14. Overrides recorded in `PROJECT-STATUS.md §4` (2026-06-30) |
| **Goal** | Four presentation fixes the owner saw on the live site + move the site onto `palestine-house.com` — UI / copy / layout / config only; no DB, approval-gate, RLS, CSP, or new dependency |

> Sprint record. This sprint was **not** a numbered roadmap row — it's a pre-S13 polish pass the owner added, built on a branch he'd already started (a related artwork PNG→JPG + hero-sizing pass, commits `c3df8c9`/`2cc11ba`/`a768361`). Built in owner-gated sub-steps 0a–0g, push-per-step. Independent **Codex review = APPROVE, zero blocking / zero non-blocking**. Overrides + the domain decision: `PROJECT-STATUS.md §4`.

## What shipped (7 gated sub-steps)

1. **(0a) Home hero fit + text position** — `src/styles/pages.css`: capped `.home-hero-art .ph-art` `max-height` to the landing viewport (`100svh − 72px − padding`) so the tall 1394:1128 art fits with **no bottom crop** (`object-fit: contain` letterboxes invisibly against the dissolved edges); nudged the copy right (`padding-inline-start` on `.home-hero-copy`, desktop) and gently rebalanced `.home-hero-grid` (`0.72/1.48fr → 0.78/1.42fr`, gap `space-10 → space-12`). Mobile stack untouched.
2. **(0b) Lead-magnet modal** — new client component `src/components/sections/lead-magnet-dialog.tsx` (Radix `Dialog`, same import as `ui/sheet.tsx` — **no new dependency**) wrapping the "Download The House Promise (free)" trigger; opens a centered `.ph-card` modal with the reused `<LeadForm single idPrefix="home-lead" />`. `src/app/page.tsx`: hero 2nd CTA → `<LeadMagnetDialog />`; removed the inline `#booklet` section; dropped the now-unused `LeadForm`/`Download` imports. Home stays a Server Component; the modal is the only new client boundary.
3. **(0c) Single lead-magnet entry point** — removed the booklet block from the **footer** (`site-footer.tsx`) and the bottom lead-magnet section from `/bring-ph`, `/experience`, `/our-support` (each now ends on its Apply closing CTA); **deleted** `footer-lead-form.tsx`; removed the dead CSS (`.phx-footer-lead*`, `.phx-footer-micro`, `.leadmagnet*`). Applied the one approved copy fix (home: dropped the now-stale "below"). Booklet capture (`/api/mailchimp/booklet-capture`, tags `lead-booklet-a/b`) unchanged. Overrides recorded in §4.
4. **(0d) Nav mega-menus** — `site-header.tsx`: removed the `.phx-mega-thumbs` render block + both `thumbs` data arrays + the `thumbs` type field + the now-unused `Artwork`/`ArtworkAssetId` import. `globals.css`: `.phx-mega-inner` → `grid-template-columns: auto auto` + `justify-content: center` (two centered link columns); removed the dead `.phx-mega-thumbs` rule. **Link strings unchanged** (approved-via-mockup).
5. **(0e) /apply art-hero** — `src/app/apply/page.tsx`: replaced the centered `.page-hero.is-center` band with the standard left-aligned `.art-hero` (copy left; PH-APPLY-01 art moved up from the body into the hero); body keeps the "Who brings what" context **left** and the `ApplyForm` (incl. the required Password field) **right**. Copy verbatim; server-side signed-in→dashboard redirect intact. Removed the dead `.apply-context-art` CSS.
6. **(0f) Exit gate** — full-branch diff review + loose-end sweep (no `#booklet` / `FooterLeadForm` / `home-leadmagnet` remain); `typecheck`/`lint`/`build` green (46 routes; public pages static, gated dynamic); tree clean + pushed; `.env.local` untracked; trackers updated (§1 change log + §2 board row + §4 overrides).
7. **(0g) Custom-domain cutover (code side)** — `src/lib/safe-redirect.ts`: added `www.palestine-house.com` + `palestine-house.com` to `PRODUCTION_HOSTS` (the comment had pre-flagged this exact step for "when the custom domain lands, D3"). **Canonical = `https://www.palestine-house.com`** (owner decision; apex 308→www per the Vercel config). Everything else (canonical/OG/sitemap/robots/approval emails) is env-driven via `NEXT_PUBLIC_SITE_URL` (`src/lib/site.ts`) and CSP/headers are origin-relative — so **no other code change**. Switch-on is owner dashboard config (recorded in §4).

**Owner decisions (2026-06-30):** lead-magnet CTA → modal popup · /apply → art-hero + form below · mega-menu → center the columns · canonical domain → `www.palestine-house.com`.

## Prompt used

<details><summary>The gated implementation prompt (drove 0a–0f; 0g — the domain cutover — was added mid-sprint by owner request)</summary>

```text
You are my senior engineer for the Palestine House website, working in Claude Code.

Session start: read docs/PROJECT-STATUS.md §1–§2 and the locked decisions in §4. CLAUDE.md governs everything below. This is a UI/copy/layout polish sprint that runs BEFORE the S13 testing sprint — NO database/migration, NO auth/approval-gate/RLS change, NO CSP/header/env change, NO new dependency. Two changes deliberately override locked decisions (I authorized them on 2026-06-30) — record those overrides in PROJECT-STATUS §4 as you make them.

Sprint: UI Polish — hero, lead-magnet, nav, apply (pre-S13)
Branch: qa/image-placement-polish (already checked out, 3 commits ahead of main with the related artwork pass — continue on it; do NOT branch fresh)

Goal:
Fix four presentation issues on the live site: (1) the home hero art is slightly too tall and its bottom is cut off — shrink it to fit fully, and nudge the left text block rightward (it's too far left); (2) collapse the booklet lead-magnet to a SINGLE entry point — the home hero's 2nd CTA opens a modal with the email form — and remove the form from the footer and all 4 page-bottom sections; (3) remove the artwork thumbnails from the "The Model" + "Experience" nav mega-menus and center the two link columns; (4) make /apply consistent with other pages by giving it a left-aligned art-hero on top (copy left, PH-APPLY-01 art right), keeping "Who brings what" + context text on the left and the form on the right below.

Execute in gated sub-steps (one owner gate after each):
1. (0a) Home hero sizing & text position — in src/styles/pages.css: cap .home-hero-art height so the 1394:1128 art fits fully inside .home-hero (min-height: calc(100vh - 72px)) with no bottom crop, and rebalance .home-hero-grid (widen the left column from 0.72fr toward ~0.80–0.85fr and restore the column gap from space-10 to space-12) so the left text sits slightly further right. Keep the existing max-width: 34rem readability cap. This is iterative — make a conservative change, push, and wait for my eyeball.
2. (0b) Lead-magnet modal on the home hero — create src/components/sections/lead-magnet-dialog.tsx (client component) using the already-installed radix-ui Dialog, following the pattern in src/components/ui/sheet.tsx. It renders the outline "Download The House Promise (free)" button (with the Download icon) as the trigger and, in the dialog, the heading "Get The House Promise — free." + <LeadForm single idPrefix="home-lead" /> (reuse src/components/sections/lead-form.tsx unchanged). In src/app/page.tsx replace the hero's second CTA (the <a href="#booklet"> button) with this component, and remove the entire "#booklet" lead-magnet section at the bottom. Any new microcopy (e.g. the dialog's accessible title/close label) — draft per docs/page-copy/00-global/brand-voice.md, show me, and wait for approval before shipping.
3. (0c) Remove the other lead forms + record overrides — delete the .phx-footer-lead block from src/components/layout/site-footer.tsx and delete src/components/layout/footer-lead-form.tsx; remove the bottom LeadForm sections from src/app/bring-ph/page.tsx, src/app/experience/page.tsx, and src/app/our-support/page.tsx, making sure each page still ends cleanly (no dangling PageDivider/empty section). Leave the LeadForm component in place (the modal uses it). Then record in PROJECT-STATUS §4: the booklet lead magnet now lives ONLY in the home-hero modal (superseding the S1 "every page ends on the booklet block" decision + removing it from the footer chrome), and note FooterLeadForm is retired while the capture still hits /api/mailchimp/booklet-capture with the same lead-booklet-a/b tags.
4. (0d) Nav mega-menus — in src/components/layout/site-header.tsx remove the .phx-mega-thumbs render block and the now-unused thumbs data + any imports that go unused (e.g. ArtworkAssetId/Artwork if nothing else uses them in this file). In src/styles/globals.css change .phx-mega-inner from grid-template-columns: auto auto 1fr to two centered columns (auto auto with justify-content: center), adjusting gap if needed. Do NOT change any mega-menu link string — they are approved-via-mockup, verbatim. Record the thumbnail removal as a sanctioned header-chrome deviation in PROJECT-STATUS §4.
5. (0e) Apply page hero — in src/app/apply/page.tsx replace the centered .page-hero.is-center band with an .art-hero section that matches model/experience/bring-ph: left = the existing eyebrow "Apply" + h1 + lead (verbatim), right = <Artwork assetId="PH-APPLY-01" …/> moved up from the body's .apply-context-art block. Keep the .apply-grid body exactly as-is otherwise: "Who brings what" / "Before you apply" / "What happens next" context on the left and the ApplyForm card (including the locked Password field) on the right. Reuse the existing .art-hero / .art-hero-grid / .art-hero-copy / .art-hero-art classes — no new hero CSS unless the apply art column genuinely needs it.
6. (0f) Sprint exit gate — full-diff self-review of the whole branch (correctness · chrome consistency · copy verbatim · responsive at 320px + desktop). Confirm typecheck/lint/build green and nothing else regressed (header/footer still identical across pages, all 4 booklet removals clean, no broken #booklet anchors anywhere). Update docs/PROJECT-STATUS.md §1/§2 change log to note this pre-S13 UI-polish pass and that §4 carries the new overrides.

Per-step protocol (every sub-step): read the locked input(s) first; smallest safe change, reuse existing components/classes/tokens, no new dependencies, responsive + accessible (the modal must trap focus, close on Esc/overlay, restore focus to the trigger); verify pnpm run typecheck && lint && build + spot-check at 320px AND desktop + confirm the locked header/footer still render identically; self-review the diff; commit AND push every sub-step; report in ≤6 lines then STOP and WAIT for "proceed". For any NEW user-facing string, show the draft and wait for approval first.

Locked inputs: copy verbatim from docs/page-copy/ (never rewrite approved copy); brand voice for any new microcopy (docs/page-copy/00-global/brand-voice.md); design tokens + the .art-hero pattern + the .ph-card recipe (docs/DESIGN.md); the lead-magnet API contract is fixed (POST /api/mailchimp/booklet-capture with { email, booklet: "both" } → tags lead-booklet-a/b). NO DB/migration, NO auth/approval-gate/RLS change, NO CSP/header/env change, NO new dependency. When a change overrides a previously-locked decision, record it in PROJECT-STATUS §4 in the same step.

Push policy: commit + push after every gated sub-step (standing authorization) so I review in the open PR; never merge, never push beyond qa/image-placement-polish.
```

**0g (added mid-sprint):** move the site to `palestine-house.com`. Code: add the custom domain to `PRODUCTION_HOSTS` in `src/lib/safe-redirect.ts` (canonical `www`); everything else is env-driven (`NEXT_PUBLIC_SITE_URL`) + owner dashboard config. Recorded in §4.

</details>

## Review

Independent **Codex review** (god-level, branch diff vs `main`) = **APPROVE — zero blocking, zero non-blocking.** Per-area verdict all PASS: new client component (only new client boundary; home stays server; LeadForm posts `{email, booklet:"both"}`; radix-ui already present) · locked chrome (footer/header columns + link strings + mobile sheet intact; thumbnail removal complete) · lead-form removals (no `#booklet`/`FooterLeadForm`/dead-class references; API/tags unchanged) · /apply (server redirect + Password field intact; PH-APPLY used once) · home hero/artwork (typed source map matches files on disk) · domain cutover (host allow-list stays exact; scheme derived from host type; origins re-parsed/re-validated; `safeNextPath` still rejects absolute/protocol-relative/backslash). Gating checks PASS — no workspace/admin/API/middleware/Supabase/schema/package/CSP/header change, no new dependency, no client secret/env exposure. Codex also ran a `safe-redirect.ts` runtime smoke (www/apex hosts, spoofed host, spoofed proto, unsafe `next=`) — all passed.

## Checks & results

typecheck ✅ · lint ✅ · build ✅ (46 routes; `/`, `/bring-ph`, `/our-support` static; `/apply`, `/experience` dynamic) — green after every sub-step · Codex APPROVE ✅ · tree clean + pushed, `.env.local` untracked, no secrets in the diff. Owner Vercel-Preview eyeball (5 UI items at desktop + 320px) pending.

## Deviations & learnings

- **Two locked-decision overrides** (owner-authorized): removing the footer + page-bottom booklet forms **supersedes the S1 "every page ends on the booklet block" decision** and the locked-footer-chrome rule; removing the nav mega-menu thumbnails is a sanctioned header-chrome deviation. Both recorded in `PROJECT-STATUS §4` — future sessions: the booklet lead magnet lives **only** in the home-hero modal, and the mega-menus are thumbnail-free with centered columns.
- **One approved copy change:** the home platform-section line lost the word "below" (it had pointed at the removed booklet section; the lead magnet is now the hero button). No other approved copy touched.
- **Domain cutover was mostly free in code** because the app was already env-driven: `SITE_URL = NEXT_PUBLIC_SITE_URL` and an origin-relative CSP mean the *only* code change was the `safe-redirect.ts` allow-list (which the code had pre-flagged for D3). The real cutover is owner dashboard config. **Gotcha for future:** `NEXT_PUBLIC_*` is inlined at build time, so the env-var change needs a fresh Production deploy to take effect.
- **Dead-CSS hygiene:** removed every class orphaned by this sprint (`.phx-footer-lead*`, `.phx-footer-micro`, `.leadmagnet*`, `.apply-context-art`, `.phx-mega-thumbs`, the old `.home-leadmagnet*`). One nit left in place on purpose: the `.page-hero.is-center` variants are now unused (only `/apply` used them) but `.page-hero-inner` is still used by `/contact`, so the family stays — a safe S13 sweep item.
- Built on a branch the owner had already started (artwork PNG→JPG + true-ratio hero sizing); those 3 commits are part of the same PR and were covered by the Codex review.

## Follow-ups

- **Owner domain switch-on (dashboard, pending — completes the cutover):** set `NEXT_PUBLIC_SITE_URL=https://www.palestine-house.com` in Vercel **Production** (the `palestine-house-website` project) → set the Supabase **PROD** Auth **Site URL** to the same + **add** `https://www.palestine-house.com/**` and `https://palestine-house.com/**` to the redirect allow-list → **redeploy** Production → re-verify canonical/OG/sitemap/robots/JSON-LD + a password-reset link on the live domain. Steps recorded in `PROJECT-STATUS §4`.
- Owner Vercel-Preview eyeball of the 5 UI items (hero fit + copy shift · modal open/submit/focus · no footer/page lead forms · thumbnail-free centered mega-menus · /apply art-hero) at desktop + 320px.
- Merge → then **S13** (full testing + fix-all). The `.page-hero.is-center` dead-CSS nit can be swept in S13.
- After merge: reconcile trackers (flip the §2 board row to ✅ + add the merge PR/commit) — e.g. via `/close`.
