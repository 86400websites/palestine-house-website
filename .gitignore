# dependencies
node_modules/
.pnpm-store/

# next.js
.next/
out/

# production (root-only — must not match the src/app/(workspace)/build route or src/lib/build)
/build/

# vercel
.vercel/

# logs
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# local env files
.env
.env*.local

# typescript
*.tsbuildinfo

# testing / coverage
/coverage

# build tooling
.turbo/

# system files
.DS_Store
Thumbs.db

# local AI reference folders
# These are intentionally kept out of GitHub and pasted locally when needed.
docs/page-copy/
docs/page-designs/
docs/source-assets/

# The owner's mockup copy at docs/ root — kept on disk until the PP series closes (PP7); never commit it
docs/PH - Palestine House Final.html

# PP6c: 0030's down-migration carries 1.6 MB of the owner's APPROVAL-GATED guide
# prose as literal inserts (D-PP-k requires the bodies to be restorable). This
# repository is public, and S7 Step 7 verified that no gated content has ever
# been committed to it. So the file stays on disk and out of git; regenerate it
# read-only from production with `pnpm exec tsx scripts/generate-0030-down.ts`.
supabase/sql/migrations/0030_content_v2_cutover.down.sql
