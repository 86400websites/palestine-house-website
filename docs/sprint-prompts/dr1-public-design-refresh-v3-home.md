# Sprint DR1 — Public design refresh v3: foundations + Home

| | |
|---|---|
| **Date merged** | _pending — build complete 2026-07-03 (DR1-0…DR1-9, head `2c96d64`); owner merge + `/close` next. Fill the PR # + merge date at close._ |
| **Branch / PR** | `claude/sprint-dr1-design-v3-home` / PR TBD |
| **Goal** | Replace the illustration-on-paper public design with the owner's **warm, cultural, photography-led premium system (v3)** — foundations (tokens · imagery treatment · chrome · brand surfaces) + the redesigned **Home page** only. Frontend/design only; the gated workspace stays visually untouched. Also: retire the un-started S13/S14 to the ROADMAP §A backlog (owner re-direction — sprints added ad hoc from here). |

## What shipped (owner-gated DR1-0…DR1-7, push per step)

- **DR1-0 — Docs prune + sprint open.** S13 (test-and-fix) + S14 (hardening + relaunch) rows removed from ROADMAP §B / PROJECT-STATUS §2; scope preserved verbatim in ROADMAP §A → Post-MVP backlog (Upstash/Turnstile hardening marked **required before scale**); all forward-pointing S13/S14 references repointed; DR1 row opened.
- **DR1-1 — Asset pipeline.** The owner's 8 photos + logo (dropped into `public/assets/art/` with spacey names) moved to gitignored masters at `docs/source-assets/design-refs/v3/` (photos/ · examples/ · logo/) **before any commit** — the 2 example mockups and full-res originals never enter the public repo. New committed idempotent `scripts/optimize-photos.ts` (sharp devDependency, `pnpm tsx`) emits `public/assets/photos/ph-photo-*.jpg` (≤2000px, all <500KB; the 19.4MB film-screening master → ~430KB) + `public/assets/logo/ph-logo-mark.png` (checkerboard keyed to real alpha, copper arch cropped; the unused full-lockup output was dropped at the exit gate).
- **DR1-2 — v3 token foundations.** Additive raw tokens in `globals.css` `:root` (copper/olive/sage/cream/charcoal + `--line-warm`, `--header-h`); new `src/styles/v3.css` remaps the semantic aliases **only** inside `.ph-page, body:has(.ph-page)` (the `:has()` arm covers Radix portals — booklet dialog, mobile Sheet, tooltips; degradation without `:has()` is cosmetic). Scoped button restyle on the `data-slot`/`data-variant` hooks (outline → quiet cream; `button.tsx` untouched), `.v3-cta` opt-in modifier, `.v3-photo` frame, warm-charcoal dark sections, cream focus rings on dark. Zero legacy `:root` values changed → workspace structurally immune (grep: `.ph-page` renders only in the public SiteChrome branch).
- **DR1-3 — Chrome.** Header → warm cream glass + `data-overlay`/scroll-morph state machine (transparent over photo heroes on `OVERLAY_ROUTES`, solid on scroll ≥32px or any open menu; rAF passive listener). Footer → warm charcoal, copper column titles. New `BrandLogo` (copper arch mark PNG + real-text Spectral wordmark — the source PNG's dark wordmark would vanish on dark surfaces) in public header + footer; `logo.tsx` (workspace/admin) untouched.
- **DR1-4 — Home hero + culture split.** New `home-hero.tsx`: full-bleed tatreez photo pulled under the transparent header (`margin-top: -72px`), pre-computed two-layer AA scrim, owner-approved copy (H1 "A home for Palestinian culture, in every city." + subline + 3 icon mini-features), locked CTA pair (olive Apply `.v3-cta` + the booklet `LeadMagnetDialog`, same Mailchimp API) + quiet "Explore the model →" link. Culture split: cream panel (locked H2/eyebrow + the reference lead + two migrated locked closing lines) + full-bleed arch-café photo. `OVERLAY_ROUTES = ["/"]` activated. HOME_FEELS section retired (owner-approved).
- **DR1-5 — Inside strip + retained sections.** New `inside-strip.tsx`: 6 photo cards (3:4) in a native CSS scroll-snap strip (focusable region, keyboard/touch, no JS carousel — motion register held) with owner-approved short labels. Stages → editorial columns with copper 01/02/03 numerals (illustrations retired; copy verbatim). Proof strip numerals → copper on charcoal (numbers verbatim: 10 · 30 · 200+ · 267 · 120-day). ~190 lines of dead `home-*` CSS deleted from `pages.css` (grep-verified unreferenced). New shared `photo.tsx` (`PHOTO_SOURCES`, straight-edge frame; legacy `Artwork` untouched for other pages).
- **DR1-6 — Brand surfaces.** `icon.svg` → simplified copper-arch vector on cream (16px-legible); `apple-icon.tsx` + `opengraph-image.tsx` composite the **real mark PNG** (fs read, static prerender) — OG = mark + "Palestine House" + tracked copper "OUR CULTURE EMBASSY" + the locked tagline; manifest/`viewport.themeColor` → charcoal `#2A241E` / cream `#FAF6EE`. Rendered outputs eyeballed against the ™ mark.
- **DR1-7 — Exit gate.** Path-guard on the full diff (zero files under workspace/admin/ui/middleware/next.config/supabase) · **8-angle adversarial full-diff review** (line-by-line, removed-behavior, cross-file, reuse, simplification, efficiency, altitude, CLAUDE.md conventions) → ~29 candidates, deduped, **17 fixed on the branch**, the rest accepted + documented. Fixed highlights: the `.ph-page, body:has(.ph-page)` comma lists would be invalidated WHOLESALE in no-`:has()` browsers (an unsupported selector kills the whole list) → wrapped in forgiving `:is(...)` so in-page v3 always survives · overlay header no-JS/scroll-restoration hardening (`<noscript>` solid style + scroll-state reset on route change + throttle simplified) · restored the ≤420px full-width hero-CTA stacking the old hero had · strengthened the hero scrim's top/right bands for nav-over-bright-cloth AA, all stops now `color-mix(...var(--char-950)...)` (tokens, not literals) · "Live now" badge re-pinned to the locked status red (the `--brand-accent` remap had turned it copper) + green/olive token pairings re-paired on `/live`/`/our-support` · footer `BrandLogo` no longer preloads eagerly (`priority` is opt-in, header-only) · hero `sizes="max(100vw, 150svh)"` for portrait cover fetches · hero src now reads `PHOTO_SOURCES` (no duplicated path string) · stages head reuses `.sec-head` · proof-numeral copper moved into its component's own block in pages.css · `--header-h` now consumed by the header itself · dark-surface focus-ring recolor via one `--focus-ring-on-dark` custom property (selector list no longer duplicated) · unused `ph-logo-full.png` + its crop path deleted · optimizer quality-floor off-by-one fixed · unused Photo props dropped. Accepted/documented (not fixed): legacy white-painted art on the cream page shows a soft halo on the 8 un-redesigned pages (intermediate state, DR2 fixes properly; cream is ~5% off white so it reads as a mild warm cast) · app-wide `themeColor`/favicon charcoal reaches workspace browser tabs (owner-acknowledged brand surface; a per-shell `viewport` export can restore green later if wanted) · `OVERLAY_ROUTES` stays a header-side list until DR2 adds more photo heroes · v3.css rides the global stylesheet like pages.css does (fold at the future route-group split) · `Photo` and `Artwork` stay separate primitives by design. Then: `DESIGN.md` rewritten to v3 (two-tier system recorded; flag-saturation hard rule retained) · PROJECT-STATUS §1/§2/§4 (D-DR1)/§8 + ROADMAP updated · this record saved.

- **DR1-8 — Owner-directed final polish (2026-07-03, pre-merge).** Five owner decisions (recorded §4 D-DR1(7)): **(a)** nav **mega-menus removed** — panels, `.phx-mega*` CSS, chevrons and the open/Escape state machine deleted; all four labels are plain links with tooltip one-liners. **(b)** the **booklet lead magnet removed entirely** — `LeadMagnetDialog`, `LeadForm`, `POST /api/mailchimp/booklet-capture` and all lead-form/dialog CSS deleted (45 routes now); Mailchimp's only remaining use is the apply `applicant` tag; Resend untouched; the hero's second CTA is the outline **"Explore the model"**; the platform-card line shortened ("Not sure yet? Explore how it works — no account needed."); `EMAIL-SETUP-CHECKLIST.md` updated (booklet steps obsolete). **(c)** the "Inside a Palestine House" strip **auto-drifts as a pure-CSS marquee** — track holds the card list twice (duplicate `aria-hidden`), `translateX(-50%)` on a 60s linear loop, pauses on hover; under `prefers-reduced-motion` the animation is off and the strip becomes a manual scroller with the duplicate hidden (sanctioned exception to the §8 no-auto-carousel rule). **(d)** the platform split sits on the **oud-night photo band, mirrored via CSS** (`scaleX(-1)` — the focal side lands left of the white partner card), charcoal scrim, white copy, light focus rings. **(e)** **hero rebalanced** — h1 clamp stepped down to ≤3.4rem, copy enters via the existing `Stagger` primitive, mini-features tightened (22px icons, sm/xs type), hero capped at 52rem; ≤420px full-width CTA stacking retained. Gotcha: deleting an API route leaves stale generated types in `.next/types` — clear `.next` before the next `tsc`.

- **DR1-9 — v3 photo heroes on the five key pages + Home/mobile refinements (2026-07-03, pre-merge; landed AFTER the Codex approval → delta re-review recommended).** Owner supplied five hero masters → pipeline extended → `ph-photo-{model,experience,bring-house,our-support,apply}.jpg` (<300KB each). New shared **`PageHero`** component (Home's photo/scrim/stagger grammar, shorter band, copy verbatim — only new strings are five neutral alt texts) swapped onto `/model`, `/experience`, `/bring-ph`, `/our-support`, `/apply`; those routes joined `OVERLAY_ROUTES`; per-photo `object-position` keeps faces framed. The Bring-a-House master is chest-down by composition — the crop centers the coffee pour (alternate master = one-line swap). Home: copy nudged left (min()-guarded, never clips) · mini-features pinned lower · marquee 35s + `will-change` (keyframes verified in compiled CSS; a static strip on a reduced-motion OS is the designed fallback). Mobile (owner-directed): hero shows copy + CTAs only (features hidden ≤720px); the strip stacks as a vertical 4:3 list — desktop keeps the marquee. The five replaced `art-hero` illustration heroes also eliminate the biggest white-halo surfaces from the intermediate state.

- **DR1-10 — end-to-end designer QA (2026-07-03, owner-requested pre-sign-off).** Mobile reversals (features restored as a stacked column; copy slightly below center) + a 3-lens design review (desktop consistency / responsive 320-1440 / polish-details) → **21 fixes**: 320px header overflow (wordmark hides ≤420), 44px tap targets + custom sheet-close, mobile scrim retunes (hero ≤720 flat ramp, platform ≤980 top-weighted), cream page-hero eyebrows, one `--ring` repoint for light focus on all photo surfaces, /experience day-figure photo corrected (was a night shot under a day caption — pre-existing), **all stale illustration alts rewritten as photo descriptions** (/model /experience /bring-ph /about — the art files have been photographs since S1), /apply outline fix (h3→p), TatreezDivider palette prop (public olive/copper, workspace heritage), platform band → film-screening (no same-page photo repeat, mirror dropped), footer Apply joins `.v3-cta`, /model heading caps, strip↔stages join halved, one hero rail, focusable marquee, mobile strip `sizes` 100vw, synced header morph, split lead rhythm. **Open owner decision:** the supplied "The Model" hero master is byte-identical to the tatreez-workshop photo — /model's hero repeats a Home strip card until a distinct master is dropped in (`docs/source-assets/design-refs/v3/photos/model.jpeg` → rerun the pipeline).

## Prompt used

<details><summary>Approved plan (2026-07-02) — executed in-session with owner gates</summary>

The sprint ran from the approved plan at `~/.claude/plans/ok-so-will-we-flickering-pumpkin.md` (DR1 — Public design refresh v3: foundations + Home): owner decisions (reference images replace all prior design inputs; photos supplied in-repo; chrome restyle sanctioned; new logo adopted), token architecture (additive + `.ph-page`/`body:has(.ph-page)` scoped remap), button strategy (scoped CSS only), chrome state machine, Home section map, asset pipeline, gated step sequence DR1-0…DR1-7, and the copy-governance rule (every new/changed string surfaced at a gate — full copy table approved at DR1-4/DR1-5).

</details>

## Reviews

- **8-angle adversarial exit-gate review (DR1-7):** ~29 candidates → 17 fixes applied on-branch, remainder accepted + documented (details in "What shipped").
- **Independent Codex review (owner-run, 2026-07-03, at `fb3be31` — through DR1-8):** **APPROVE — zero blocking, zero non-blocking.** Verified: protected-path guard clean; lead-magnet removal left no dangling references; session probe same-origin + CSP untouched; marquee CSS-only with reduced-motion fallback; secret/path scan clean. (Codex note: `docs/page-copy/` absent from its checkout → copy compared against the diff + §4 D-DR1 instead; local build needed `NODE_OPTIONS=--max-old-space-size=4096`.)
- **Engine delta self-review of `fb3be31..2c96d64` (DR1-9, post-Codex):** clean — path-guard still CLEAN on the full branch; `OVERLAY_ROUTES` additions + `PageHero` hunks reviewed line-by-line; `.ph-page` confirmed `container-type: inline-size` (the `cqw` nudge math is valid); five hero pages' headlines/eyebrows/support lines grep-verified **verbatim** in the prerendered HTML; one `priority` image per page; five neutral new alt strings are the only new copy.

## Checks & results

- `pnpm run typecheck` / `lint` / `build` — green after **every** sub-step (46 routes; `/` static; apple-icon + OG prerender statically).
- Rendered verification per step: compiled-CSS greps (scoped remap present; the `*/`-in-comment optimizer bug caught + fixed at DR1-2), prerendered-HTML greps (hero copy, preload srcset, strip, proof numbers verbatim), favicon/apple-icon/OG PNGs extracted and eyeballed.
- Exit gate: diff path-guard CLEAN; 8-angle review + verify (findings + fixes listed in the PR/§8); secret-scan habits held (`.env.local` untracked; no secrets in diff).
- Owner Vercel-Preview walkthrough at each gate (mobile + desktop) — final pre-merge Preview pass recommended on: Home at 320/768/1440, header overlay→solid morph, booklet dialog POST, one workspace page (visually unchanged), reduced-motion.

## Deviations & learnings

- **`--surface-hero` stayed a light cream** (plan draft had charcoal): `/apply`, auth pages and other art-heroes still consume it with dark text mid-migration; the v3 Home hero uses its own photo+scrim instead.
- **CSS-comment gotcha:** `ws-*/adm` inside a CSS comment terminated it early (`*/`) and the optimizer silently dropped the entire remap rule — caught only because verification grepped the **compiled** CSS. Always verify the built stylesheet, not the source.
- The pixel-faithful mark PNG beats a hand vector for apple-icon/OG (composite the asset); the favicon still wants a simplified vector for 16px legibility.
- Filenames with trailing spaces (OneDrive drops) must be renamed via git-bash, not PowerShell.

## Follow-ups

- **DR2+:** remaining public page BODIES get their v3 layouts (`/model`, `/experience`, `/bring-ph`, `/our-support` bodies below their new heroes, plus `/live`, `/about`, `/focus-areas`, `/contact`, auth, legal) — until then they intentionally render old layouts in v3 colors.
- **Bring-a-House hero master** is framed chest-down (no head in the source) — the crop centers the coffee pour deliberately; owner may supply an alternate master (one-line swap in `bring-ph/page.tsx` + one pipeline entry).
- The two public **booklet PDFs stay in Storage** and remain featured in the gated `/resources`; only the public capture UI was retired (D-DR1(7b)).
- Request from the designer: the logo **SVG master** + a reverse (light) wordmark variant; a higher-res hero master (current 1535px is thin for 2× large desktops).
- Owner: confirm photo usage rights/consent for identifiable people; consider publishing the v3 foundations to a new Claude Design **V3** project for browsable preview.
- Workspace logo swap (green arch → copper) deferred to the future workspace-refresh sprint (2-line change).
- The retired S13/S14 scope lives in ROADMAP §A backlog — the **hardening pass remains required before scale**.
