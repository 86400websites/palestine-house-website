# Palestine House — Roadmap (Features & Sprints)

> **What we build, in what order.** This is the master plan: the full feature list, then the stage/sprint sequence with scope, dependencies, and exit gates. Run **one sprint at a time**; big sprints are split into lettered phases, each its own branch → PR → Preview → merge loop (`WORKFLOW.md`). Where we currently stand lives in [`PROJECT-STATUS.md`](./PROJECT-STATUS.md) — update it in the same PR that completes a sprint or phase. Architecture detail: [`TECH-ARCHITECTURE.md`](./TECH-ARCHITECTURE.md) §0 and `/docs/page-designs/content/PH_Sitemap_Architecture_TECH.txt`.

---

## A. Full feature list (MVP scope)

### Public shell
- [x] Locked site chrome — header (logo → Home + four nav labels with tooltip one-liners, mega-menus on The Model + Experience per mockup, + Sign in + green **Apply** CTA), footer (link columns, lead-magnet block, legal, tagline), mobile Sheet menu
- [x] `/` Home (full "Bring Palestine House to Your City" section)
- [x] `/model` The Model
- [x] `/experience` Experience — the decision page (hero artwork, day/night section, five programming threads, closing CTA, lead magnet) — *live strip wired to `programming_sessions` in S9 (9d), same shared SessionCard as `/live`*
- [x] `/bring-ph` Bring a House (absorbs How It Works: stage triptych + Day 30/60/108 milestone timeline, "who brings what," three rules)
- [x] `/our-support` Our Support
- [x] `/live` Live Programming — *real session data + `/live/[id]` watch view (youtube-nocookie) + interactive `?mode=` filters shipped in S9 (9a–9c)*  **[public hero feature]**
- [x] `/apply` — *live (S3c): sign-up → pending account + application record (zod-validated); single "Your name" field per D5 + an owner-approved Password field (§4). Rate-limit + Turnstile live in the **Post-MVP backlog hardening pass** (ex-S14, retired un-started 2026-07-02; required before scale) — see `PROJECT-STATUS.md` §7*
- [x] `/about`, `/contact` (form UI built; sends via Resend in S12), `/focus-areas` (secondary route, linked from page bodies), `/privacy`, `/terms` (placeholder legal copy, counsel before launch)
- [ ] Lead magnets: the two public booklets (*The House Promise*, *Operating Model & Governance*) — *capture forms built (Stage 0, honest no-op); Mailchimp tags `lead-booklet-a` / `lead-booklet-b` in S12*
- [x] SEO: per-route metadata, `sitemap.ts`, `robots.ts`, OG image, JSON-LD (Organization, WebSite)

### Auth & access
- [x] `/login`, `/forgot-password`, `/update-password` (no separate signup — retired, merged into Apply) — *live S3a/3b; `/auth/confirm` exchanges the reset code→session*
- [x] Supabase `@supabase/ssr` browser + server clients, `src/middleware.ts` session refresh, same-origin validated redirects (`safe-redirect.ts`) — *S3a; route protection enforcement is S4*
- [x] Approval gate: `profiles.is_approved`, pending-state dashboard, server-side session gate on the gated route group (content RPCs inherit the pattern in S5/S6) — *S4*
- [x] `/admin/approvals` — HQ approval queue (server-checked `admins` table) — *S4*  **[MVP-critical]**
- [x] **Session-aware Apply CTA** — the public header's green Apply button becomes **My Dashboard** (→ `/dashboard`) once signed in (any state); logged-out keeps Apply. Same-origin session probe, public pages stay static, CSP unchanged — *S6 Step 3.5 (owner-requested UX)*
- [x] **Apply → dashboard redirect** — a successful application signs the partner in (instant session, email confirmation OFF) and lands them on the `/dashboard` pending "under review" state, with no separate sign-in — *S6 Step 3.5 (owner-requested UX)*

### Partner platform (gated)
- [x] Gated shell: persistent left sidebar + pending/locked state — *S4 (S6 destinations inert until built)*
- [x] `/dashboard` Welcome as a Partner (orientation by stage; "current stage" snapshot — Design & Build % hidden until Build starts) — *S6 6a*
- [x] `/plan` Plan & Prepare (reference) · `/operate` Operate & Program / Managing & Operating (one page, two entry points) — *S6 6b*
- [x] `/build` Design & Build — launch checklist: 200+ items, **saved per-user progress** (the only per-user interactivity) — *S6 6c (grouped by the 10 focus areas; milestone gates retired site-wide, D-S8-c)*
- [x] `/elements/[slug]` — tabbed canonical element page component × 30 (A1–J3), MDX bodies — *S6 6d (DB markdown, sanitized server-side)*
- [x] `/resources` + `/resources/[category]` — guides, 267 templates, tools; private Storage bucket + server-issued signed URLs — *S6 6e (migration 0017)*
- [x] `/academy` + `/academy/[slug]` — optional video reference library (no progress, no quizzes, no certificate) — *S6 6f: `/academy` library shipped; `/academy/[slug]` not built at MVP (each topic's Video tab + card link covers it, §3)*
- [x] `/live` partner tools — publish a YouTube link + event metadata from the gated `/programming` tool → populates the public listing — *S9 9f (migrations 0020/0021; owner-scoped, approval-gated)*
- [x] `/tools` House Applications — coming-soon placeholder — *S6 6g*
- [x] `/account`, `/support` — *S6 6g (migrations 0018/0019; `/account` Delete hidden D-S6-c; `/support` email deferred — requests stored)*

### Platform plumbing
- [x] Database: `profiles`, `applications`, `admins`, `elements`, `checklist_items`, `checklist_progress`, `programming_sessions`, `resources`, `academy_modules` — RLS default-deny, hardened `SECURITY DEFINER` RPCs, anon-safe public projections — *S2/S5; S6 adds the download/account/support writes (migrations 0017–0019). Test ✓; prod apply of 0016–0019 pending owner*
- [ ] Email: Mailchimp (lead magnets, newsletter, apply tagging) + Resend (contact, transactional/approval notifications), verified sending domain — *wired as no-op placeholders in **S12**; live when the owner adds keys + verifies the Resend domain*
- [ ] Hardening: Upstash rate limiting, Turnstile, security headers + CSP (embed origin only), fail-closed production forms — *moved to the Post-MVP backlog (S14 retired 2026-07-02); **required before scale***
- [ ] CI (typecheck, lint, build, gitleaks) + branch protection + Vercel Preview/Production

### Post-MVP backlog (do not build during MVP)
`/search` (V1) · `/admin/partner-interest` (later) · functional House Applications tools · additional languages · RSVP for in-person events (MVP is listing-only, pending confirmation). *(`/admin/content` graduated from this backlog to **S11 — Admin content management**, 2026-06-26.)*

**Absorbed from the retired S13/S14 sprints (2026-07-02, owner direction — do not lose):**
- **Hardening (ex-S14 · required before scale):** Upstash rate-limiting + Turnstile on the public writes (`/apply`, contact, lead magnet) · fail-closed verification in real Production · CSP/security headers re-verified live · secret-path threat-model pass · full `SECURITY-CHECKLIST.md` §1–§15 pass. Brief: `docs/sprint-prompts/s14-final-hardening-relaunch.md`.
- **Full-site test-and-fix pass (ex-S13):** multi-hat end-to-end walk (copywriting → UX → feature — every page, CTA, link, a11y, security) → fix everything found → re-verify to 0 known bugs. Brief: `docs/sprint-prompts/s13-full-testing-fix-all.md`.
- **Domain/SEO relaunch verification (ex-S14):** after the owner's domain switch-on (`PROJECT-STATUS.md` §4), re-verify canonical/OG/sitemap/robots/JSON-LD + an auth email link on `https://www.palestine-house.com`.

---

## B. Stages & sprints

### Stage 0 — Foundational build (local · public shell only · no Supabase)

Goal: a complete, polished, working public website running locally. All forms render but **no-op cleanly** (no env vars yet). Live Programming renders its designed empty/fallback states.

| Sprint | Scope | Notes |
|---|---|---|
| **0.1 Foundation** | Scaffold (Next.js 15, TS strict, pnpm, Tailwind v4, shadcn/ui, Framer Motion); copy the 10 core docs + `/docs` content layer in; port `/docs/page-designs/design-system/tokens/` + `shared/pages.css` primitives into `globals.css`; `next/font` Spectral + Inter; motion primitives; security headers in `next.config.ts`; locked chrome (header + Tooltip nav + footer + mobile Sheet); 404/error pages | Scaffold + repo + CI + Vercel Preview pre-completed (pre-sprints 0a–0d, PRs #1–#2); design system is in-repo |
| **0.2 Home + Model** | `/` and `/model` from `/docs/page-designs/` + `/docs/page-copy/`, with final artwork from `/docs/page-designs/assets/art/` | |
| **0.3 New marketing pages** | `/experience` (live strip in empty/fallback state), `/bring-ph` (triptych + milestone timeline), `/our-support` | Densest pages — pace per `DESIGN.md` §12 |
| **0.4 Live + supporting pages** | `/live` public listing + watch view (static empty states), `/focus-areas`, `/about`, `/contact` (form UI, no-op) | |
| **0.5 Apply + auth UI + legal + SEO** | `/apply` form UI (no-op submit with honest "not yet live" handling), `/login` `/forgot-password` `/update-password` UI shells, `/privacy` `/terms`, metadata + sitemap + robots + OG | |

**Stage 0 exit gate:** typecheck/lint/build green · every public page matches its mockup at 320px → desktop · copy verbatim · proof numbers correct (10 · 30 · 200+ · 267 · 120-day launch) · reduced-motion respected · no console errors · headers/footer identical everywhere.

### Stage 1 — Connect & launch the barebones site

| Step | Scope |
|---|---|
| **1.1 GitHub** | Push repo; branch protection on `main`; CI (install, typecheck, lint, build, gitleaks) as required check; secret scanning + Dependabot on — *repo + base CI already live since pre-sprint 0c; remaining: gitleaks step, branch protection, secret scanning, Dependabot* |
| **1.2 Vercel** | Import (`nextjs` preset); env vars per environment (`SUPABASE-VERCEL-SETUP.md`); Production deploy of the public site (Vercel domain per resolved decision D3; custom domain later) — *project already connected with Preview verified (PR #2); remaining: env vars + first Production deploy* |
| **1.3 Supabase projects** | Create **production + non-production** projects (no schema yet); Auth → URL Configuration (Site URL, local + Preview wildcard + Production redirects); record refs in `SUPABASE-VERCEL-SETUP.md` + `PROJECT-STATUS.md` |

**Stage 1 exit gate:** live public site verified desktop + mobile · CI required + branch protection active · Preview deployments working · env matrix recorded.

> From here on: **every sprint = branch → PR → CI → Preview → merge**, status updated in the same PR.

### Stage 2 — Sprints (run in order)

| Sprint | Phases | Scope & exit gate | Depends on |
|---|---|---|---|
| **S1 — Design consistency pass** | 1a tokens/type audit · 1b motion + reduced-motion · 1c responsive 320px+ audit · 1d accessibility AA (contrast, focus, skip link, keyboard tooltip) | Whole public shell consistent with `DESIGN.md`; AA verified; zero visual drift between pages | Stage 1 |
| ✅ **S2 — Database phase 1: identity & approval** *(done 2026-06-15, merge pending)* | 2a `profiles` (+`is_approved`), `applications`, `admins` + RLS + RPCs · 2b apply to non-prod, test anon/authed, then prod | Versioned up + `.down.sql` + policies in PR; default-deny verified | Stage 1 |
| ✅ **S3 — Auth complete** *(merged 2026-06-16, PR #19)* | 3a Supabase clients + `src/middleware.ts` + login/logout · 3b forgot/update password + `/auth/confirm` (same-origin redirects; Preview emails → Preview) · 3c **Apply live** = sign-up + pending account + application record (zod; `full_name` via the `0008` trigger; + owner-approved Password field, §4) | All auth flows pass on local + Preview; `SUPABASE-VERCEL-SETUP.md` checklists green | S2 |
| ✅ **S4 — Approval gate + admin** *(built 2026-06-17, PR open; Preview + merge pending)* | 4a gated shell + sidebar + `/dashboard` pending state · 4b `/admin/approvals` queue (server-checked `admins`) · 4c server-side session gate on the gated route group + post-login → `/dashboard` (**approval-notification email deferred to S10** — Resend domain unverified until then; **"Add note" → backlog**) | 🔴 `SECURITY-CHECKLIST.md` §15 self-reviewed; pending user sees only pending state; approval unlocks without re-login; non-admin → 404 on `/admin/*` | S3 |
| ✅ **S5 — Database phase 2: content schema** *(merged 2026-06-18, PR #23)* | 5a `elements` + content ingestion (30 topics from `/docs/page-copy/06-elements`) · 5b `checklist_items` + `checklist_progress` (200+ items) · 5c `programming_sessions` (anon-safe public read) · 5d `resources` metadata + private Storage bucket + 267 templates upload · 5e `academy_modules` | Each phase: non-prod first, RLS verified anon vs pending vs approved | S4 |
| **S6 — Private platform pages** | 6a `/dashboard` full (current-stage snapshot rule) · 6b `/plan` + `/operate` · 6c `/build` checklist tracker (saved progress) · 6d element page component + 30 instances · 6e `/resources` hub + signed-URL downloads · 6f `/academy` · 6g `/tools` placeholder + `/account` + `/support` | Private-page mockups already in `/docs/page-designs/member-workspace/` (admin in `/docs/page-designs/admin/`) | S5 |
| ✅ 🚀 **S7 — Final review & launch** *(LAUNCHED 2026-06-19, PR #29 — production live + public-layer verified; Codex review = approve, zero blocking)* | 7a full-site QA: cross-browser/device, 0 known bugs, consistency sweep · 7b content verification (copy verbatim, numbers, booklet filename mapping) · 7c SEO/structured data + performance (CWV) · 7d go-live on the **Vercel domain** + post-deploy checks (custom domain post-launch, §4) | Every **launch-blocking** checklist green; `PROJECT-STATUS.md` marked **Launched**. ⚠️ Launches **before** the post-launch sprints — public writes go live without Upstash rate-limit/Turnstile (accepted owner decision, §5/§7); the HQ approval gate keeps abuse out of gated content | **S6** |
### Stage 3 — Post-launch (run in order)

> Sequenced after go-live. **S8–S12 (+ the pre-S13 UI-polish/domain pass) are all merged.** **Re-directed 2026-07-02 per owner direction:** the un-started S13/S14 rows are retired to the §A backlog — the post-launch focus shifts to the **public design refresh v3** (warm, cultural, photography-led premium system; DR-series sprints, added ad hoc by the owner as the site is already live). One sprint at a time, same branch → PR → Preview → merge loop.

| Sprint | Phases | Scope & exit gate | Depends on |
|---|---|---|---|
| **S8 — Workspace Visual Polish** *(post-launch · ✅ merged PR #32, 2026-06-24)* | 8a shared shell + premium kit (reveals · tatreez dividers · paper washes · card-lift · empty/loading states) · 8b dashboard (3 states) · 8c plan + operate · 8d build tracker · 8e element page · 8f resources + resources/[category] + academy · 8g account + support + tools · **8i topic-page redesign + inline template downloads** · **8j workspace logo → public home** (public header trialled, then removed per owner preference) · **8k retire the milestone gates concept site-wide (D-S8-c)** · 8h exit gate + review | Lift all ~12 gated pages to the **public-shell premium bar** using already-approved patterns + design-system tokens (no new design language, no new mockups); behaviour untouched. Grew on live owner feedback (topic-page redesign, workspace logo → home, gate removal — D-S8-b / D-S8-c). Exit: every workspace route reads premium + digestible at 320px + desktop + reduced-motion (owner Preview sign-off); typecheck/lint/build green; zero behaviour changes; independent Codex review = approve (one finding fixed). Record: `docs/sprint-prompts/s8-workspace-visual-polish.md`. | S6 |
| **S9 — Live Programming** *(post-launch · ✅ merged PR #34, 2026-06-25)* | 9a `/live` on real data + watch view (YouTube embed; CSP extended for the YouTube origin only — D1) · 9b Experience live-strip wired to the same feed/component · 9c partner publishing = **publish a YouTube link + event metadata from the gated `/programming` tool** (owner-scoped writes under RLS) | Simplest powerful path — no streaming infra. The partner-publish UI (the one prior design gap, §3) shipped as `/programming`. Exit ✅ **met:** `/live` + Experience on real data; publish flow owner-scoped (migrations `0020`+`0021`, applied+verified test+prod); empty states still graceful; **D2 (in-person RSVP → listing-only) RESOLVED**. Per-step + 4-lens exit-gate adversarial reviews clean. Record: `docs/sprint-prompts/s9-live-programming.md`. | S6, S8 |
| **S10 — Private-pages clarity & polish** *(post-launch · ✅ merged PR #37, 2026-06-26)* | 10-1 simplify the workspace menu (flatten the group headers; drop the duplicate **Program** + coming-soon **House Applications**; Library→**Resources** › Videos + Tools & Templates) · 10-2 operate-vs-program content re-grouping (**proposal-first**, all 30 topics intact) · 10-3 crystal-clear beginner intro copy on every gated page · 10-4 Build CTAs reordered (Read the full guide · Watch video · Mark in progress · Add notes · Mark Complete) · 10-5 code-level sample videos + premium **white** cards · 10-6 mobile fixes (esp. the element/topic page) · 10-7 exit gate | Make the gated workspace simple, premium, idiot-proof — **UI / copy / curation only**; intentionally revises previously-locked workspace menu/curation/intro-copy with owner authorization (**D-S10-b**). **No DB / auth / RLS / approval-gate change; no public-page change.** Exit: every private page reads clear + premium + balanced at 320px + desktop + reduced-motion (owner Preview sign-off); all 30 topics intact; typecheck/lint/build green. Brief: `docs/sprint-prompts/s10-private-pages-clarity.md`. | S6, S8 |
| **S11 — Admin content management** *(post-launch · ✅ merged PR #39, 2026-06-27 — migrations 0023/0024 applied + verified TEST + PROD; 4-lens exit review PASS + Codex review 1-blocking-fixed)* | 11a admin-panel CRUD over the private-page content (the 30 element/topic bodies, academy videos, resources/templates) behind the existing `is_admin()` gate · 11b add / update / delete with RLS-safe writes · *(the old-S10 admin-management UI — add/remove admins — folds in here, to confirm at scope)* | The client fully self-manages all private-page content + resources without scripts — builds the graduated backlog `/admin/content`. Exit: an admin can add/update/delete content end-to-end behind the admin gate; writes RLS-safe; no content loss; versioned up + `.down.sql` + RLS in the PR (TEST-first, `WORKFLOW.md` §14). Brief: `docs/sprint-prompts/s11-admin-content-management.md`. | S5, S10 |
| **S12 — Email-automation placeholders + settings** *(post-launch · ✅ merged PR #41, 2026-06-27 — Codex review = approve; no migration; switch-on by env vars + Resend domain, see `docs/EMAIL-SETUP-CHECKLIST.md`)* | 12a Mailchimp: booklet capture (`lead-booklet-a/b`) + newsletter + apply tagging · 12b Resend: contact + support + the **approval/decline email** (deferred from S4) + **declined→contact** path · 12c all settings + env wiring, no-op until keys present; document SPF/DKIM/DMARC verification | Everything wired with honest no-op placeholders so the owner goes live by **adding keys/domain alone** (Mailchimp API key + audience id; Resend env + verified sending domain). Absorbs the old-S10 email + approval/decline email. Exit: each integration no-ops cleanly without env vars; keys server-only; switch-on is env-vars-only. Brief: `docs/sprint-prompts/s12-email-automation-placeholders.md`. | S3 |
| **DR1 — Public design refresh v3: foundations + Home** *(post-launch · ✅ merged PR #45, 2026-07-03 — post-merge `/close` = GO; Codex = APPROVE zero blocking)* | DR1-0 docs prune + sprint open · DR1-1 asset hygiene (photos/logo optimized + renamed; example mockups out of `public/`) · DR1-2 v3 token foundations (additive tokens, public-scoped remap) · DR1-3 chrome (warm solid header + transparent-over-photo on Home · new BrandLogo · charcoal footer) · DR1-4 Home hero + culture split · DR1-5 "Inside a Palestine House" photo strip + retained sections · DR1-6 brand surfaces (favicon/apple-icon/OG/manifest) · DR1-7 exit gate + `DESIGN.md` v3 rewrite · DR1-8 owner final polish (mega-menus removed · booklet lead magnet removed, hero CTA-2 = "Explore the model" · Inside-a-House auto-marquee · platform photo band · hero rebalance + mobile pass) · DR1-9 v3 photo heroes on /model /experience /bring-ph /our-support /apply (shared `PageHero`, copy verbatim, overlay header) + Home/mobile refinements (copy nudge · features lower · 35s marquee · mobile hero = copy+CTAs only · mobile strip stacks vertically) | Replace the illustration-on-paper public design with the owner's **warm, cultural, photography-led premium system (v3)** — foundations (tokens · type · imagery treatment · chrome) + the redesigned **Home page** only; frontend/design only, **zero feature/DB/auth/gate/CSP/routing change**; the gated workspace stays visually untouched (scoped tokens + diff path-guard); every new/changed string is owner-gated before it ships. Exit: v3 foundations live across the public shell; Home matches the owner's reference examples; AA contrast verified on rendered pages; workspace unaffected; typecheck/lint/build green; `DESIGN.md` rewritten to v3 + overrides recorded in `PROJECT-STATUS.md` §4. Brief: `docs/sprint-prompts/dr1-public-design-refresh-v3-home.md`. | S12 |
| **DR2 — Home v3 sections + chrome: stages · proof band · footer** *(post-launch · ✅ merged PR #48, 2026-07-06 — post-merge `/close` = GO; Codex = APPROVE zero blocking)* | DR2-0 docs-only sprint open (this row + `PROJECT-STATUS.md` flip; owner drops the master images into `docs/source-assets/design-refs/v3/` — `photos/` 3 stage photos · `art/` line-art + 4 olive-branch PNGs · `refs/` stages-section / proof-band / footer mockups + palette poster) · DR2-1 asset pipeline (extend + run `scripts/optimize-photos.ts`: stage photos → jpg ≤2000px <500 KB; olive branches → alpha-preserving PNG/WebP; line-art keyed or matched-background; register ids) · DR2-2 additive tokens (dark olive-green band + stage-plate green + warm-sand numeral, sampled from the mockups) + a `.ph-section-olive` band variant · DR2-3 Home "One path, three stages" rebuild (photo cards + dark-green arch-notch number plates + the line-art beside the grid; copy-table gate for the sub-line + card descriptions) · DR2-4 proof band restyle (`.ph-section-olive` + olive-branch ornaments + warm-sand numerals; layout per mockup; label-casing copy gate) · DR2-5 footer refresh (Arabic tagline line `lang="ar" dir="rtl"` · column labels per mockup · olive sprig; the Apply band + single-conversion rule survive; copy gate) · DR2-6 dedicated mobile-responsiveness pass (owner-added 2026-07-06: 320px→768px sweep of the three new surfaces + chrome on the key routes; fix everything found) · DR2-7 exit gate (adversarial full-diff review · 320px/desktop + AA on the green surfaces · path-guard · `DESIGN.md` §3/§5/§12 + trackers + record) | Bring the three owner-mocked surfaces to final v3 from the owner's new DR2 mockups: the Home stages section, the proof-numbers band, and the global footer. **Additive tokens only** (owner decision 2026-07-06 — existing v3 values untouched; the palette poster is reference, not a retune); frontend/design only, **zero feature/DB/auth/gate/CSP/routing change**; the gated workspace stays untouched (path-guard); proof numbers verbatim (10 · 30 · 200+ · 267 · 120-day launch); every new/changed string ships only via owner-approved copy tables at the sub-step gates. The footer change is the owner updating the locked chrome itself — recorded in `DESIGN.md` §5. **Remaining public page bodies → DR3.** Exit: the three surfaces match the mockups; AA verified; typecheck/lint/build green; workspace unaffected; trackers + `DESIGN.md` updated; record `docs/sprint-prompts/dr2-stages-proof-footer.md`. | DR1 |
| **DR2.1 — Brand lockup rollout + Home band polish + bring-ph stage cards** *(post-launch · ✅ merged PR #50, 2026-07-06 — post-merge `/close` = GO; Codex = APPROVE zero blocking)* | DR2.1-0 docs-only sprint open · DR2.1-1 logo asset pipeline (the owner's new lockup master — copper arch + all-caps serif wordmark + "OUR CULTURE EMBASSY" tagline — relocated out of `public/` into gitignored `docs/source-assets/design-refs/v3/`; cropped/optimized `public/assets/logo/ph-logo-lockup.png` via `scripts/optimize-photos.ts`) · DR2.1-2 BrandLogo v2 (full lockup PNG on light chrome; adaptive real-text lockup in cream on the dark footer + the `[data-overlay]` photo-header state — owner decision 2026-07-06; copy gate for the tagline string) · DR2.1-3 proof-band polish (the caption between the two sprigs sized to ~2 balanced desktop lines; the bottom-right olive branch adjusted per the owner's reference re-attached at this gate) · DR2.1-4 bring-ph stage cards (Home's `v3-stage-card` grid extracted to a shared cards-only component replacing the `.bring-triptych` — no side art; copy gate for card-text reuse) · DR2.1-5 dedicated responsive sweep 320→1440 over the touched surfaces + public-route spot-check · DR2.1-6 exit gate (adversarial full-diff review · AA on the text lockup over photos + footer · path-guard · `DESIGN.md` §5 + trackers + record) | Owner-requested ad-hoc polish pass on DR1/DR2 surfaces (2026-07-06): the new logo used site-wide in the public chrome, the proof band refined, the Home stage cards reused on `/bring-ph`, responsiveness verified. Frontend/design only — **zero feature/DB/auth/gate/CSP/routing change**; gated workspace untouched (path-guard); proof numbers verbatim (10 · 30 · 200+ · 267 · 120-day); new/changed strings only via owner-gated copy tables. The chrome-logo change is the owner updating the locked chrome itself → `DESIGN.md` §5. **DR3 (public page bodies) stays next.** Exit: the lockup correct in both header states + footer; caption = 2 desktop lines; bring-ph cards match Home's; 320px+ clean; typecheck/lint/build green; record `docs/sprint-prompts/dr2-1-logo-band-cards.md`. | DR2 |

> **Retired from the plan (2026-07-02, owner direction):** the un-started **S13 (full testing + fix-all)** and **S14 (final hardening + relaunch)** rows were removed — the owner is changing direction post-launch (public design refresh first) and will add sprints ad hoc. Their scope is **preserved in §A → Post-MVP backlog** (the hardening items remain **required before scale**); the original briefs stay at `docs/sprint-prompts/s13-full-testing-fix-all.md` + `s14-final-hardening-relaunch.md`.

### Sprint exit gate (applies to every sprint)

- [ ] Typecheck + lint + build green locally; CI green
- [ ] Vercel Preview tested, desktop + mobile
- [ ] Relevant `SECURITY-CHECKLIST.md` sections pass (always §15 once auth exists)
- [ ] Copy verbatim; design matches mockups/tokens; numbers correct
- [ ] No new console errors or hydration warnings
- [ ] `PROJECT-STATUS.md` updated in the same PR

## C. Ordering rationale (read once)

Public shell first because it has zero data dependencies and gets a real site live fast. Identity schema (S2) before auth (S3) because Apply writes `profiles` + `applications` on day one. The approval gate (S4) immediately after auth because **no gated content may ship before the gate is proven** — that's why content schema (S5) and private pages (S6) come after. Private pages (S6) complete the MVP product, so **launch (S7) comes next** — the owner ships on the Vercel domain immediately after S6. **Stage 3 is post-launch.** Workspace Visual Polish (S8) and Live Programming (S9) are merged. **Replanned 2026-06-26 (D-S10-b):** make the partner platform crystal-clear and premium (S10) → give the client full content control (S11) → wire the email automations as ready-to-switch-on placeholders (S12). (Earlier post-launch orders ran Live/Email/Hardening as S8–S10, then S8 polish / S9 Live / S10 Email / S11 QA / S12 Hardening — both superseded.) **Re-directed 2026-07-02 (owner):** with S8–S12 merged and the site live, the planned S13 (test-and-fix) / S14 (hardening + relaunch) were retired to the §A backlog; post-launch work continues as owner-added ad-hoc sprints, starting with the **DR series** (public design refresh v3 — foundations + Home first, remaining public pages after). Deliberate tradeoff carried since the MVP launch: public writes (Apply, contact, support) went live without Upstash rate-limiting/Turnstile — accepted knowingly, since the HQ approval gate keeps any abuse out of gated content; the full fix is tracked in the **§A Post-MVP backlog (required before scale)** (`PROJECT-STATUS.md` §5/§7).
