/* Framer Motion DOM animation features, isolated for lazy loading — imported
   asynchronously by LazyMotion in providers.tsx so the animation engine ships
   in its own deferred chunk instead of the first-load bundle. */
export { domAnimation as default } from "framer-motion";
