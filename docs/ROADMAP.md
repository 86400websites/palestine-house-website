# Palestine House — Roadmap (Features & Sprints)

> **What we build, in what order.** This is the master plan: the full feature list, then the stage/sprint sequence with scope, dependencies, and exit gates. Run **one sprint at a time**; big sprints are split into lettered phases, each its own branch → PR → Preview → merge loop (`WORKFLOW.md`). Where we currently stand lives in [`PROJECT-STATUS.md`](./PROJECT-STATUS.md) — update it in the same PR that completes a sprint or phase. Architecture detail: [`TECH-ARCHITECTURE.md`](./TECH-ARCHITECTURE.md) §0 and `/docs/page-designs/content/PH_Sitemap_Architecture_TECH.txt`.

---

## A. Full feature list (MVP scope)

### Public shell
- [x] Locked site chrome — header (logo → Home + four nav labels with tooltip one-liners, mega-menus on The Model + Experience per mockup, + Sign in + green **Apply** CTA), footer (link columns, lead-magnet block, legal, tagline), mobile Sheet menu
- [x] `/` Home (full "Bring Palestine House to Your City" section)
- [x] `/model` The Model
- [x] `/experience` Experience — the decision page (hero artwork, day/night section, five programming threads, closing CTA, lead magnet) — *live strip wired to `programming_sessions` in S9 (9d), same shared SessionCard as `/live`*
- [x] `/bring-ph` Bring a House (absorbs How It Works: stage triptych + Day 30/60/108 milestone timeline, "who brings what," three rules)
- [x] `/our-support` Our Support
- [x] `/live` Live Programming — *real session data + `/live/[id]` watch view (youtube-nocookie) + interactive `?mode=` filters shipped in S9 (9a–9c)*  **[public hero feature]**
- [x] `/apply` — *live (S3c): sign-up → pending account + application record (zod-validated); single "Your name" field per D5 + an owner-approved Password field (§4). Rate-limit + Turnstile live in the **Post-MVP backlog hardening pass** (ex-S14, retired un-started 2026-07-02; required before scale) — see `PROJECT-STATUS.md` §7*
- [x] `/about`, `/contact` (form UI built; sends via Resend in S12), `/focus-areas` (secondary route, linked from page bodies), `/privacy`, `/terms` (placeholder legal copy, counsel before launch)
- ~~Lead magnets: the two public booklets (*The House Promise*, *Operating Model & Governance*)~~ — **removed in DR1-8 (owner decision, 2026-07-02):** the capture dialog/forms + `/api/mailchimp/booklet-capture` were deleted; the two booklet PDFs remain the only public files
- [x] SEO: per-route metadata, `sitemap.ts`, `robots.ts`, OG image, JSON-LD (Organization, WebSite)

### Auth & access
- [x] `/login`, `/forgot-password`, `/update-password` (no separate signup — retired, merged into Apply) — *live S3a/3b; `/auth/confirm` exchanges the reset code→session*
- [x] Supabase `@supabase/ssr` browser + server clients, `src/middleware.ts` session refresh, same-origin validated redirects (`safe-redirect.ts`) — *S3a; route protection enforcement is S4*
- [x] Approval gate: `profiles.is_approved`, pending-state dashboard, server-side session gate on the gated route group (content RPCs inherit the pattern in S5/S6) — *S4*
- [x] `/admin/approvals` — HQ approval queue (server-checked `admins` table) — *S4*  **[MVP-critical]**
- [x] **Session-aware Apply CTA** — the public header's green Apply button becomes **My Dashboard** (→ `/dashboard`) once signed in (any state); logged-out keeps Apply. Same-origin session probe, public pages stay static, CSP unchanged — *S6 Step 3.5 (owner-requested UX)*
- [x] **Apply → dashboard redirect** — a successful application signs the partner in (instant session, email confirmation OFF) and lands them on the `/dashboard` pending "under review" state, with no separate sign-in — *S6 Step 3.5 (owner-requested UX)*

### Partner platform (gated)
- [x] Gated shell: persistent left sidebar + pending/locked state — *S4 (S6 destinations inert until built)*
- [x] `/dashboard` Welcome as a Partner (orientation by stage; "current stage" snapshot — Design & Build % hidden until Build starts) — *S6 6a*
- [x] `/plan` Plan & Prepare (reference) · `/operate` Operate & Program / Managing & Operating (one page, two entry points) — *S6 6b*
- [x] `/build` Design & Build — launch checklist: 200+ items, **saved per-user progress** (the only per-user interactivity) — *S6 6c (grouped by the 11 focus areas; milestone gates retired site-wide, D-S8-c)*
- [x] `/elements/[slug]` — tabbed canonical element page component × 33 (A1–K3), MDX bodies — *S6 6d (DB markdown, sanitized server-side)*
- [x] `/resources` + `/resources/[category]` — guides, 297 templates, tools; private Storage bucket + server-issued signed URLs — *S6 6e (migration 0017)*
- [x] `/academy` + `/academy/[slug]` — optional video reference library (no progress, no quizzes, no certificate) — *S6 6f: `/academy` library shipped; `/academy/[slug]` not built at MVP (each topic's Video tab + card link covers it, §3)*
- [x] `/live` partner tools — publish a YouTube link + event metadata from the gated `/programming` tool → populates the public listing — *S9 9f (migrations 0020/0021; owner-scoped, approval-gated)*
- [x] `/tools` House Applications — coming-soon placeholder — *S6 6g*
- [x] `/account`, `/support` — *S6 6g (migrations 0018/0019; `/account` Delete hidden D-S6-c; `/support` email deferred — requests stored)*

### Platform plumbing
- [x] Database: `profiles`, `applications`, `admins`, `elements`, `checklist_items`, `checklist_progress`, `programming_sessions`, `resources`, `academy_modules` — RLS default-deny, hardened `SECURITY DEFINER` RPCs, anon-safe public projections — *S2/S5; S6 adds the download/account/support writes (migrations 0017–0019). Test ✓; prod apply of 0016–0019 pending owner*
- [x] Email: Resend, 4 flows (contact · support · application-received pair · approve/decline) — **E1 ✅ MERGED (PR #55, 2026-07-09; Codex post-merge = GO)** and the owner executed the switch-on the same day: `palestine-house.com` **verified** in Resend (GoDaddy DNS) + the three `RESEND_*` vars in Vercel Production + Preview. **Remaining: redeploy Production + the `docs/EMAIL-SETUP-CHECKLIST.md` Part 4 live test matrix** — *wired as no-op placeholders in **S12**; the application-received pair added in E1. Mailchimp (apply tagging) **dormant by owner decision** (E1) — code stays no-op, no keys planned*
- [ ] Hardening: Upstash rate limiting, Turnstile, security headers + CSP (embed origin only), fail-closed production forms — *moved to the Post-MVP backlog (S14 retired 2026-07-02); **required before scale***
- [ ] CI (typecheck, lint, build, gitleaks) + branch protection + Vercel Preview/Production

### Post-MVP backlog (do not build during MVP)
`/search` (V1) · `/admin/partner-interest` (later) · functional House Applications tools · additional languages · RSVP for in-person events (MVP is listing-only, pending confirmation). *(`/admin/content` graduated from this backlog to **S11 — Admin content management**, 2026-06-26.)*

**Absorbed from the retired S13/S14 sprints (2026-07-02, owner direction — do not lose):**
- **Hardening (ex-S14 · required before scale):** Upstash rate-limiting + Turnstile on the public writes (`/apply`, contact, lead magnet) · fail-closed verification in real Production · CSP/security headers re-verified live · secret-path threat-model pass · full `SECURITY-CHECKLIST.md` §1–§15 pass. Brief: `docs/sprint-prompts/s14-final-hardening-relaunch.md`.
- **Full-site test-and-fix pass (ex-S13):** multi-hat end-to-end walk (copywriting → UX → feature — every page, CTA, link, a11y, security) → fix everything found → re-verify to 0 known bugs. Brief: `docs/sprint-prompts/s13-full-testing-fix-all.md`.
- **Domain/SEO relaunch verification (ex-S14):** after the owner's domain switch-on (`PROJECT-STATUS.md` §4), re-verify canonical/OG/sitemap/robots/JSON-LD + an auth email link on `https://www.palestine-house.com`.

---

## B. Stages & sprints

### Stage 0 — Foundational build (local · public shell only · no Supabase)

Goal: a complete, polished, working public website running locally. All forms render but **no-op cleanly** (no env vars yet). Live Programming renders its designed empty/fallback states.

| Sprint | Scope | Notes |
|---|---|---|
| **0.1 Foundation** | Scaffold (Next.js 15, TS strict, pnpm, Tailwind v4, shadcn/ui, Framer Motion); copy the 10 core docs + `/docs` content layer in; port `/docs/page-designs/design-system/tokens/` + `shared/pages.css` primitives into `globals.css`; `next/font` Spectral + Inter; motion primitives; security headers in `next.config.ts`; locked chrome (header + Tooltip nav + footer + mobile Sheet); 404/error pages | Scaffold + repo + CI + Vercel Preview pre-completed (pre-sprints 0a–0d, PRs #1–#2); design system is in-repo |
| **0.2 Home + Model** | `/` and `/model` from `/docs/page-designs/` + `/docs/page-copy/`, with final artwork from `/docs/page-designs/assets/art/` | |
| **0.3 New marketing pages** | `/experience` (live strip in empty/fallback state), `/bring-ph` (triptych + milestone timeline), `/our-support` | Densest pages — pace per `DESIGN.md` §12 |
| **0.4 Live + supporting pages** | `/live` public listing + watch view (static empty states), `/focus-areas`, `/about`, `/contact` (form UI, no-op) | |
| **0.5 Apply + auth UI + legal + SEO** | `/apply` form UI (no-op submit with honest "not yet live" handling), `/login` `/forgot-password` `/update-password` UI shells, `/privacy` `/terms`, metadata + sitemap + robots + OG | |

**Stage 0 exit gate:** typecheck/lint/build green · every public page matches its mockup at 320px → desktop · copy verbatim · proof numbers correct (11 · 33 · 200+ · 297 · 120-day launch — updated from 10 · 30 · 267 with Focus Area 11, FA11 2026-07-18) · reduced-motion respected · no console errors · headers/footer identical everywhere.

### Stage 1 — Connect & launch the barebones site

| Step | Scope |
|---|---|
| **1.1 GitHub** | Push repo; branch protection on `main`; CI (install, typecheck, lint, build, gitleaks) as required check; secret scanning + Dependabot on — *repo + base CI already live since pre-sprint 0c; remaining: gitleaks step, branch protection, secret scanning, Dependabot* |
| **1.2 Vercel** | Import (`nextjs` preset); env vars per environment (`SUPABASE-VERCEL-SETUP.md`); Production deploy of the public site (Vercel domain per resolved decision D3; custom domain later) — *project already connected with Preview verified (PR #2); remaining: env vars + first Production deploy* |
| **1.3 Supabase projects** | Create **production + non-production** projects (no schema yet); Auth → URL Configuration (Site URL, local + Preview wildcard + Production redirects); record refs in `SUPABASE-VERCEL-SETUP.md` + `PROJECT-STATUS.md` |

**Stage 1 exit gate:** live public site verified desktop + mobile · CI required + branch protection active · Preview deployments working · env matrix recorded.

> From here on: **every sprint = branch → PR → CI → Preview → merge**, status updated in the same PR.

### Stage 2 — Sprints (run in order)

| Sprint | Phases | Scope & exit gate | Depends on |
|---|---|---|---|
| **S1 — Design consistency pass** | 1a tokens/type audit · 1b motion + reduced-motion · 1c responsive 320px+ audit · 1d accessibility AA (contrast, focus, skip link, keyboard tooltip) | Whole public shell consistent with `DESIGN.md`; AA verified; zero visual drift between pages | Stage 1 |
| ✅ **S2 — Database phase 1: identity & approval** *(done 2026-06-15, merge pending)* | 2a `profiles` (+`is_approved`), `applications`, `admins` + RLS + RPCs · 2b apply to non-prod, test anon/authed, then prod | Versioned up + `.down.sql` + policies in PR; default-deny verified | Stage 1 |
| ✅ **S3 — Auth complete** *(merged 2026-06-16, PR #19)* | 3a Supabase clients + `src/middleware.ts` + login/logout · 3b forgot/update password + `/auth/confirm` (same-origin redirects; Preview emails → Preview) · 3c **Apply live** = sign-up + pending account + application record (zod; `full_name` via the `0008` trigger; + owner-approved Password field, §4) | All auth flows pass on local + Preview; `SUPABASE-VERCEL-SETUP.md` checklists green | S2 |
| ✅ **S4 — Approval gate + admin** *(built 2026-06-17, PR open; Preview + merge pending)* | 4a gated shell + sidebar + `/dashboard` pending state · 4b `/admin/approvals` queue (server-checked `admins`) · 4c server-side session gate on the gated route group + post-login → `/dashboard` (**approval-notification email deferred to S10** — Resend domain unverified until then; **"Add note" → backlog**) | 🔴 `SECURITY-CHECKLIST.md` §15 self-reviewed; pending user sees only pending state; approval unlocks without re-login; non-admin → 404 on `/admin/*` | S3 |
| ✅ **S5 — Database phase 2: content schema** *(merged 2026-06-18, PR #23)* | 5a `elements` + content ingestion (30 topics from `/docs/page-copy/06-elements`) · 5b `checklist_items` + `checklist_progress` (200+ items) · 5c `programming_sessions` (anon-safe public read) · 5d `resources` metadata + private Storage bucket + 267 templates upload · 5e `academy_modules` | Each phase: non-prod first, RLS verified anon vs pending vs approved | S4 |
| **S6 — Private platform pages** | 6a `/dashboard` full (current-stage snapshot rule) · 6b `/plan` + `/operate` · 6c `/build` checklist tracker (saved progress) · 6d element page component + 30 instances · 6e `/resources` hub + signed-URL downloads · 6f `/academy` · 6g `/tools` placeholder + `/account` + `/support` | Private-page mockups already in `/docs/page-designs/member-workspace/` (admin in `/docs/page-designs/admin/`) | S5 |
| ✅ 🚀 **S7 — Final review & launch** *(LAUNCHED 2026-06-19, PR #29 — production live + public-layer verified; Codex review = approve, zero blocking)* | 7a full-site QA: cross-browser/device, 0 known bugs, consistency sweep · 7b content verification (copy verbatim, numbers, booklet filename mapping) · 7c SEO/structured data + performance (CWV) · 7d go-live on the **Vercel domain** + post-deploy checks (custom domain post-launch, §4) | Every **launch-blocking** checklist green; `PROJECT-STATUS.md` marked **Launched**. ⚠️ Launches **before** the post-launch sprints — public writes go live without Upstash rate-limit/Turnstile (accepted owner decision, §5/§7); the HQ approval gate keeps abuse out of gated content | **S6** |
### Stage 3 — Post-launch (run in order)

> Sequenced after go-live. **S8–S12 (+ the pre-S13 UI-polish/domain pass) are all merged.** **Re-directed 2026-07-02 per owner direction:** the un-started S13/S14 rows are retired to the §A backlog — the post-launch focus shifts to the **public design refresh v3** (warm, cultural, photography-led premium system; DR-series sprints, added ad hoc by the owner as the site is already live). One sprint at a time, same branch → PR → Preview → merge loop.

| Sprint | Phases | Scope & exit gate | Depends on |
|---|---|---|---|
| **S8 — Workspace Visual Polish** *(post-launch · ✅ merged PR #32, 2026-06-24)* | 8a shared shell + premium kit (reveals · tatreez dividers · paper washes · card-lift · empty/loading states) · 8b dashboard (3 states) · 8c plan + operate · 8d build tracker · 8e element page · 8f resources + resources/[category] + academy · 8g account + support + tools · **8i topic-page redesign + inline template downloads** · **8j workspace logo → public home** (public header trialled, then removed per owner preference) · **8k retire the milestone gates concept site-wide (D-S8-c)** · 8h exit gate + review | Lift all ~12 gated pages to the **public-shell premium bar** using already-approved patterns + design-system tokens (no new design language, no new mockups); behaviour untouched. Grew on live owner feedback (topic-page redesign, workspace logo → home, gate removal — D-S8-b / D-S8-c). Exit: every workspace route reads premium + digestible at 320px + desktop + reduced-motion (owner Preview sign-off); typecheck/lint/build green; zero behaviour changes; independent Codex review = approve (one finding fixed). Record: `docs/sprint-prompts/s8-workspace-visual-polish.md`. | S6 |
| **S9 — Live Programming** *(post-launch · ✅ merged PR #34, 2026-06-25)* | 9a `/live` on real data + watch view (YouTube embed; CSP extended for the YouTube origin only — D1) · 9b Experience live-strip wired to the same feed/component · 9c partner publishing = **publish a YouTube link + event metadata from the gated `/programming` tool** (owner-scoped writes under RLS) | Simplest powerful path — no streaming infra. The partner-publish UI (the one prior design gap, §3) shipped as `/programming`. Exit ✅ **met:** `/live` + Experience on real data; publish flow owner-scoped (migrations `0020`+`0021`, applied+verified test+prod); empty states still graceful; **D2 (in-person RSVP → listing-only) RESOLVED**. Per-step + 4-lens exit-gate adversarial reviews clean. Record: `docs/sprint-prompts/s9-live-programming.md`. | S6, S8 |
| **S10 — Private-pages clarity & polish** *(post-launch · ✅ merged PR #37, 2026-06-26)* | 10-1 simplify the workspace menu (flatten the group headers; drop the duplicate **Program** + coming-soon **House Applications**; Library→**Resources** › Videos + Tools & Templates) · 10-2 operate-vs-program content re-grouping (**proposal-first**, all 30 topics intact) · 10-3 crystal-clear beginner intro copy on every gated page · 10-4 Build CTAs reordered (Read the full guide · Watch video · Mark in progress · Add notes · Mark Complete) · 10-5 code-level sample videos + premium **white** cards · 10-6 mobile fixes (esp. the element/topic page) · 10-7 exit gate | Make the gated workspace simple, premium, idiot-proof — **UI / copy / curation only**; intentionally revises previously-locked workspace menu/curation/intro-copy with owner authorization (**D-S10-b**). **No DB / auth / RLS / approval-gate change; no public-page change.** Exit: every private page reads clear + premium + balanced at 320px + desktop + reduced-motion (owner Preview sign-off); all 30 topics intact; typecheck/lint/build green. Brief: `docs/sprint-prompts/s10-private-pages-clarity.md`. | S6, S8 |
| **S11 — Admin content management** *(post-launch · ✅ merged PR #39, 2026-06-27 — migrations 0023/0024 applied + verified TEST + PROD; 4-lens exit review PASS + Codex review 1-blocking-fixed)* | 11a admin-panel CRUD over the private-page content (the 30 element/topic bodies, academy videos, resources/templates) behind the existing `is_admin()` gate · 11b add / update / delete with RLS-safe writes · *(the old-S10 admin-management UI — add/remove admins — folds in here, to confirm at scope)* | The client fully self-manages all private-page content + resources without scripts — builds the graduated backlog `/admin/content`. Exit: an admin can add/update/delete content end-to-end behind the admin gate; writes RLS-safe; no content loss; versioned up + `.down.sql` + RLS in the PR (TEST-first, `WORKFLOW.md` §14). Brief: `docs/sprint-prompts/s11-admin-content-management.md`. | S5, S10 |
| **S12 — Email-automation placeholders + settings** *(post-launch · ✅ merged PR #41, 2026-06-27 — Codex review = approve; no migration; switch-on by env vars + Resend domain, see `docs/EMAIL-SETUP-CHECKLIST.md`)* | 12a Mailchimp: booklet capture (`lead-booklet-a/b`) + newsletter + apply tagging · 12b Resend: contact + support + the **approval/decline email** (deferred from S4) + **declined→contact** path · 12c all settings + env wiring, no-op until keys present; document SPF/DKIM/DMARC verification | Everything wired with honest no-op placeholders so the owner goes live by **adding keys/domain alone** (Mailchimp API key + audience id; Resend env + verified sending domain). Absorbs the old-S10 email + approval/decline email. Exit: each integration no-ops cleanly without env vars; keys server-only; switch-on is env-vars-only. Brief: `docs/sprint-prompts/s12-email-automation-placeholders.md`. | S3 |
| **DR1 — Public design refresh v3: foundations + Home** *(post-launch · ✅ merged PR #45, 2026-07-03 — post-merge `/close` = GO; Codex = APPROVE zero blocking)* | DR1-0 docs prune + sprint open · DR1-1 asset hygiene (photos/logo optimized + renamed; example mockups out of `public/`) · DR1-2 v3 token foundations (additive tokens, public-scoped remap) · DR1-3 chrome (warm solid header + transparent-over-photo on Home · new BrandLogo · charcoal footer) · DR1-4 Home hero + culture split · DR1-5 "Inside a Palestine House" photo strip + retained sections · DR1-6 brand surfaces (favicon/apple-icon/OG/manifest) · DR1-7 exit gate + `DESIGN.md` v3 rewrite · DR1-8 owner final polish (mega-menus removed · booklet lead magnet removed, hero CTA-2 = "Explore the model" · Inside-a-House auto-marquee · platform photo band · hero rebalance + mobile pass) · DR1-9 v3 photo heroes on /model /experience /bring-ph /our-support /apply (shared `PageHero`, copy verbatim, overlay header) + Home/mobile refinements (copy nudge · features lower · 35s marquee · mobile hero = copy+CTAs only · mobile strip stacks vertically) | Replace the illustration-on-paper public design with the owner's **warm, cultural, photography-led premium system (v3)** — foundations (tokens · type · imagery treatment · chrome) + the redesigned **Home page** only; frontend/design only, **zero feature/DB/auth/gate/CSP/routing change**; the gated workspace stays visually untouched (scoped tokens + diff path-guard); every new/changed string is owner-gated before it ships. Exit: v3 foundations live across the public shell; Home matches the owner's reference examples; AA contrast verified on rendered pages; workspace unaffected; typecheck/lint/build green; `DESIGN.md` rewritten to v3 + overrides recorded in `PROJECT-STATUS.md` §4. Brief: `docs/sprint-prompts/dr1-public-design-refresh-v3-home.md`. | S12 |
| **DR2 — Home v3 sections + chrome: stages · proof band · footer** *(post-launch · ✅ merged PR #48, 2026-07-06 — post-merge `/close` = GO; Codex = APPROVE zero blocking)* | DR2-0 docs-only sprint open (this row + `PROJECT-STATUS.md` flip; owner drops the master images into `docs/source-assets/design-refs/v3/` — `photos/` 3 stage photos · `art/` line-art + 4 olive-branch PNGs · `refs/` stages-section / proof-band / footer mockups + palette poster) · DR2-1 asset pipeline (extend + run `scripts/optimize-photos.ts`: stage photos → jpg ≤2000px <500 KB; olive branches → alpha-preserving PNG/WebP; line-art keyed or matched-background; register ids) · DR2-2 additive tokens (dark olive-green band + stage-plate green + warm-sand numeral, sampled from the mockups) + a `.ph-section-olive` band variant · DR2-3 Home "One path, three stages" rebuild (photo cards + dark-green arch-notch number plates + the line-art beside the grid; copy-table gate for the sub-line + card descriptions) · DR2-4 proof band restyle (`.ph-section-olive` + olive-branch ornaments + warm-sand numerals; layout per mockup; label-casing copy gate) · DR2-5 footer refresh (Arabic tagline line `lang="ar" dir="rtl"` · column labels per mockup · olive sprig; the Apply band + single-conversion rule survive; copy gate) · DR2-6 dedicated mobile-responsiveness pass (owner-added 2026-07-06: 320px→768px sweep of the three new surfaces + chrome on the key routes; fix everything found) · DR2-7 exit gate (adversarial full-diff review · 320px/desktop + AA on the green surfaces · path-guard · `DESIGN.md` §3/§5/§12 + trackers + record) | Bring the three owner-mocked surfaces to final v3 from the owner's new DR2 mockups: the Home stages section, the proof-numbers band, and the global footer. **Additive tokens only** (owner decision 2026-07-06 — existing v3 values untouched; the palette poster is reference, not a retune); frontend/design only, **zero feature/DB/auth/gate/CSP/routing change**; the gated workspace stays untouched (path-guard); proof numbers verbatim (10 · 30 · 200+ · 267 · 120-day launch); every new/changed string ships only via owner-approved copy tables at the sub-step gates. The footer change is the owner updating the locked chrome itself — recorded in `DESIGN.md` §5. **Remaining public page bodies → DR3.** Exit: the three surfaces match the mockups; AA verified; typecheck/lint/build green; workspace unaffected; trackers + `DESIGN.md` updated; record `docs/sprint-prompts/dr2-stages-proof-footer.md`. | DR1 |
| **DR2.1 — Brand lockup rollout + Home band polish + bring-ph stage cards** *(post-launch · ✅ merged PR #50, 2026-07-06 — post-merge `/close` = GO; Codex = APPROVE zero blocking)* | DR2.1-0 docs-only sprint open · DR2.1-1 logo asset pipeline (the owner's new lockup master — copper arch + all-caps serif wordmark + "OUR CULTURE EMBASSY" tagline — relocated out of `public/` into gitignored `docs/source-assets/design-refs/v3/`; cropped/optimized `public/assets/logo/ph-logo-lockup.png` via `scripts/optimize-photos.ts`) · DR2.1-2 BrandLogo v2 (full lockup PNG on light chrome; adaptive real-text lockup in cream on the dark footer + the `[data-overlay]` photo-header state — owner decision 2026-07-06; copy gate for the tagline string) · DR2.1-3 proof-band polish (the caption between the two sprigs sized to ~2 balanced desktop lines; the bottom-right olive branch adjusted per the owner's reference re-attached at this gate) · DR2.1-4 bring-ph stage cards (Home's `v3-stage-card` grid extracted to a shared cards-only component replacing the `.bring-triptych` — no side art; copy gate for card-text reuse) · DR2.1-5 dedicated responsive sweep 320→1440 over the touched surfaces + public-route spot-check · DR2.1-6 exit gate (adversarial full-diff review · AA on the text lockup over photos + footer · path-guard · `DESIGN.md` §5 + trackers + record) | Owner-requested ad-hoc polish pass on DR1/DR2 surfaces (2026-07-06): the new logo used site-wide in the public chrome, the proof band refined, the Home stage cards reused on `/bring-ph`, responsiveness verified. Frontend/design only — **zero feature/DB/auth/gate/CSP/routing change**; gated workspace untouched (path-guard); proof numbers verbatim (10 · 30 · 200+ · 267 · 120-day); new/changed strings only via owner-gated copy tables. The chrome-logo change is the owner updating the locked chrome itself → `DESIGN.md` §5. **DR3 (public page bodies) stays next.** Exit: the lockup correct in both header states + footer; caption = 2 desktop lines; bring-ph cards match Home's; 320px+ clean; typecheck/lint/build green; record `docs/sprint-prompts/dr2-1-logo-band-cards.md`. | DR2 |
| **DR3 — remaining public page bodies to v3** *(post-launch · page-by-page series; DR3.1 = The Model)* | Each page built to the owner's own section mockups (dropped into `docs/source-assets/design-refs/v3/`), **copy adopted from those mockups** (owner decision), owner-gated section-by-section with a photo-pipeline step. **DR3.1 — The Model (`/model`) ✅ MERGED (PR #52, 2026-07-08, `claude/sprint-dr3-1-model-v3`):** DR3.1-0 asset pipeline (12 embassy photos + green tatreez strip + Aswātna cream mark + adaptively-keyed gold branch + owner still-life/doorway masters) · DR3.1-1 §1 "cultural embassy" 3-column collage (café/venue/community) + hero left unchanged · DR3.1-2 §2 "Three layers" arches (fills sampled) · DR3.1-3 §3 "The shared promise" dark moss band · DR3.1-4 §4 never-negotiable cream cards + what-is/✗ + "Culture leads." · DR3.1-5 §5 "Could your city…" band — **promoted into the footer** (owner decision: one premium closing CTA on every public page) · DR3.1-6 exit gate. **DR3.1.1 — `/model` embassy + footer-CTA polish ✅ MERGED (PR #53, 2026-07-08, `claude/sprint-dr3-1-1-model-cta-polish`):** owner follow-up (2026-07-08) from marked-up screenshots — vertical green side-strip removed → **horizontal tatreez band** at the footer-CTA base only; the collage **auto-rotation removed** (**D-DR3.1 retired** → static); subtle **cream cards** + copper **tatreez rosette**; **photos filling their frames edge-to-edge (`object-fit: cover`)** — collage + the footer invite photo (an original-ratio `contain` pass was tried mid-flight then reverted at owner request); enlarged collage + collapsed the "Three layers" gap; footer CTA taller/wider. Frontend/design + footer chrome only; path-guard clean; record `docs/sprint-prompts/dr3-1-1-model-cta-polish.md`. **DR3.2 — Bring a House (`/bring-ph`) ✅ BUILD COMPLETE (pre-merge, 2026-07-14, `claude/bring-ph-redesign`; owner-initiated ad-hoc, interactive plan-mode + 5 review rounds):** §2/3/5/6/7 rebuilt to the owner's mockups (hero + "What it takes" unchanged) — §2 big statement + a **cropped gold-arch illustration** on a warm-cream band · §3 bordered split card · §5 star-medallion timeline · §6 three medallion cards on **light cream** (was dark) · §7 CTA + arch photo + **PALESTINE HOUSE wordmark lockup** + olives + arch sparkle; new `src/components/shared/ornament.tsx` (**StarLogo** = the owner's star-logo photo keyed to a transparent copper PNG · single SVG star for the §5 medallions · sparkle · olive-branch reusing `/model`'s); §4↔§5 tatreez divider removed; the two **Experience** section closing lines aligned to one centered display type. Divergences (terracotta Apply · forest headings `#2E351A` · light §6) scoped to `.bring-page`; copy verbatim; header/footer chrome untouched; path-guard clean; independent **Codex review = 1 Medium (secondary-CTA cascade) fixed + verified**. Record `docs/sprint-prompts/dr3-2-bring-ph.md`. **DR3.3 — Our Support (`/our-support`) ✅ BUILD COMPLETE (pre-merge, 2026-07-14, `claude/sprint-dr3-3-our-support-v3`; owner-gated DR3.3-1…-8 + a 6-item owner feedback round):** split hero (cream copy + the 10/30/267/120 proof row · a rounded full-height photo panel on the right; `/our-support` removed from `OVERLAY_ROUTES`) · premium white toolkit cards (sage icon-chip + serif title, owner's sample) · The Standard (matted **arch** photos + copper khatam stars + centred PALESTINE HOUSE lockup) · Aswātna full **muted-red** band `#9b361e` + the **gold** Aṣwātna seal + three captioned photos (Before/At/After) · The Backing horizontal **01·02·03 numbered timeline** (moss/terracotta/gold nodes on a red line, vertical per-step under 620px) · What-you're-responsible-for **forest-green** band `#323621` + four topics over a doorway-photo/checklists split (copper khatam-star divider). 8 new photos + a gold Aṣwātna seal via `optimize-photos.ts`; node/band colours **sampled** from the mockups; AA verified (cream/red ≈6.6:1, cream/green ≈11:1); no horizontal overflow 320→1440; dead `.sup-*` pruned. **Owner feedback round (2026-07-14):** v3 page-hero copy **left-aligned** site-wide (`.v3-page-hero-inner` fills width → Model/Experience/Bring/Apply), toolkit cards to the owner's card sample, Standard top divider removed + arches enlarged + lockup/tagline centred, full mobile designer pass. Path-guard clean (only `our-support/page.tsx`, `pages.css`, `v3.css`, `photo.tsx`, `optimize-photos.ts`, the `site-header` OVERLAY_ROUTES line, `public/assets/*`). Record `docs/sprint-prompts/dr3-3-our-support.md`. **DR3.4 — `/focus-areas` + `/apply` hero redesign ✅ BUILD COMPLETE (pre-merge, 2026-07-15, `claude/focus-apply-hero-redesign`; owner-directed ad-hoc, plan-mode):** both page heroes rebuilt to the owner mockups as cream split heroes (modeled on `.support-hero`) — **`/apply`** cream copy + a short copper rule + the riad House-entrance photo **full-bleed** to the right edge, removed from `OVERLAY_ROUTES` → solid cream header (copy verbatim); **`/focus-areas`** `THE FULL MAP` eyebrow + copper hairline · 2-line heading · owner-approved **shorter intro** · the dark stats card (10·30·200+·267·120) **folded into the hero** · green-door courtyard photo in a rounded panel; standalone `.fa-stats` band absorbed + dead `.fa-stats`/old `.fa-hero-photo` CSS pruned; shared `.art-hero` (`/about`) untouched; 2 new photos via `optimize-photos.ts`. Path-guard clean; **Codex review = APPROVE, zero blocking** (one Low: orphaned `ph-photo-apply`). Record `docs/sprint-prompts/dr3-4-focus-apply-heroes.md`. Remaining DR3 pages next: `/about` `/contact` + auth + legal (each its own DR3.x; the `/experience` body + the whole `/live` surface shipped in **LH1**, 2026-07-10 — `/live` is a gated workspace hub now, no longer a public body). Frontend/design + approved-copy only — **the footer-chrome CTA is an owner-authorized, in-scope exception** this sprint; otherwise zero feature/DB/auth/gate/CSP/routing change, gated workspace untouched (path-guard); proof numbers verbatim. Exit (per page): matches the mockups; 320px→desktop + AA; path-guard; `DESIGN.md` §8/§12 + trackers + record `docs/sprint-prompts/dr3-<page>.md`. | DR2.1 |
| **COPY OVERHAUL — owner "TEXT EDITS" across the public site** *(post-launch · owner-directed ad-hoc · 🔵 BUILD COMPLETE pre-merge 2026-07-17 on `claude/public-copy-overhaul`, local/not pushed)* | The owner dropped seven Word docs (one per public page) + six images into `public/assets/TEXT EDITS/`. Applies that revised copy to **all seven public pages** (`/` `/model` `/experience` `/bring-ph` `/our-support` `/apply` `/focus-areas`) **on top of the existing v3 layouts**, plus the shared **footer CTA** → "Ready to bring Palestine House to your city?" (owner-authorised locked-chrome edit). Owner decisions: apply ALL the copy + keep layouts · update the footer CTA site-wide · rebuild the 120-day timeline to the reference. **Three design pieces** built to the owner's reference images: `/bring-ph` §5 → four arched DAY cards (30·60·108·**120 new**) + ribbon ("The goal is not simply to open on time. It is to open well."), retiring `.bring-mile*` · `/model` §6 → "A cultural institution built to last." + is/is-not + "Culture leads. / The structure helps it last." with the **new coffee still-life** · `/experience` §6 → **new** "More than somewhere to visit." closing section with the **new gathering photo**. **Two owner removals** (Home's "It isn't charity…" line; the whole Our Support "You bring / We make sure" split + consts + CSS) and **two renames** ("Three rules, no exceptions."/"Three things, never negotiable" → **"Three commitments every House shares."**; "The agreement" → **"The Partnership"**). `StageCards` gained a `stages` prop (default = Home copy) since the docs give Home and `/bring-ph` different stage copy. 2 new photos via `optimize-photos.ts`; masters gitignored; **`public/assets/TEXT EDITS/` removed from the served tree, never committed**. **Copy verified in three independent rounds by agents extracting the owner's original `.docx`** (not the engineer's transcription) — **11 real defects found + fixed**, incl. the Model doc's closing two sentences dropped entirely and both proof rows losing the 120 unit. Guards PASS (proof numerals 10·30·200+·267·120 unchanged · locked "Every application is reviewed by HQ." verbatim · path-guard clean · no dangling refs); typecheck/lint/build green (45/45 static). Frontend copy + 2 photos only — no DB/auth/gate/CSP/routing/env change. Record `docs/sprint-prompts/copy-overhaul-text-edits.md`. | DR3.4 |
| **LH1 — Members-only Live hub + Experience body v3 + Model collage sizing** *(post-launch · 🔵 BUILD COMPLETE pre-merge 2026-07-10 on `codex/sprint-lh1-live-hub`; **0025 applied + verified on TEST AND PROD** — owner restored the paused TEST project + applied PROD early, 2026-07-10; one-off role swap: built by Claude from the committed Codex brief, **Codex = PR reviewer**)* | Task 0 branch + brief commit (Codex) · Task 1 migration **0025_member_live_hub** (`member_programming_sessions()` w/ derived `is_mine`, `where is_approved()`, authenticated-only; anon `public_programming_sessions()` dropped; up + down + TEST/PROD verify scripts) · Task 2 data layer (`is_mine`; `mine.ts` deleted; revalidate `/live` only) · Task 3 route move `src/app/live/*` → `(workspace)/live/*` + `/programming` → `redirect("/live")` + `"/live"` in `GATED_PREFIXES` · Task 4 nav (sidebar flip + fifth dashboard card "Live") · Task 5 unified hub (Live now · chips · Upcoming · Recordings · `#publish` manage) + gated watch view (approval check **before** `notFound()`) + "Yours" tag · Task 6 public removals (Experience strip + hero CTA · footer link **owner-authorized** · PUBLIC_ROUTES/sitemap · stale strings) · Task 7 photo pipeline (3 masters → `ph-photo-exp-*`) · Task 8 Experience §2 day/night 3:2 photos · Task 9 §5 Permanence `.v3-split` + bonsai · Task 10 `/model` collage sizes (1/1.05 hero · 5/7 thumbs) · Task 11 checks + trackers | Owner decisions (2026-07-10): live/recorded sessions move ENTIRELY into the member workspace — approved partners (`is_approved`) publish AND watch; the public site exposes **zero session data**; one idiot-proof hub at `/live` (workspace-gated + noindex). DB change ships up + down + verify scripts in the PR, proven on TEST (role-simulated verification, down + re-up reversibility, advisors clean); the owner applied PROD by hand **early** (2026-07-10) and the engine verified it read-only. CSP/env/deps untouched; existing copy verbatim; new strings from the owner-approved table in the brief `docs/sprint-prompts/lh1-live-hub-experience-codex-prompt.md`. ⚠️ Prod DB is ahead of the deploy: 0025 is live on PROD while the deployed code still calls the deleted anon RPC — it fail-closes to the approved empty states on `/live` + the Experience strip until this merges; merge promptly. Exit: TEST apply + verify green · Preview gates A (hub role matrix) / B (`/experience`) / C (collage) · independent Codex PR review · typecheck/lint/build green (46 routes) · trackers + record `docs/sprint-prompts/lh1-live-hub-experience.md`. | E1 |
| **FA11 — Food focus area: "Café & Culinary Experience" (Focus Area K) + `/food` workspace section** *(post-launch · 🔵 BUILD COMPLETE pre-merge 2026-07-17 on `claude/sprint-fa11-food-focus-area`; owner-gated FA11-0…FA11-7, push-per-step, isolated worktree)* | FA11-0 worktree + branch + baseline · FA11-1 canonical pack folder (`docs/source-assets/resources/2. Focus Areas/11. Café & Culinary Experience/` + `_pack.md`; CP437 mojibake repaired; duplicates dropped) + the one-shot owner copy gate · FA11-2 migration **0026_focus_area_k** (3 CHECKs + `admin_upsert_element`/`admin_update_resource` regexes A–J → A–K; up + destructive-documented down + TEST/PROD verify scripts) · FA11-3 apply + verify on TEST via MCP (6 role sims, leave-no-trace) · FA11-4 additive **`--pack food`** ingest mode (K-only, idempotent, hard guards) + TEST ingest + verify (33/818/33/299 + 30 objects; role matrix 9/9; group-label parser fix) · FA11-5 app A–K widenings (4 zod regexes + `/resources/[category]` guard + `revalidatePath("/food")`) · FA11-6 the Food section (`/food` program-pattern page K1–K3 · sidebar Operate→**Food**→Program · 6th dashboard card · academy/admin captions · **`/food` in `GATED_PREFIXES`**) · FA11-7 exit gate (path-guard CLEAN · trackers · record) | Owner-added sprint (2026-07-17): land the authored Focus Area 11 content package in the existing content system (editable via `/admin/content`) with a dedicated workspace section between Operate and Program. DB TEST-first (WORKFLOW §14); PROD = owner post-merge runbook (0026 → verify A → prod `--pack food` ingest → verify B). **Public site untouched by owner decision — the 11 · 33 · 297 public refresh + the `/focus-areas` map addition ride a follow-up after `claude/public-copy-overhaul` merges (PROJECT-STATUS §5 D-FA11-a/b).** Heads-up: Build % denominator 728→818. Exit: TEST DB verified end-to-end · typecheck/lint/build green (47 routes) · path-guard clean of all public/chrome/config surfaces · record `docs/sprint-prompts/fa11-food-focus-area.md`. | S11, LH1 |

> **Retired from the plan (2026-07-02, owner direction):** the un-started **S13 (full testing + fix-all)** and **S14 (final hardening + relaunch)** rows were removed — the owner is changing direction post-launch (public design refresh first) and will add sprints ad hoc. Their scope is **preserved in §A → Post-MVP backlog** (the hardening items remain **required before scale**); the original briefs stay at `docs/sprint-prompts/s13-full-testing-fix-all.md` + `s14-final-hardening-relaunch.md`.

### Stage 4 — Private Platform Revamp (PP series · owner-directed, added 2026-08-10)

> The owner delivered a final HTML prototype (`PH - Palestine House Final Mockup.html`, kept in gitignored `docs/page-designs/`; the committed, canonical extraction is **`docs/workspace-spec.json`**) that replaces the **entire gated member workspace** — new IA, new design language (cream/char/olive/copper tokens, Inter+Spectral), new content presentation, desktop + mobile. **Public pages stay exactly as-is.** Run **PP1 → PP7 strictly in order**, one sprint = one branch = one PR = one Codex review. **Build first, tear down after cutover** (production is live — the old workspace keeps working until the new one is complete). **Card-model exception (owner, 2026-08-11 D-PP-c, revised 2026-08-12 D-PP-f):** the mockup's per-topic card row is superseded — the final model is **summary → one Simple guide card → Watch Video → templates grid** (decision ⑦ below, which supersedes the 3-card row in ⑤). The mockup's **templates grid is back in force**; the mockup remains the reference for everything else. The owner's copy of it at `docs/PH - Palestine House Final.html` (repo `docs/` root, gitignored since PP1.1) is kept until the PP series closes — do not remove it.
>
> **Owner decisions (2026-08-10, supersede earlier records where they conflict):** ① the Live hub `/live` is **dropped entirely** (UI + publish flow; `programming_sessions` data stays in the DB until the final contraction migration). ② **Saved checklist progress is dropped** — the checklist becomes read/download content; the mockup is followed exactly (supersedes "saved checklist progress is the one per-user interactivity"). ③ Videos = **YouTube links** added later via the CMS (CSP already allows youtube-nocookie). ④ Ask HQ = **email to HQ via Resend only** (no in-account ticketing). ⑤ **(2026-08-11, D-PP-c)** each focus area shows exactly **3 cards — Overview / Simple guide / Template**: the Practical-checklist and What-to-watch-out-for cards are removed; **one template per focus area** (a download-only card; the owner picks/uploads the single file via CMS v2 (PP6) — honest "coming soon" until then; the 297 already-uploaded coded files stay dormant as the picker's candidate list). ⑥ **(2026-08-11)** the 15 "More guides" extras are **dropped entirely** (no `platform_extras` table/RPC/seed); Watch Video buttons + `youtube_url` stay. ⑦ **(2026-08-12, D-PP-f — supersedes the card half of ⑤)** the final per-topic model is **summary → ONE Simple guide card (Read Now + Download Now) → Watch Video → a templates grid with MANY downloadable templates**. The **Overview card is removed** (the owner is folding overview material into the focus-area summaries themselves); **templates return to the mockup's multi-template grid** (the 297 already-uploaded coded files go live again, attached by `element_id` — no per-row flag needed); the checklist and watch-out cards stay removed and the extras stay dropped. `resources.doc_key` therefore narrows to **`('guide')`** — the existing partial-unique index now enforces "one downloadable guide file per topic". The topic **summary** is the topic's own `description` + `intro` (owner-edited via CMS v2 in PP6); **`elements.overview_md` is rendered nowhere and is scheduled for deletion in the PP7 contraction migration 0030** (owner, 2026-08-12).
>
> **Load-bearing verified facts:** the 33 new topics map **1:1** onto the 33 existing `elements` (new IA = 4 sections → 10 groups → 33 topics, a pure regrouping; explicit map checked into `scripts/extract-mockup-spec.ts` + `docs/workspace-spec.json`, verified against the live DB 2026-08-10) — the surfaced Read-Now body (`simple_guide_md`) and the **297 coded template files** carry over with **zero re-upload** — under D-PP-f the templates are a live surface again, reached by `element_id`; `overview_md`, `watch_out_for_md` and the 818 `checklist_items` stay in the DB but **dormant** (no consumer — `overview_md` is scheduled for deletion in 0030, the other two are an owner call at PP7). New model per topic (**D-PP-f**, 2026-08-12 — supersedes the D-PP-c 3-card row): **summary → one Simple guide card → Watch Video → templates grid**. The **summary** is the topic's own `description` + `intro` (the owner folds the overview material into them via CMS v2; there is no Overview card and `elements.overview_md` is rendered nowhere). **Simple guide** is one card carrying **Read Now** (DB `simple_guide_md`; reader page design is Claude's freedom — the ONE licensed deviation from the mockup) + **Download Now** (`resources.doc_key='guide'`, at most one per element via the existing partial-unique index; the 33 guide files are **not uploaded yet** → honest "coming soon" toasts until CMS upload, mirroring the mockup's video toast). **Templates** is the mockup's grid restored: **many downloadable templates per topic**, which are simply the `resources` rows carrying that `element_id` — the 297 coded files already in the DB and Storage, badged by `resources.code`, served through the existing signed-URL path. `elements.watch_out_for_md`, `elements.overview_md` and the 818 `checklist_items` have no consumer; `overview_md` is scheduled for deletion in 0030, the other two remain an owner call at PP7. `academy_modules` is retired (its `youtube_url` — all NULL — moves to `platform_topics`; `body_md` was a drifting duplicate of `simple_guide_md`). `/dashboard` must survive as a path (hardcoded in the public header, login/apply redirects, outbound email) — it becomes the new **About** landing keeping the pending/declined states. **DB timing: expand early, contract last** — additive migrations 0027 (IA tables + RPCs) + 0028 (seed), **as corrected by PP1.1**, go to PROD at the PP2 merge gate; the contraction (0030: drop `academy_modules`, `checklist_progress` + `set_checklist_progress`, `checklist_items` + `get_checklist`, `get_academy_modules`, live-hub RPCs, **and `elements.overview_md`** — owner-confirmed 2026-08-12 under D-PP-f; the owner decides then only whether `elements.watch_out_for_md` stays or goes. ~~**The 297 coded template rows and their Storage objects are NEVER dropped — they are the live templates grid.**~~ **⚠️ SUPERSEDED 2026-08-14 by D-PP-k — the 297 ARE dropped, by PP6c's `0030`, and the contraction is renumbered to `0031`. See the Stage 4 supersession block below.**) runs only after PP7 QA passes. Owner applies all PROD SQL by hand; Claude proves everything on TEST first (`SUPABASE-MCP-SAFETY.md`).
>
> ---
>
> ## ⚠️ STAGE 4 SUPERSESSION — the real content arrived (2026-08-14, D-PP-k … D-PP-o)
>
> **Everything above this block describes the platform seeded from the mockup's placeholder curriculum. The owner has now delivered the real, final content, and it is a different information architecture.** Map + evidence: **[`content-migration-map.md`](./content-migration-map.md)**. Decisions: `PROJECT-STATUS.md` §5.
>
> **The gap, verified read-only against PROD 2026-08-14 and adversarially re-checked (five refutation attempts, all failed):** live = 4 sections → **10 groups → 33 focus areas → 297 templates**, every guide body real (10,609–34,653 chars), every template a real named file. Delivered = 4 sections → **22 focus areas → 88 templates**, guides 1,960–4,019 chars in operator voice. **Overlap: 0 of 22 focus-area names · 2 of 88 template names, and both are different files (19,809 B live vs 186,726 B new) · file-size ranges completely disjoint (live 10.5–57.5 KB, new 183–188 KB) · zero repo files mapping 22→33 · zero prior decisions anticipating it.** The two occupy the same address space (`1.1`–`1.5`, `2.x`, `3.x`, `4.x`) — same slots, different occupants. **Owner, 2026-08-14: "this is the final version (the final focus areas and content that we have)."**
>
> **What this changes:** ⓐ **PP6 as written is retired** — its scope was "let the owner edit the existing 33"; the need is "let the owner build and rebuild focus areas", a superset. It **splits into PP6a (machinery, `0029`) → PP6b (pilot, no migration, owner gate) → PP6c (rollout + cutover + deletion, `0030`)**. ⓑ **PP7's contraction renumbers `0030` → `0031`.** 0027/0028 remain immutable (PROD ran them 2026-08-12); nothing above 0028 exists on disk yet, so the renumber is free. ⓒ **The "297 are never dropped" rule is reversed** (D-PP-k). ⓓ **A 34th focus area is physically impossible today** — `elements.slug` is constrained `^[a-k][1-3]$` (`0011:32`, widened `0026:23`) = 11 × 3 = exactly 33 slots, all full. Even a one-focus-area pilot needs `0029`. ⓔ **`elements.overview_md` is still dropped at PP7** — D-PP-m routes the new Overview text into `simple_guide_md`, not into `overview_md`.
>
> **The five new decisions (owner, 2026-08-14):** **D-PP-k** the 22 replace the 33; old topics/groups/elements/297 templates + Storage objects are **deleted** in `0030` — *with a mandatory cold backup first, because a `.down.sql` restores rows but never Storage objects*. **D-PP-l** files are served from our own private bucket via signed URLs, **not Google Drive** (Drive cannot see our approval gate — the only workable Drive setting is "anyone with the link", a permanent public address for the whole partner library, which breaks `SECURITY-CHECKLIST.md:142` and `:138`); templates stay `.docx` (66 of 88 carry real tick-boxes — a PDF of an empty budget grid is useless). **D-PP-m** each Overview's opening sentence becomes the card summary, the rest becomes the first section of the Simple Guide; **no Overview card returns**. **D-PP-n** one group per section; a single-group section renders as a flat list with no accordion header. **D-PP-o** the existing 33 photographs are reused, remapped onto the 22 by subject (map in `content-migration-map.md` §4; 11 spare as alternates).
>
> **Public pages remain untouched** throughout, as in PP1–PP5. **D-PP-a stops being parkable at PP7:** after cutover the public band "11 focus areas · 33 topics · 200+ checklist items · 297 templates" describes nothing that exists.

| Sprint | Scope | Exit gate | Depends on |
|---|---|---|---|
| **PP1 — Foundations: spec, schema, seed, assets** *(no UI change; TEST DB only · ✅ **MERGED PR #73, 2026-08-10** — 0027+0028 applied + fully verified on TEST (role sims 9/9, reversibility + idempotency proven, advisors clean); Codex = REQUEST CHANGES ×2 Medium, both fixed on-branch + re-verified byte-identical. Record: `docs/sprint-prompts/pp1-platform-foundations.md` · review: `docs/code-reviews/pp1-platform-foundations.md`. **✅ PROD APPLIED + VERIFIED 2026-08-12 (owner ran 0027 then 0028 by hand; read-only verification green — see the PP2 record). 0027/0028 are now IMMUTABLE: any further schema change is a new migration 0029+.** **The scope cell below records PP1 AS SHIPPED and is history, not current truth — it was corrected twice pre-PROD: by PP1.1 (D-PP-c, extras + `platform_extras` removed) and by PP2 (D-PP-f: no Overview card, `doc_key` narrowed to `('guide')`, templates back to a many-per-topic grid). Read decision ⑦ above for the model that actually shipped.**)* | `scripts/extract-mockup-spec.ts` parses the mockup's `#appData` → committed `docs/workspace-spec.json` + generated seed SQL + extracted base64-only assets (page heroes, footer photo, sprig, tatreez pattern) → `public/assets/workspace/`; hard-asserts 33/33 element↔topic map, 4 toolkit sections, 10 groups, 15 extras, 297 template codes, and that the per-kind card copy is constant across all topics. Migration **0027_platform_ia** (single transaction, additive): `platform_sections` (**5** — the About landing at `num` 0 plus the 4 toolkit pages, so all page-hero copy has one CMS-editable home) · `platform_groups` (10) · `platform_topics` (33; `element_id UNIQUE FK→elements` = the integrity spine; new slug/title/desc/intro/icon/image/`youtube_url`) · `platform_extras` (15; nullable `resource_id`) · `resources` + `code` (T01… backfilled from `storage_path`) + `doc_key` (overview/guide/checklist/watch; partial unique per element — future home of the 132 standard docx uploads); RLS default-deny; approval-gated RPCs `get_platform_sections/topics/extras` (0011 pattern); `get_resources()` drop+create widened in-transaction (old code unaffected). Migration **0028_platform_seed** (idempotent, `ON CONFLICT (element_id)`). the owner's 37 topic/section PNG masters optimized → `public/assets/workspace/{topics,sections}/<slug>.jpg` (mozjpeg, each <600 KB; masters moved to gitignored `docs/source-assets/design-refs/workspace/`), plus 5 page heroes + 5 misc ornaments extracted from the mockup's base64. | 0027+0028 applied + verified on TEST (anon/pending zero rows · approved **5**/10/33/15 · template-code backfill = 297); typecheck/lint/build green; no UI/behaviour change; up + down + verify scripts in the PR. | FA11 |
| **PP1.1 — Card-model correction: 3 cards, extras dropped** *(no UI change; corrects PP1's foundations pre-PROD per D-PP-c · ✅ **BUILT + VERIFIED ON TEST 2026-08-12** on `claude/pp1-1-card-model` — corrected 0027+0028 re-applied and re-verified on TEST (extras negatives pass · `doc_key` CHECK = overview/guide/template · role sims 7/7 · seed digest byte-matches `docs/workspace-spec.json` · advisors clean); typecheck/lint/build green, full diff reviewed. ✅ **MERGED PR #75, 2026-08-12** (`docs/pp1-close` = PR #74 merged first, 2026-08-10). PROD apply happened at the PP2 merge gate, as planned. **⚠️ Partly superseded the next day by D-PP-f (2026-08-12): the Overview card is removed and templates return to a multi-template grid, so `doc_key` narrows again from `('overview','guide','template')` to `('guide')`. PP1.1's other corrections — extras/`platform_extras` removed, the generator transform pattern, the verification negatives — all stand. The re-correction ships in PP2 (steps 2d–2f), still pre-PROD.**)* | Adopt the 3-card topic model across everything PP1 shipped. `scripts/extract-mockup-spec.ts` gains an explicit dated transform (the mockup still shows 4 cards — the raw-shape asserts stay as source-drift guards; post-transform asserts: 66 standard resources, 2 per topic, kinds OVERVIEW/GUIDE + a synthesized constant Template card) → regenerated `docs/workspace-spec.json` (3-entry `resourceKinds` · per-topic `standardDocs` ×2 · `extras` gone · per-topic `templates` kept as the PP6 picker candidate list) + regenerated 0028 (extras seed gone). 0027 edited in place (PROD never ran it): `platform_extras` + `get_platform_extras` removed; `doc_key` CHECK → `('overview','guide','template')`. Both verification scripts updated (3 tables / 3 RPCs · extras negatives · a `pg_get_constraintdef` probe on the CHECK). `.gitignore` += the owner's mockup copy `docs/PH - Palestine House Final.html` (kept until the PP series closes). | Corrected 0027+0028 re-applied + re-verified on TEST (approved 5/10/33 · coded 297 · doc_keys 0 · extras negatives pass); generator asserts green + regeneration stable; typecheck/lint/build green; no UI/behaviour change. | PP1 |
| **PP2 — Workspace shell v2 + About landing** *(+ the D-PP-f model re-correction, owner-added mid-sprint 2026-08-12 · ✅ **MERGED PR #76, 2026-08-12** (`main` = `ccbceba`) on `claude/sprint-pp2-workspace-shell-v2`, 10 gated steps 2a–2k. Corrected 0027+0028 re-applied + re-verified on TEST (all EXPECTs, role sims 8/8, seed digests match the spec, advisors clean). typecheck/lint/build green; CSS containment proven rule-by-rule (0 baseline rules changed, all additions `.pw-` scoped); path-guard clean (zero public/API/middleware/config files). **Not verified: the signed-in visual — still the owner's Preview/Production check, carried into PP3.** **0027+0028 applied to PROD by hand 2026-08-12 and verified read-only — they are now IMMUTABLE; any further schema change is a NEW numbered migration.** Record: `docs/sprint-prompts/pp2-workspace-shell-v2.md`)* | **First, the model re-correction (steps 2d–2f), pre-PROD:** ROADMAP/PROJECT-STATUS rewritten for D-PP-f (⑦ above) · the generator transform changed to keep **guide only** (33 standard docs, 1 per topic), drop the synthesized Template card, and emit the **297 templates as a live per-topic surface** · regenerated `docs/workspace-spec.json` + `src/lib/workspace-v2/spec.ts` + the 0028 seed · `0027_platform_ia.up.sql` edited in place again (PROD still never ran it): `doc_key` CHECK → `('guide')` · both verification scripts updated · corrected pair re-applied + re-verified on TEST. **Then the chrome:** header (5 nav links About/Setup/Operate/Program/Support + tooltips, solid-on-scroll, mobile panel, account button → `/account`) + workspace footer (search CTA · Ask HQ · tatreez band · Arabic tagline) + toast system — all namespaced (`.pw-` prefix + scoped tokens; fonts via `next/font` in the workspace layout) so legacy workspace CSS coexists until PP5; never touch `.adm-toast*`. `/dashboard` → the About landing (hero + 4 journey cards) **keeping pending/declined states**. 4 stub section pages (`/setup` new + `/operate` `/program` `/support` replaced) with real chrome + hero + honest "content arriving" state so no nav link 404s. `GATED_PREFIXES` += `/setup`. | **Owner applies the corrected (PP1.1) 0027+0028 to PROD before merging** (additive; old code provably unaffected). About matches the mockup desktop+mobile; pending/declined walkthrough passes; typecheck/lint/build green. | PP1.1 |
| **PP3 — The four toolkit pages** *(the big one · ✅ **MERGED PR #77, 2026-08-13** (`main` = `50978fe`, branch deleted); built + pushed 2026-08-12 on `claude/sprint-pp3-toolkit-pages`, 9 gated steps (3a–3g, 3i, 3j); **code only — zero SQL files in the diff**, since 0027/0028 have run on PROD and are immutable. typecheck/lint/build green (47 routes); CSS containment re-proven rule-by-rule at every step (0 baseline rules removed, 129 added, every selector rooted at `.pw-root`, 0 `.adm-*` touched); path-guard clean; PROD invariants re-verified read-only (grid 297 / wrong-bucket 0 / empty topics 0 / anon EXECUTE false on all four RPCs / all three read RPCs still `is_approved()`-gated). 320px measured in a browser against the built CSS. **Not verified: the signed-in visual — no Supabase env in the build checkout, so it stays the owner's Preview check, as in PP2.** **Independent review 2026-08-12 = "request changes", no blocking issue; 2 correctness defects + 5 design mismatches, all fixed on-branch and re-measured.** Record: `docs/sprint-prompts/pp3-toolkit-pages.md`)* | `/setup` `/operate` `/program` `/support` fully per mockup: one `SectionExplorer` client component (accordion groups → image-led topic cards → inline expand Explore↔Back, `#topic-slug` hash deep links) fed by server pages via the PP1 RPCs + `getResources()`; per topic (**D-PP-f**): the **summary** (`description` + `intro`) · **one "Start here" Simple guide card** (Read Now → coming-soon toast in PP3, wired to the reader route in PP4 — PP3 must not link to a route that does not exist yet · Download Now → existing signed-URL action, or coming-soon toast until the guide file is uploaded) · **Watch Video** toast/YouTube · the **templates grid restored** — the `resources` rows for that `element_id` under the **D-PP-i predicate** (`is_public = false` + `doc_key IS NULL` + `code IS NOT NULL`), badged by `resources.code`, downloading through the existing signed-URL server action (these files already exist, so this is the one surface that is fully live from day one). **No Overview card, no checklist/watch cards, no extras block, no preview modal — and no per-page local search (D-PP-g: the final mockup renders none; its `.local-search`/`localSearch()` are dead CSS/JS, and search arrives once as PP4's global overlay).** **Ask HQ on `/support` ships COMPLETE** — the mockup's moss panel wired to the Ask HQ server action that has been live since S6 (approved-only RPC `0019` + a Resend notification, S12 12-6). **D-PP-h is superseded:** it had deferred the send to PP4 on the premise that it needed building, which reading `src/lib/support/actions.ts` disproved — only the field shape differed (Focus Area → `subject`, question → `message`; name/email prefilled read-only from the session), and it maps with zero backend change. The legacy form is replaced outright and no channel is lost. | All 4 pages match the mockup desktop+mobile (D-PP-f model applied); deep links + template downloads work for an approved user; pending user sees zero content — no topics, no guide cards, no template rows; typecheck/lint/build green; CSS containment re-proven by a rule-level bundle diff; no SQL file in the diff. | PP2 |
| **PP4 — Read Now reader + global search** *(✅ **MERGED PR #78, 2026-08-14** (`main` = `638b6f4`, branch deleted); built + pushed 2026-08-13 on `claude/sprint-pp4-reader-search`, off `main` `50978fe`, 9 gated steps (4a–4i); **code only — zero SQL files in the diff**, which D-PP-j is what guarantees. "+ Ask HQ send" is struck from the title: it already sends, D-PP-h having been superseded mid-PP3. typecheck/lint/build green (51 routes; reader routes 107 kB, every existing page unchanged because the search index is fetched on first open). CSS containment re-proven rule-by-rule at every step (1840 → 1925 rules, 0 changed, 0 added rules outside `.pw-root`, 0 `.adm-*` touched). Path-guard clean, zero SQL. PROD invariants re-verified read-only. **Not verified: the signed-in visual** — no Supabase env in the build checkout, as in PP2 and PP3; fail-closed behaviour was proven instead by database role simulation (pending → 0 topics / 0 resources → an empty search index). **Independent review 2026-08-13 = "request changes", NO blocking issue** — it reproduced the containment claim independently; its one Medium finding (`stripGuideCover` deleted a leading non-Latin line, because compaction was ASCII-only) was correct and is fixed + regression-tested in `cab785e`, which also stops documentation from contributing rules to the CSS bundle. Record: `docs/sprint-prompts/pp4-reader-search.md`)* | Reader at `/{section}/{topic}/guide` (4 thin section wrappers → one shared server `toolkit-reader.tsx`; **`guide` is the only doc kind under D-PP-f**): renders `simple_guide_md` through the existing sanitized markdown (`src/lib/workspace/markdown.ts` reused); download footer (the `doc_key='guide'` resources row, or coming-soon); breadcrumb back to `/{section}#{topic}`. Reader page design = Claude's licensed freedom (new tokens, editorial reading layout). Global search overlay (Ctrl/⌘+K; the mockup's four chips All/Focus Areas/Guides/Templates; deep-links into topic). ~~**Scope decision required at PP4 kickoff (review 2026-08-12)**~~ → **RESOLVED as D-PP-j (owner, 2026-08-13): titles + summaries.** A query matches an item's title + breadcrumb path + kind, plus — for a focus area — its own `description` + `intro`. **No guide-body search, so no search RPC and no migration: PP6 keeps 0029 and PP7 keeps 0030.** The index is the same approval-gated reads PP3 already uses (33 focus areas + 33 guides + 297 templates), fails closed to empty for a pending or anonymous caller, and carries no resource ids, storage paths or bucket names. The mockup's per-hit **preview modal is not ported** (PP3 dropped it): a Guide hit opens this sprint's reader, a Template hit deep-links to `/{section}#topic-<slug>`. ~~Ask HQ submits via the existing Resend server-action pattern~~ — **already shipped in PP3**, see below. **One PP3 hand-off is mandatory (2026-08-12):** swap PP3's Read Now coming-soon toast for the real reader link on all 33 topics. *(The second hand-off — closing the D-PP-h Ask HQ regression — no longer exists: PP3 shipped the panel wired to the S6 action, so `/support` already sends. **PP4's Ask HQ work therefore shrinks to whatever the existing action does not already do**, which on inspection is nothing this scope names — re-read `src/lib/support/actions.ts` at PP4 kickoff before scoping any of it.)* | Read Now works for the guide on all 33 topics; search finds topics/guides and deep-links correctly; Ask HQ email arrives (or no-ops honestly without env); typecheck/lint/build green. | PP3 |
| **PP5 — Teardown + cutover** *(code only, no DB change · ✅ **MERGED 2026-08-15 inside PR #82** — stacked under PP6a, branch deleted · built + pushed 2026-08-14 on `claude/sprint-pp5-teardown-cutover`, off `main` `638b6f4`, 8 gated steps (5a–5h); **zero SQL files in the diff**. 56 files, **523 insertions / 6,087 deletions**. Route table 52 → 41; CSS shipped to every visitor **219,786 → 180,391 bytes** (−39 KB), rule headers 1932 → 1588, with `.ws-`/`.acct-`/`.live-` at **0** and `.adm-` unchanged at **64**. typecheck/lint/build green. Path-guard clean: no public page, API route, middleware or `lib/site.ts` touched; `next.config.ts` changed only by `redirects()` plus a CSP **comment** (no directive value moved). All twelve retired paths verified 307 → `/dashboard` against a running production server. PROD invariants re-verified read-only: 297 in-grid templates / 0 wrong-bucket / 2 public booklets / 33-10-5-33 / anon EXECUTE false and `is_approved` gating intact on all read RPCs. **Not verified: the signed-in visual** — no Supabase env in the build checkout, fourth sprint running. **Independent review 2026-08-14 = "request changes", NO blocking issue** — no gating failure and no runtime bypass; its one Medium finding was a defect in **this sprint's own docs rewrite**, where the blanket "every workspace RPC enforces `is_approved`" rule had been narrowed to a list of four read RPCs, dropping the `submit_support_request` write and `get_resource_download` out of scope. Both re-verified gated on PROD, and the blanket rule is restored on-branch. **The scope cell below was corrected at kickoff — the original named four deletions that would have broken live v2 surfaces, and CSS line numbers that had drifted; ⚠️ marks each. Two further corrections were found while building: the `pages.css` `.live-*` recipes were NOT shared with `/experience` (LH1 had already retired that strip) so they went too, and `v3.css` — absent from the scope entirely — held three legacy overrides, one pairing a dead class with the PUBLIC `/our-support` artefact icon.** Record: `docs/sprint-prompts/pp5-teardown-cutover.md`)* | Delete the old workspace: routes `/plan /build /food /programming /live /live/[id] /elements /resources /academy /tools` + old dashboard/support UI, `workspace-shell.tsx` + `pending-state.tsx` + `components/shared/session-card.tsx` (its only consumers were the two `/live` routes), and the `(workspace)` layout — the whole route group goes, because **⚠️ `/account` moves to `src/app/(platform)/account` and is restyled in the `.pw-` register** (owner, 2026-08-14: the new header links to it twice and the footer once, so it must survive; restyling it rather than keeping its `.acct-*` block is what lets the legacy CSS go in full). Dead libs: `src/lib/build`, `src/lib/live/{actions,format,sessions,types}.ts`, `src/lib/resources/view.ts`, `src/lib/workspace/{progress,sample-videos}.ts`, and the now-unreferenced `getElements`/`getChecklist`/`getAcademyModules` exports. **⚠️ Four modules the original list would have deleted are load-bearing and STAY:** `src/lib/live/youtube.ts` (`src/lib/admin/content-actions.ts` imports `parseYouTubeId`; PP6's CMS needs it) · `src/lib/resources/actions.ts` (the signed-URL download behind `pw-guide-download` + `pw-template-grid`) · `src/lib/workspace/{content,markdown,types}.ts` (`src/lib/workspace-v2/content.ts` imports `getElement`, `getResources`, `safeHttpUrl`, `renderMarkdown`, `ResourceRow`) · `src/lib/support/focus-areas.ts` (`pw-ask-hq.tsx`). Old workspace CSS — **⚠️ `globals.css` **885–2726** (the "Gated workspace chrome (S4)" banner up to, not including, the "Internal HQ admin chrome" banner) + **3141–3225** (the LH1 live-hub block to EOF); the earlier "~874–2716 + 3130–3214" had drifted, so cut on the banner comments, never on line numbers. The `.adm-*` block between them survives untouched. **⚠️ `pages.css`'s `.live-*` recipes are SHARED with the public `/experience` strip** (the file's own comment says so) and are kept unless grep proves a rule unreferenced** — all grep-verified zero references. `next.config.ts` gains `redirects()` old paths → `/dashboard`, `permanent: false` (config change — call out; a browser-cached 308 on a gated, noindexed URL would be unpickable if the model moves again). Sync `GATED_PREFIXES` + admin `revalidatePath` targets. Update CLAUDE.md · **AGENTS.md** · TECH-ARCHITECTURE · SECURITY-CHECKLIST §15 (recording that the D-PP-i `storage_bucket` guard is still owed by PP6 — PP5 does not close it) · DESIGN for the new workspace model, deleting their Stage 4 supersession blocks. **No SQL: the academy/checklist tables stay until PP7's 0030, and the `/admin/content/academy` screen is PP6's to delete, not PP5's.** | No dead routes/links/CSS; redirects live; docs match reality; typecheck/lint/build green; public pages byte-identical (path-guard). | PP4 |
| **PP6a — IA unlock + CMS write layer** *(+ migration 0029 · ✅ **MERGED 2026-08-15 as PR #82** (`main` = `81f1707`; PP5 + `docs/pp6-replan` landed in the same PR, the branches being stacked; all three deleted). Built 2026-08-14 on `claude/sprint-pp6a-ia-unlock-cms-write`, 9 gated steps (6a-a…6a-i), plus a review round. **`0029` applied to PRODUCTION by the owner by hand and verified read-only — every late fix present, 0 overloads, 0 anon-executable, all hardened, content unchanged, and all 297 private objects still resolve under the narrowed storage policy (0 unreferenced, 0 broken rows).** ⭐ **The partner path was finally proven end-to-end with a real signed-in session** (template 20,083 bytes of `.docx`, booklet 66,815 bytes of PDF), and the review's Draft attack was **executed and refused** — 33→32 topics, 299→291 resources, guide 0 rows, direct signing REFUSED, file absent from listings. **Independent review = BLOCKING (4 + 2 findings), all six fixed on-branch before `0029` ever reached production.** 34 files, **+5,382 / −1,055**. typecheck/lint/build green (41 routes). **0029 proven on TEST** — full down→up→down→up cycle, the `.up.sql` file re-applied verbatim, the down file's abort guard fires, zero leftover functions after the down. **74 role-simulated scenarios pass**, including 4 direct-INSERT attacks that bypass every RPC. **PROD re-verified read-only at the gate and is untouched**: 5/10/33/33/299/297, 0 wrong-bucket, 2 booklets, `published` absent, 0 platform admin functions, 1 storage policy — `0029` is the owner's hand-run at the merge gate. CSS 180,391 → 180,530 (+139), exactly 2 rules added, both `.pw-root`-scoped, 0 removed. Path-guard clean. **Not verified: the signed-in visual** (no `.env.local` in this checkout — fifth sprint running). **Five bugs found by running the code rather than reading it** — see the record. Record: `docs/sprint-prompts/pp6a-ia-unlock-cms-write.md` · the first of the three sprints that replace the retired single PP6 · added 2026-08-14 under D-PP-k · ⚠️ **SCOPE AMENDED AT KICKOFF, same day**, after every load-bearing claim was re-verified read-only against PROD: **6 blocking corrections, 1 scope addition, 1 scope drop**, each marked ⚠️ inline with its evidence. The pre-amendment scope would have shipped a sprint whose headline deliverable did not work.)* | **No content moves in this sprint — this is the machinery the content needs.** ① **Remove the 33-slot ceiling.** ⚠️ **It has THREE enforcement layers, not one:** the table CHECK `elements_slug_shape` (`0011:32` — not `:33` — widened `0026:23`); the **live `admin_upsert_element` body**, which independently raises on `v_slug !~ '^[a-k][1-3]$'` and `v_fac !~ '^[A-K]$'`, so `0029` must `CREATE OR REPLACE` it; and the **zod schemas** at `src/lib/admin/content-actions.ts:56, :58, :132`. Relaxing only the CHECK ships a sprint whose stated purpose does not work. ⚠️ **Also `elements_focus_area_shape` plus three NOT NULL columns:** `focus_area_code`, `focus_area_name` and `code` are all NOT NULL with no default, and the new 22 focus areas have no A–K identity — so `0029` makes `focus_area_code`/`focus_area_name` **nullable** and relaxes the CHECK to `focus_area_code IS NULL OR ~ '^[A-K]$'`, rather than fabricating junk values. ② **Draft/Live:** `published boolean not null default true` on `platform_topics` + `platform_groups` (default `true`, so this sprint is a no-op on screen). ⚠️ **The filter must reach FOUR read RPCs, not one** — the pre-amendment scope said "member reads + the search index" and would have left three holes: `get_platform_topics` (which covers the section pages, the reader **and** the search index in one move — the index is `getSearchIndex` at `src/lib/workspace-v2/content.ts:238-290`, **not** `search.ts`, which is a 27-line `"use server"` boundary); **`get_element`**, which returns `simple_guide_md` directly and is executable by any approved caller, so a draft guide body is one console call away; **`get_resources`**; and **`get_resource_download`** — *the only surface that hands out bytes*, so without it "Draft" is an editorial illusion at the file layer. `published` must propagate `element_id → platform_topics → platform_groups`. Also settle `get_elements()`: live, unfiltered, **zero callers**, and absent from PP7's `0031` list — filter it or drop it here. ⚠️ `get_platform_groups` **does not exist** (group data rides inside `get_platform_topics`); any plan text naming it was planning against nothing. ③ **The IA write path:** `admin_upsert_platform_section` / `_group` / `_topic`, plus delete and reorder, plus `admin_list_platform_*` that **does** return drafts (the member RPCs must not). ⚠️ **Creating a focus area is a two-table transaction:** `platform_topics.element_id` is NOT NULL + UNIQUE + FK→`elements`, so the RPC must insert `elements` then `platform_topics` atomically, or a half-created focus area is possible; the CMS's guide-body field lives on `elements.simple_guide_md` while the other ten live on `platform_topics`. ⚠️ **Delete semantics are asymmetric and one direction is silently destructive:** group→topics is `ON DELETE RESTRICT` (catch `23503` and say *move its focus areas first*, never leak the SQLSTATE); section→groups is CASCADE; and deleting an `elements` row CASCADEs its `platform_topics` row while `SET NULL`-ing its resources' `element_id` — which drops those templates out of every grid (`content.ts:106`) while row and Storage object survive untracked. Order is **resources → topic → element**, the pattern `0026_focus_area_k.down.sql` already documents. ⚠️ `platform_topics_slug_key` is **globally** unique, not per section, and the old 33 keep their slugs until `0030` — the CMS needs a *that name is already used* message, not a raw `23505`. ④ **A real resource-file lifecycle:** `admin_register_resource_file` / replace / reorder / delete, covering the `resources` row **and** the Storage object together, with a **stated** compensation path when one half fails. ⚠️ **Nothing can INSERT a `resources` row today** — zero `insert into public.resources` in any shipped function — so this is built from nothing; `storage_bucket`/`storage_path` are NOT NULL with a UNIQUE pair, so the first INSERT path must set them, which is precisely why D-PP-i must close in the same migration. ⚠️ **Make `code` required at registration:** `code IS NOT NULL` in the grid predicate is currently a no-op (0 rows violate it), and this RPC is the first thing that could create a private, uncoded row that silently vanishes from every grid. ⑤ **`storage.objects` policies.** ⚠️ Re-verified: **exactly one policy exists** — `resources_bucket_select_approved`, SELECT only, `USING (bucket_id = 'resources' AND is_approved())`, no WITH CHECK, no INSERT/UPDATE/DELETE policy on any bucket. `is_approved()` does not include admins, so admin write policies **and** an admin read path are genuinely required; the new policies must not widen partner SELECT while adding admin access. ⑥ **The D-PP-i guards — in the RPC, closing `SECURITY-CHECKLIST` §15.** A template row forced `is_public = false`, bucket `resources`, `element_id NOT NULL`, `doc_key NULL`; a guide row `doc_key='guide'` with `element_id NOT NULL`. ⚠️ **Do NOT write a blanket `storage_bucket = 'resources'` table CHECK — `0029` would abort at apply time:** 2 rows legitimately live in the public `booklets` bucket (the two booklet PDFs). Make it conditional (`is_public = false → bucket = 'resources'`) or RPC-side only. ⚠️ **The unique index is `(element_id, doc_key) WHERE doc_key IS NOT NULL`** — the `'guide'` restriction is a *separate* CHECK (`resources_doc_key_shape`), and with no `NULLS NOT DISTINCT` an **unlimited** number of `element_id IS NULL, doc_key='guide'` rows are insertable, so the orphan-guide guard is load-bearing, not belt-and-braces. ⑦ **CMS shell** — `/admin/content` restructured to **Pages · Focus areas · Files · Admins**; the retired Videos/academy screen deleted (`src/app/admin/content/academy/`, hub card at `admin/content/page.tsx:25-28`; PP5 left it to this sprint). ⚠️ **SCOPE ADDITION, accepted at kickoff — otherwise Pages is a dead screen:** `get_platform_sections()` has **zero call sites**; every section hero, label and breadcrumb comes from the generated static `PLATFORM_PAGES` (`spec.ts:110`, consumed at `pw-hero.tsx:14`, `pw-section-page.tsx:62`, `pw-guide-reader.tsx:72`, `content.ts:257`), and `content.ts:33-36` says in comments that PP6 is where the wrapper belongs. So PP6a ships the `get_platform_sections` wrapper and switches those consumers — a form whose Save changes nothing must not be handed to the person whose sign-off gates PP6b. The 5 sections are frozen by `platform_sections_slug_shape`; the screen edits, never adds, and the copy should say so. ⑧ **Focus areas + Files screens** — the eleven fields, Draft/Live, and the eight unbreakable rules (Draft by default · delete only on Draft · type-the-name-to-delete · slug frozen on rename · the owner never chooses where a file lives · type + size checked before upload · Save disabled until changed · cannot remove yourself or the last admin, already enforced at `0024:105-112`). **Never a path, id, bucket name or JSON on any admin screen.** Reuse the existing revalidation pattern (`content-actions.ts:120-121, :241`), don't reinvent it. ⑨ `SectionExplorer` renders a single-group section as a **flat list with no accordion header** (D-PP-n) — `pw-section-explorer.tsx:197-251`; `setup` and `support` have exactly one group each, so this is the one member-facing change and it is visible immediately. The change must rehome `hidden={!open}`, `aria-controls`, the *{n} Focus Areas* meta and `group.description`, all of which live inside the button being removed. Fix `FOCUS_AREAS` (`resources-admin.tsx:43` — the array stops at `"J"`; **30 rows carry `focus_area_code='K'`** and Save currently nulls them, so this is destructive, not cosmetic). ⑩ ⚠️ **DROPPED from scope at kickoff: the `serverActions.bodySizeLimit` bump.** `next.config.ts` has no `experimental` block at all, the Next.js default is 1 MB, and the largest delivered file is 188 KB. Adding an experimental block to a path-guarded config file with no evidence it is needed is the wrong trade; revisit only if a real file exceeds the default. | `0029` + `.down.sql` + **both** verification scripts (⚠️ note: **no `0028_verify_*` exists** — `0029` restores the one-per-migration convention rather than assuming it held) applied and **verified on TEST**: full down→up→down→up clean and idempotent · anon and pending callers 0 rows on every read RPC · a Draft focus area is invisible to an approved partner **and** its guide body unreachable via a direct `get_element` call **and** its template ids unusable via `get_resource_download` **and** absent from the Ctrl/⌘+K index · an admin cannot create a public template, a wrong-bucket row, an uncoded template or an orphan guide · role sims green · advisors clean. ⚠️ **`0029.down.sql` carries a `*** READ BEFORE RUNNING ***` header and an ABORT guard** — *not* the delete-first pattern of `0026_focus_area_k.down.sql`, because here the non-conforming rows would be the owner's real final content: raise if any `elements.slug` fails `^[a-k][1-3]$`, and say plainly that the file is safe only before PP6b has loaded content. **Reversibility must be proven on TEST before PP6b runs, because after PP6b the proof is no longer repeatable.** **Owner applies `0029` to PROD by hand at the merge gate.** typecheck/lint/build green; zero content change; CSS containment re-proven by a rule-level bundle diff — ⚠️ counting `.adm-` by the *same method* before and after (grep gives **63**; the PP5 record's "64" is a bundle-level figure from a different counter, and a mismatched method would fail the gate on a measurement artefact). | PP5 |
| **PP6b — PILOT: Focus Area 1.1 end-to-end** *(data only — no migration · owner review gate · added 2026-08-14 · ✅ **COMPLETE — OWNER SIGNED OFF 2026-08-15.** Built on `claude/sprint-pp6b-pilot-focus-area` off `main` `8fbff9d`, steps 6b-a … 6b-h, plus **two independent review rounds**. **6b-g passed: the owner drove the CMS himself, published the pilot from the Focus areas screen, downloaded the guide file, and answered the five pilot questions** (§5 D-PP-q). TEST only, **zero SQL**, production untouched. 1.1 loaded as Draft (34 topics — the 33-slot ceiling proven gone with real content); the partner path **executed** in both Draft and Live states, including a template downloaded as **183,352 real bytes** and a direct storage-signing attack **refused**; ⭐ **the signed-in visual, unverified since PP2, verified in a browser.** typecheck/lint/build green · CSS 180,530 → **180,418** (−2 rules, **0 added**) measured against a build of the base commit · path-guard clean. **Sixteen defects, none found by reading code** — six by running it, and **ten across two independent review rounds, each of which returned BLOCKING and was right on every count.** Round 1 (1 Critical + 1 High + 2 Medium): the section label was deleting a legitimate `## Program` heading — content loss in the one function that edits an owner's words, missed by this sprint's own suite because its test used a heading naming a *different* section; and the loader could delete a registered object on a retitle. Round 2 (1 Critical + 1 High + 4 Medium): decoration patterns (`Section n`, `n Steps`) still licensed removal on their own, so a bare `## Section 3` heading was deleted; two headings concatenating to the title were consumed as a wrapped cover; and a template **code is not a stable identity**, so an alphabetical rename could attach each file's name to the other's bytes. All ten fixed in `4ce4b5e` and `fbc6c31`, each reproduced before and after. ⚠️ **`e49d243..fbc6c31` is unreviewed — the owner declined a third round (2026-08-15), accepting the risk.** Record: `docs/sprint-prompts/pp6b-pilot.md` · ⚠️ **SCOPE AMENDED AT KICKOFF 2026-08-15**, after every load-bearing claim was re-checked by extracting all 132 delivered documents with the real code and querying both databases read-only: **twelve findings, three blocking**, each marked ⚠️/🔴 inline with its evidence, plus one scope addition and two owner decisions — see §5 D-PP-p. The pre-amendment scope would have shipped a "one-line fix" for a defect with two causes, one of which nobody had seen.)* | **One focus area, real content, real screens, built with the exact machinery that will do the other 21.** Target: **Setup → Get Legally Ready**, loaded as **Draft** on **TEST**. ① **Fix the content-shape defects.** 🔴 `stripGuideCover` (`src/lib/workspace-v2/guide-cover.ts`) fires on **0 of 22** delivered guides — measured 2026-08-15 by running it over all of them, not on 1.1 alone as first recorded — so "Palestine House: Set up" + "*Title* Simple Guide" would print above the reader's own heading on **every** focus area. ⚠️ **It is not a one-line fix; there are two independent causes.** 🔴 **All 132 delivered documents carry `<a id="…"></a>` anchors (19–21 each) and ZERO of the 33 live guide bodies carry any** (`select count(*) from elements where simple_guide_md like '%<a id=%'` = **0** on PROD) — a shape nobody had seen. The ids are copy-paste residue (`get-legally-ready` heads *every* Overview; `step-1.-register-our-local-entity` appears inside Monthly Check-Up), one sits **mid-line inside bold** so a line-leading strip is not enough, and an anchor id survives the function's own compaction as content, which is why the first line never looks like cover matter. The second cause is the **section label**: the cover's first line is `Palestine House: <Section>`, and "Set up"/"Operate"/"Program"/"Support" are not cover vocabulary. Fix **conservatively** — this is the only code in the product that alters an owner-authored body — and regression-test on the **real** openings (all four section variants + 4.5's mid-line anchor), keeping every PP4 case green in `scripts/verify-guide-cover.mts`. Also map the new template kinds onto the seven values the DB allows (`form/script/log/report/approval/guide/booklet` — rendered nowhere, bookkeeping only). ② **Rewrite `scripts/ingest-content.ts`.** ⚠️ **"the front half" understates it:** its *back* half writes `checklist_items` and `academy_modules` — retired tables whose read RPCs `0029` already dropped, and which `CLAUDE.md` forbids adding a caller to — so the write half goes too. Its source directory (`docs/source-assets/resources/2. Focus Areas/…`) **no longer exists on disk** and it hard-fails unless it finds exactly 30 focus areas (`:275-276`) and 267 templates (`:792`). It must read `docs/source-assets/Resource/Palestine House Website Content - Complet and Formatted/` and survive its real messiness (three templates-folder names — `Template-Samples` ×5 · `Template` ×13 · `Templates` ×4 · `_V.1` on 128 of 132 but none of folder 1.1's 4 · ⚠️ **6 folders whose name ≠ their document's title, not 4** · typos `Responsiblity`/`House-toHouse`/`Complet` · `&` in three paths) — full trap list in `docs/content-migration-map.md` §5. **Rules: read the title from inside the document, never from the folder name; never rewrite the owner's wording.** ⚠️ **A title may span two source lines** (3.6 = *"Connect to the Wider Palestine"* + *"House Network"*), so a one-line read is wrong; ⚠️ **"the opening sentence" is not a split on `.!?`** — 2.6's contains `"How are we doing?"`; ⚠️ **one deliberate rule is owed for template titles**, because §4's names are cleaned and the delivered filenames are not. Hard-assert **4 · 22 · 88** and one Overview + one Simple Guide each, failing loudly. Emit a **committed, reviewable spec file** (the PP1 pattern) so the content can be reviewed without opening 132 Word files. **No DB writes in this step.** ③ **Load 1.1 onto TEST as Draft.** ⚠️ **Through the same admin RPCs the CMS calls** (`admin_upsert_platform_group` → `admin_upsert_platform_topic` → storage upload + `admin_register_resource_file`), never hand-written INSERTs: `published` **defaults to `true` at the column level**, so a direct INSERT lands **Live**, while the RPCs default to Draft — the pilot must not be able to publish itself by accident. Idempotent and re-runnable, signing in with the credentials already in `.env.local`, which the script reads itself and never prints. 1 group + 1 focus area + summary (the Overview's opening sentence → `description`; `intro` left empty) + Simple Guide (the rest of the Overview prepended, **D-PP-m**) + 2 templates into the private bucket + the photo **copied**, never renamed, from `legal-compliance-and-risk.jpg` (**D-PP-o**, §4). ④ **Prove it by running it, as a real signed-in partner** — possible for the first time in the PP series, the five-sprint blind spot having closed at the PP6a merge. **Draft:** absent from `/setup`, guide body unreachable via a direct `get_element`, template ids unusable via `get_resource_download`, absent from the Ctrl/⌘+K index — four surfaces, executed rather than reasoned. **Live:** the reader renders the guide with the title printed **once** and no stray anchor markup, both templates download as real bytes, search finds it, `#topic-get-legally-ready` deep-links. **Pending:** nothing at all. The `.env.local` account is an **admin**, so it is temporarily removed from `admins` on TEST for the partner leg and restored; TEST's exact starting state is recorded first and restored at the end. ⚠️ **Expect Setup to show two accordion groups once the pilot is Live** — it has exactly one group today (`setup-toolkit`, 5 topics) and D-PP-n renders that flat, so a second makes it two until `0030` deletes the old one. Correct and temporary, not a bug; invisible while Draft, because a group with no published topic returns no rows. ⑤ ⚠️ **SCOPE ADDITION, accepted at kickoff (owner, 2026-08-15): the Photo field becomes a picker.** `focus-areas-admin.tsx:449-462` is a free-text box whose placeholder is literally `/assets/workspace/topics/legal-compliance-and-risk.jpg` — contradicting PP6a's own rule ⑧ (*never a path, id, bucket name or JSON on any admin screen*) and this sprint's gate (*without help*). A chooser over the 33 photos already in the repo; no upload, no storage, no migration. *(`platform_topics.icon` needs no equivalent: new rows get `'sprig'` and the column is deliberately not carried into the card — `types.ts:35`.)* ⑥ **The owner drives the CMS unaided** — title · summary · photo · YouTube · guide text · files · Draft⇄Live — then views it as an approved partner and as a pending one. | **Owner sign-off is the gate.** He can add a focus area, edit its words, upload a file, publish and unpublish it **without help**; an approved partner sees it correctly on phone + laptop; a pending partner sees nothing at all. Owner answers the five pilot questions (summary length · guide proportions in the reader · the photo mapping · is the CMS genuinely simple enough · are the three prose "templates" downloads or reading material). ✅ **Resolved at kickoff — and it was recorded all along: Vercel Preview points at TEST** (`PROJECT-STATUS.md` §6, non-production ref `sdszcralogcrujtyghig` — *"Preview + Development env vars point here"*), so **the pilot is built and reviewed on TEST and PP6b touches production not at all**; the migration-map row calling this UNVERIFIED is corrected in the same PR. Also required: `stripGuideCover` fires correctly on all 22 real openings **with every PP4 case still green** · the extractor's asserts (4 · 22 · 88) hold and its spec file is committed · the loader is idempotent (a second run changes nothing) · **TEST left in a stated, known state** · typecheck/lint/build green · CSS containment re-proven by a rule-level bundle diff counted by **one** method · path-guard clean (no public page, API route, middleware or `lib/site.ts`) · **zero SQL files in the diff.** | PP6a |
| **PP6c — Rollout: the remaining 21 + cutover + deletion** ✅ **MERGED PR #85, 2026-08-16** (`main` = `d723475`) *(+ migration 0030 · added 2026-08-14)* | ✅ **MERGED as PR #85; built on `claude/sprint-pp6c-rollout-cutover`, 8 gated steps (6c-a…6c-h). ⛔ 0030 is applied to TEST ONLY — PROD is deliberately still on 0029 until PP7 lands, because the review returned BLOCKING on the destructive tooling.** TEST end state: **22 elements · 22 topics live · 4 groups · 112 resources · 110 objects · 0 orphans**. Cold backup verified 3 independent ways (297 · 5,320,962 B · `6fde7927…`); partner path 9/9 Draft, 12/12 Live, 11/11 post-`0030`; cutover round-tripped 4 times including the break-glass SQL. *(Original scope:)* Run the PP6b pipeline over all 22 focus areas: **22 summaries · 22 Simple Guides · 88 templates (110 files uploaded) · 4 groups (one per section, D-PP-n) · 22 photos copied per `docs/content-migration-map.md` §4**. Everything lands as **Draft**; one switch flips all 22 Live. Produced as a reviewable, idempotent, reversible SQL file plus an out-of-band Storage upload — the pattern PP1 used. **Then the deletion (D-PP-k, owner 2026-08-14 — this reverses the standing "the 297 are never dropped" rule):** `0030` drops the old **33 `platform_topics`**, the **10 superseded `platform_groups`**, the **33 `elements`** rows carrying the old guide bodies, and the **297 `resources` rows + their Storage objects**. ⚠️ **HARD PREREQUISITE, not a nice-to-have — a `.down.sql` restores rows, never Storage objects:** before any DROP, export all **297 Storage objects** to `docs/source-assets/_archive-297-templates/` (gitignored, ~5 MB) and carry the 33 `simple_guide_md` / `overview_md` / `watch_out_for_md` bodies (~1.6 MB of finished prose) as **literal inserts inside `0030.down.sql`**. Record object count + total bytes in the sprint record. ⭐ **Amended at the 6c-a kickoff, 2026-08-16, and it dissolves the credentials problem:** the objects that must survive are **production's**, and exporting them looked to require production Storage credentials. Queried read-only across both projects, **TEST's 297 legacy objects are byte-identical to PROD's 297** — same names, eTags and sizes, with `md5(string_agg(name‖unquoted-eTag‖size order by name))` = **`6fde792718130d12071b69459f9d70ab`** on both, at **297 objects / 5,320,962 bytes** (TEST holds 300; excluding the pilot's 3, identical). **The backup is therefore pulled from TEST and *proven* equal to PROD's bytes** — assert each downloaded file's MD5 against its stored eTag, then recompute the aggregate fingerprint from disk and require that exact value. Commit `docs/archive-297-manifest.json` (name · eTag · size); the bytes stay gitignored under `docs/source-assets/`. ⚠️ **Also corrected here:** the "~1.6 MB of `simple_guide_md`" figure is **all three body columns** (1,577,160 chars); `simple_guide_md` alone is **630,020**. ⚠️ **Verify, do not assume, at 6c-g:** deleting a row from `storage.objects` in SQL removes the *metadata* and may **orphan the bytes** in the backing store — establish empirically what happens on TEST, and if it orphans, the objects must be removed through the Storage API as a separate explicitly-ordered step, with the migration not pretending to have done it. ⚠️ **The pilot is Live on TEST**, so the first `--all` run is refused without `--allow-live`, and TEST will briefly carry **55 focus areas in 14 groups** between 6c-d and 6c-g — correct and temporary. | All 22 render correctly for an approved partner on TEST (desktop + mobile); pending partner 0 rows; guide downloads, template downloads, Ctrl/⌘+K search and `#topic-slug` deep links all work; the Draft→Live cutover is reversible in one statement; **the 297-object backup is verified complete (count + bytes recorded) BEFORE the drop runs**; 0030 + `.down.sql` + verification applied and proven on TEST → **owner applies to PROD by hand**; typecheck/lint/build green. | PP6b sign-off |
| **PP7 — Review-round-3 hardening + public reconciliation** 🔵 **IN PROGRESS on `claude/sprint-pp7-hardening-reconciliation`, off `main` = `d723475`** *(NEW, inserted 2026-08-16 · + migrations `0032` and `0033`)* | ✅ **Step 7-a done: the kickoff verification re-derived all 15 findings from source — 11 of 11 blockers stand, M4 is measurably worse than written (all 297 PROD template rows carry a `focus_area_code` the rollback drops), B4 is real logic but not currently triggered, and the PP6c record's "global signOut fixed" is true of 1 file in 8. Ledger: `docs/code-reviews/pp7-round-3-ledger.md`.** 13 gated steps (7-a…7-m). **Why it exists:** the independent review of the whole PP series returned **BLOCKING with 11 blocking findings**. Three were fixed inside PP6c (the fifth cover-strip bug, its non-idempotence at the cap, and `0031`'s RLS hole); **the other eight need real design and are this sprint.** Inserted before final QA deliberately — the old PP7's gate is *"0 known bugs; visual acceptance passes"*, and QA that runs before the public pages are reconciled and the destructive tooling is hardened is QA of a site about to change. **Scope: (1) the 8 remaining blockers** — the backup verifier can overwrite the only good archive copy before failing (stage, verify, then atomically promote; add a strictly read-only offline verifier) · **flip the deletion order to rows-first**, because orphan bytes are inert and retryable while live rows pointing at deleted files are not, and add a database preflight to the object deletion · strengthen `0030`'s guard to an **exact immutable 22-area manifest** (slugs, codes, groups, non-empty bodies, one guide file, per-area template counts) with a publication re-check immediately before commit, since counting "any 22 live numeric-coded rows" passes a decoy · make the down-generator snapshot **exactly the predicate the up-migration deletes** (an unlinked legacy element is deleted but never snapshotted) · make the rollback operationally recoverable (durable copies + an exact-key object restore tool + rehearsal) · make `cutover.ts` validate the exact 22-slug/code/group set and run **in one transaction** via a new RPC in `0032` · **build `--target prod` (D-PP-r) with strict argument parsing** — today an unknown argument is silently ignored, so `--target prod` runs against TEST · plus 4 mediums (swap detector still evadable · slug-as-identity bypasses the live guard · the "read-only" dry run mutates Auth state via global `signOut()` · restored `resources` rows omit `focus_area_code` and timestamps). **(2) D-PP-a IN FULL** — see the note carried below: the public `/focus-areas` page is a full map of the retired IA **by name**, not a proof band, so it is rewritten to the real 22. ✅ **Copy unblocked 2026-08-16 (D-PP-s): the owner authorised Claude to draft it** from the delivered Overviews — each focus area's own opening sentence becomes its public one-liner, so public and private cannot drift. Owner reviews on Preview before the sprint closes. Proof numbers **11 · 33 · 297 → 4 · 22 · 88**, and *200+ checklist items* is **removed** (checklists died at D-PP-b; `0030` deleted the last 818 rows). **(3) The See More card affordance** (owner request 2026-08-16): the focus-area card shows the Overview's opening sentence and should expand to the rest. `platform_topics.intro` already exists, is empty on all 22, and the D-PP-f model already defines the summary as `description` + `intro` — so the extractor writes the remainder there and the card gains a disclosure toggle. **No schema change.** The text deliberately also remains at the head of the guide (D-PP-m) — owner-confirmed duplication. **(4) Load production** — nothing has ever been loaded there. **(5) Repair `0029_verify_TEST_db_only.sql`** (obsolete delete/replace signatures abort it early) and **rehearse the full 1.75 MB down-migration** end to end. **(6) THE CONTRACTION, moved here from PP8 (owner, 2026-08-16).** `0033` drops `checklist_items`, `checklist_progress` + `set_checklist_progress`, `academy_modules` and its four dependent admin RPCs (`0023`), the live-hub RPCs, `programming_sessions` policies, and **`elements.overview_md`** — ⚠️ **`CREATE OR REPLACE` `get_element()` (`0011`), `admin_upsert_element` (`0026`) and the admin list/get element RPCs (`0023`) WITHOUT the column BEFORE the `DROP COLUMN`, or it fails or breaks callers.** Also delete the 33 superseded + 11 unused topic photographs. **Why it moved: `0030` already emptied all three tables (0 rows) and `0029` already dropped their read RPCs, so this is hygiene on things that are already dead — it changes nothing a partner can perceive, and does not deserve to gate the final QA sprint.** The owner decides here whether `elements.watch_out_for_md` stays or goes. | All 11 blocking + 4 medium findings fixed, each reproduced before and after; public pages describe the real 22 and the owner has signed them off on Preview; See More ships; `--target prod` exists, is argument-strict and rehearsed; **production loaded, cut over and verified**; the full rollback executed once on TEST; `0032` + `0033` applied TEST → owner PROD; typecheck/lint/build green. | PP6c |
| **PP8 — Final verification** *(was "Final QA + contraction" · slimmed 2026-08-16, owner-approved · **no migration**)* | **The contraction moved to PP7**, because `0030` had already emptied `checklist_items` / `academy_modules` / `checklist_progress` and `0029` had already dropped their read RPCs — dropping empty tables changes nothing a partner can perceive and does not deserve to gate the last sprint. **What remains is the half that is not droppable, and the argument for it is this project's own record: three independent review rounds, three BLOCKING verdicts, and every one found a real Critical** — including two in the one function that edits the owner's prose, the second found *after* a round had explicitly gone looking for that class of bug. Across PP6c my own checks found five defects and **every one was in the probe, not the product**. On that evidence "we tested as we went" is exactly the belief that has been wrong three times. **Scope: (1) multi-hat E2E**, desktop **and 320px** — pending-partner walkthrough · approved-partner walkthrough · the four toolkit pages · the reader on all 22 · guide + template downloads · Ctrl/⌘+K search · `#topic-slug` deep links · admin CMS flows · Ask HQ · the public shell and Apply. **(2) a11y pass** — the mockup's focus order, aria labelling and announcer behaviour; keyboard-only traversal of the accordions, the reader and the search palette. **(3) Security invariants re-verified end to end** (`SECURITY-CHECKLIST` §15): approval enforced server-side on every RPC · anon and pending get zero rows everywhere · Draft invisible for rows **and bytes** · admin 404s for non-admins · signed-URL TTL · no secret leakage · the D-PP-i storage guards still hold · **and the `0031` policy sweep re-run, asserting exactly three documented ungated policies and no fourth**. **(4) Performance** — image weights on the 22 topic photographs (they are 200–290 KB each, unoptimised, and there are now 22 of them above the fold across four pages). **(5) BOOK AN INDEPENDENT REVIEW.** Not optional. It is the only thing in this project that has reliably found the Criticals, and PP7 is the largest and most destructive sprint of the series. **(6) Finalise trackers + sprint records** and close Stage 4. | 0 known bugs; visual acceptance signed off by the owner on Preview at both viewports; a11y pass clean; every §15 invariant re-verified with evidence; the independent review returns approve (or its findings are fixed and re-reviewed); trackers flipped and Stage 4 closed. | PP7 |

> **Open decision parked for a later PUBLIC sprint (not PP scope):** the new model calls the 33 items "Focus Areas" (4 sections · 33 focus areas · one Simple guide + a many-template grid each, **D-PP-f**) while the public proof band still says "11 focus areas · 33 topics · 200+ · 297 · 120" — the public site stays untouched during PP per owner instruction; reconcile terminology afterwards (logged in `PROJECT-STATUS.md` §5). The 3-card model also leaves the public "**200+ checklist items**" proof number with no private surface behind it — part of the same D-PP-a reconciliation. The OneDrive page-copy docs for the workspace are also stale against the mockup — owner follow-up.

### Sprint exit gate (applies to every sprint)

- [ ] Typecheck + lint + build green locally; CI green
- [ ] Vercel Preview tested, desktop + mobile
- [ ] Relevant `SECURITY-CHECKLIST.md` sections pass (always §15 once auth exists)
- [ ] Copy verbatim; design matches mockups/tokens; numbers correct
- [ ] No new console errors or hydration warnings
- [ ] `PROJECT-STATUS.md` updated in the same PR

## C. Ordering rationale (read once)

Public shell first because it has zero data dependencies and gets a real site live fast. Identity schema (S2) before auth (S3) because Apply writes `profiles` + `applications` on day one. The approval gate (S4) immediately after auth because **no gated content may ship before the gate is proven** — that's why content schema (S5) and private pages (S6) come after. Private pages (S6) complete the MVP product, so **launch (S7) comes next** — the owner ships on the Vercel domain immediately after S6. **Stage 3 is post-launch.** Workspace Visual Polish (S8) and Live Programming (S9) are merged. **Replanned 2026-06-26 (D-S10-b):** make the partner platform crystal-clear and premium (S10) → give the client full content control (S11) → wire the email automations as ready-to-switch-on placeholders (S12). (Earlier post-launch orders ran Live/Email/Hardening as S8–S10, then S8 polish / S9 Live / S10 Email / S11 QA / S12 Hardening — both superseded.) **Re-directed 2026-07-02 (owner):** with S8–S12 merged and the site live, the planned S13 (test-and-fix) / S14 (hardening + relaunch) were retired to the §A backlog; post-launch work continues as owner-added ad-hoc sprints, starting with the **DR series** (public design refresh v3 — foundations + Home first, remaining public pages after). Deliberate tradeoff carried since the MVP launch: public writes (Apply, contact, support) went live without Upstash rate-limiting/Turnstile — accepted knowingly, since the HQ approval gate keeps any abuse out of gated content; the full fix is tracked in the **§A Post-MVP backlog (required before scale)** (`PROJECT-STATUS.md` §5/§7).
