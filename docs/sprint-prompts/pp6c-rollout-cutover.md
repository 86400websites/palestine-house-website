# Sprint PP6c — Rollout: the remaining 21 + cutover + deletion (migration 0030)

| | |
|---|---|
| **Date built** | 2026-08-16 |
| **Branch / PR** | `claude/sprint-pp6c-rollout-cutover` (off `main` = `8417fbb`), 8 gated steps 6c-a … 6c-h |
| **Goal** | Load all 22 real focus areas, flip them Live, then delete the legacy platform — the project's first destructive sprint. |
| **Shape** | TEST only. `0030` + a Storage-API deletion + a production-sourced rollback. Production untouched; the owner applies by hand. |
| **Totals** | 36 files · +1,853 / −59 · **zero files under `src/`** · CSS unchanged at 180,418 bytes throughout |

## Why this sprint exists

PP6b proved the pipeline on one focus area. PP6c ran it over all 22, then executed
D-PP-k: the old 33 focus areas, 33 guide bodies and 297 template rows + Storage
objects are deleted, not archived. Every prior sprint has been reversible. This
one is not, which is why half of it is interlocks.

## What shipped

**6c-a — the plan survived its own verification, a first for the PP series.**
PP6a's kickoff found six load-bearing errors and PP6b's twelve. PP6c's found
**zero blocking**. The spec regenerated byte-identical (`ef37d9d3…`); all 22
titles, slugs, photos and per-area counts agreed with the migration map; the
loader's four guards were read rather than trusted. ⭐ One new fact dissolved the
sprint's hardest logistics: **TEST's 297 legacy Storage objects are byte-identical
to production's**, so the mandatory cold backup could be pulled from TEST and
*proven* equal to production's bytes. 🔒 And a safety finding — `.env.local` had
been pointed at **PRODUCTION** with a service key added; the loader's TEST-ref
assertion is why that was never a production write.

**6c-b — the cold backup (D-PP-k's hard prerequisite).** 297 objects ·
5,320,962 bytes · fingerprint `6fde792718130d12071b69459f9d70ab`, agreed **three
independent ways**: production's metadata, the script's hash of the downloaded
bytes, and a separate `find`+`md5sum`+`stat` pipeline. Read-only script; no
delete, no upload, no database write.

**6c-c — 22 photographs copied, 33 originals untouched.** Which is exactly why
D-PP-o says *copy, never rename*: a rename would have broken all 33 live focus
areas at this step instead of at cutover.

**6c-d — the rollout.** 14 groups · 55 topics · 409 resources · 407 objects
(297 legacy + 110 new). The four new groups came out **5/6/6/5**, exactly the
migration map. The loader refused its first run — *"1 of the targeted focus
area(s) are LIVE"* — **in the dry run, before writing anything**.

**6c-e — the partner path, run rather than reasoned.** Draft **9/9** (33 topics
not 55 · all 22 guide bodies `no rows` · direct Storage signing **refused** ·
folder lists 0 entries). Live **12/12** (55 topics · every guide body at its
exact length · templates byte-exact against the source `.docx`). Plus the visual
at 1440px and 320px: 0 broken images, no horizontal overflow, the reader printing
its title **once**, and Ctrl/⌘+K spanning all three types.

**6c-f — the cutover, and a rollback that was executed rather than written down.**
Four flips, each read back. **The cutover hides content, it never destroys it** —
after each reverse, all 409 rows and 407 objects were still present.

**6c-g — `0030`.** TEST end state: **22 elements · 22 topics · 4 groups · 112
resources · 110 objects · 0 orphans**, 8/8 verification checks, partner path
11/11 afterwards.

**6c-h — the exit gate**, which found the public-pages gap below.

## The five findings that changed the work

1. **A SQL delete of `storage.objects` ORPHANS the bytes.** Supabase's guidance is
   explicit. So `0030` does not touch storage at all; `delete-297-objects.ts`
   removes files through the API **before** the migration (the rows carry the
   paths), and refuses to start unless the backup re-verifies from disk *and*
   every object is present in it **by name and content hash**. In the migration it
   would have dropped metadata, stranded 5.3 MB, and reported success.

2. **TEST and PRODUCTION hold different generations of the legacy content.**
   Only element A1 is identical; `simple_guide_md` totals **630,020** chars on
   production against **872,068** on TEST. The whole sprint had been working on
   TEST, so generating the rollback there was the obvious move — and it would have
   restored **the wrong generation of the owner's prose to production**, revealed
   only during a real emergency. Generated read-only from production instead.

3. **Deleting the elements cascades into three retired tables** —
   `checklist_items` (818), `academy_modules` (33), `checklist_progress` (15).
   866 rows would have gone silently. Owner's decision **A**: delete them
   explicitly so the loss is visible; the rollback does not restore them.

4. **Two foreign keys set the deletion order, and one is a trap.**
   `resources.element_id` is **ON DELETE SET NULL** — deleting elements first
   would have *orphaned* the 297 rows rather than removed them. `platform_topics
   .group_id` is **ON DELETE RESTRICT**. Hence resources → topics → groups →
   elements.

5. **The down-migration cannot be committed.** D-PP-k requires the 33 guide bodies
   as literal inserts, making it **1.75 MB of approval-gated prose** — and this
   repository is public. Committing it would publish the private corpus in one
   commit and break the S7 Step 7 invariant. Gitignored, regenerable in one
   command, documented in `0030_content_v2_cutover.down.README.md`.

## Checks & results

typecheck ✅ · lint ✅ · build ✅ · **CSS unchanged at 180,418 bytes** (zero `src/`
files changed) · path guard clean — no public page, API route, middleware,
`lib/site.ts` or `next.config.ts` in the diff.

| Proof | Result |
|---|---|
| Cold backup | 297 · 5,320,962 B · `6fde7927…` — three independent agreements |
| Partner path, Draft | 9/9 |
| Partner path, Live | 12/12 |
| Partner path, post-`0030` | 11/11 |
| `0030` verification | 8/8 |
| Cutover round trips | 4 (script ×3 + break-glass SQL ×1) |
| Down-migration inserts validated | 373/373 offline; 10 executed against TEST |

## Deviations & learnings

**Four probe bugs. Zero product defects found by probes.** An integrity query that
hardcoded one bucket and flagged the two public booklets; a template match **by
title** that produced a false security-leak reading (the legacy `Monthly Content
Calendar` on element G1 — *the same defect PP6b's first review round found in the
loader, reappearing in a different file*); a validator that assumed one statement
per line when the bodies contain newlines; and the same validator mangled by bash
`$` escaping. Every one read as a product defect for a moment.

**The lesson that generalises:** the title-matching bug had already been found,
reviewed and fixed once — in the loader. It came back because *the fix was applied
where the bug was found, not where the reasoning was wrong*. A defect class is not
closed until the reasoning behind it is.

**And the one that nearly shipped quietly:** generating the rollback from TEST
would have passed every check in this sprint. Nothing would have failed, no
assertion would have fired, and the file would have looked perfect — until someone
ran it on production during an incident and restored a different generation of the
owner's words. It was caught only by comparing the two databases, which nothing
asked for.

## ⚠️ Owner actions outstanding

| # | Item |
|---|---|
| 1 | **Apply to PRODUCTION, in this exact order:** preflight (`0030_verify_PROD_safe_readonly.sql`) → rollout → `backup-297-objects` → `delete-297-objects` → `0030_..._up.sql` → postflight. The order is not negotiable: backup before delete, objects before rows. |
| 2 | **Rehearse the 1.75 MB down-migration once in the SQL Editor.** It has never been executed end to end — no channel in the build environment can push that much SQL. Its 373 inserts are validated and 10 were run live, but a rollback that has never been run is a hypothesis, and this sprint deleted content on the strength of it. |
| 3 | **`--target prod` (D-PP-r) is designed but NOT built.** PP6c loaded content to TEST only. Production still needs its 22 focus areas and 110 files. |
| 4 | **PP7 must rewrite the public focus-area map, not just the proof numbers.** See below. |
| 5 | Decide whether the repo should go private so the down-migration can be committed. |

## 🔴 The gap this exit gate found: D-PP-a is bigger than four numbers

D-PP-a was recorded as *"public proof numbers"*. Grepped at 6c-h, it is not.
`src/app/focus-areas/page.tsx` is a **full public map of the retired IA by name** —
11 named focus areas and 33 named topics, every one of which `0030` has now
deleted. `/our-support` repeats *"11 focus areas, 33 topics, 297 templates"* in
prose and metadata; `/bring-ph` carries the proof band.

**Nothing is broken for a visitor** — these pages are static marketing copy with no
database read — but the public site now advertises 33 focus areas by name that
exist nowhere. PP7's scope has been widened accordingly, with the note that the
new public wording is **copy, a locked input**: the owner supplies or approves it
before PP7 writes a line.


---

## The independent review — BLOCKING, and right for the third time in this series

Booked at the exit gate over the whole PP1→PP6c body of work. **11 blocking + 4
medium findings.** Six are fixed here; the rest became **PP7 (new)**, and the old
PP7 was renumbered **PP8**.

### Fixed in this sprint

| # | Finding | Evidence |
|---|---|---|
| 🔴 | **The gated corpus was already public.** `docs/content-v2-spec.json` carries all 22 guide bodies — 68,492 chars — tracked since `664b7ec` (PP6b). | Verified three ways: tracked, 22/22 bodies, introducing commit an ancestor of public `main`. **Owner declassified it, 2026-08-16.** |
| 🔴 | **A fifth way the cover strip ate owner structure.** A heading could still be consumed as the continuation of a wrapped title. | Reproduced, fixed, 4 new cases. |
| 🔴 | **Still non-idempotent at the removal cap.** | Reproduced with `Alpha Beta`, fixed. |
| 🔴 | **Blanket-gate failure open since `0013`** — `programming_sessions_select_own` had ownership but no approval check. | Fixed in `0031`, applied + verified on TEST. |
| 🔴 | **`--target prod` was silently ignored**, so a run intended for production went to TEST. | `scripts/lib/argv.ts`; verified it now refuses. |
| 🟠 | **"Read-only" script mutated Auth state** via global-scope `signOut()`. | Local scope, in `finally`. |

### The disclosure, and why it was missed here

This sprint was careful about the 297 object paths at 6c-b and about the
down-migration at 6c-g — and *inherited* the spec file without ever asking whether
it was already tracked. **Caution applied to new artefacts does not extend itself
to old ones.** The withholding was also, in hindsight, incoherent: the same content
was already public by another route.

### What the review refuted

**Four of the eight claims put to it.** C1 — the `0030` guard counts *any* 22 live
numeric-coded rows, so a decoy passes. C3 — the backup verifier can overwrite the
archive before the aggregate check fails. C5 — SQL and API deletion are uncoupled
in both directions. C7 — the disclosure above. C2, C4, C6 and C8 held.

**The pattern across all three rounds is the same one PP6b named and this sprint
still did not escape:** a test that exercises the safe variant of a risk reads as
coverage and is not. The suite already had a case for two consecutive *headings*
concatenating to the title, and it passed. The dangerous shape — a plain line
followed by a heading — had never been written, in a round that had explicitly gone
looking for exactly this class of bug.

### And the tally worth carrying forward

**Five probe bugs this sprint. Zero product defects found by probes.** Every real
defect came either from running the product or from the reviewer. The probes found
nothing but their own mistakes — a hardcoded bucket, a title match, a
one-statement-per-line assumption, bash `$` mangling, and `NOT (a OR NULL)` being
NULL in SQL. That is worth knowing before trusting the next verification script.
